  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-29 at 07:16. */
  
  import org.springframework.beans.factory.annotation.Autowired;
  import org.springframework.beans.factory.annotation.Qualifier;
  import com.cloudframe.app.callvp77.file.*;
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.exception.Terminate;
  import java.util.ArrayList;
  import com.cloudframe.app.data.Field;
  import java.util.Map;
  import java.util.HashMap;
  import com.cloudframe.app.callvp77.dto.Dt1SrtFixedArea;
  import com.cloudframe.app.process.Vp776a00;
  import com.cloudframe.app.callvp77.dto.*;
  import com.cloudframe.app.callvp77.dto.SrpSortRequestParameters;
  import com.cloudframe.app.callvp77.file.records.OutputRec;
  import com.cloudframe.app.callvp77.file.records.InputRec;
  import com.cloudframe.app.callvp77.dto.InSrtArray;
  import com.cloudframe.app.callvp77.dto.Dt1SrtContainer;
  import com.cloudframe.app.callvp77.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("callvp77")
  
  public class Callvp77 extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Callvp77.class);
  
  private SrpSortRequestParameters srpSortRequestParameters = new SrpSortRequestParameters() ;
  private OutputRec outputRec = new OutputRec() ;
  private InputRec inputRec = new InputRec() ;
  private InSrtArray inSrtArray = new InSrtArray() ;
  private Dt1SrtContainer dt1SrtContainer = new Dt1SrtContainer() ;
  private Work work = new Work() ;
  
  
  
  @Autowired 
  @Qualifier("callvp77_inputFile")
  InputFile inputFile;
  @Autowired 
  @Qualifier("callvp77_outputFile")
  OutputFile outputFile;
  @Autowired 
  @Qualifier("vp776a00")
  Vp776a00 vp776a00;
  
  
  
  
  
  
      /**
      * process 
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");
          ;
//  PERFORM 0000-MAINLINE
          mainline();/*0000-MAINLINE SECTION*/
          if (this.isProgramEnded()) {
              return getRc();
          }
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
        finally {
      		if(inputFile.hasOpened() && !inputFile.isReadOnly()) { 
      			inputFile.flush(); 
      		}
      		if(outputFile.hasOpened() && !outputFile.isReadOnly()) { 
      			outputFile.flush(); 
      		}
      }
      
       return getRc(); // Exit with return code
      // end of process method
      }
      /**
      * mainline 
      *   This method is derived from 
  *   COBOL Paragraph - 0000-MAINLINE SECTION COBOL Cyclomatic complexity - 4
      * Input  :  

      * - noItemsTobeSorted              COBOL Name: NO-ITEMS-TOBE-SORTED
      *
      * Output :  

      * - noItemsTobeSorted              COBOL Name: NO-ITEMS-TOBE-SORTED
      *
      * @throws CFException
      */
      private void mainline() throws Exception {
//  PERFORM 0010-POPULATE-IN-SRT-ARRAY
          populateInSrtArray();/*0010-POPULATE-IN-SRT-ARRAY*/
          if (this.isProgramEnded()) {
              return ;
          }
//  PERFORM VARYING NO-ITEMS-TOBE-SORTED FROM 100 BY 100 UNTIL NO-ITEMS-TOBE-SORTED > 800
          for (work.setNoItemsTobeSorted(100); (	( work.getNoItemsTobeSorted() <= 800 )) ; work.setNoItemsTobeSorted(work.getNoItemsTobeSorted() + 100) ) {
//  PERFORM 0020-SORT-ARRAY
              sortArray();/*0020-SORT-ARRAY*/
              if (this.isProgramEnded()) {
                  return ;
              }
          }
//  CLOSE OUTPUT-FILE
          outputFile.close(); 
          work.setOutputStatus(outputFile.getStatus() );
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          this.setProgramEnded(true);
          return ;
      
      }
      /**
      * populateInSrtArray 
      *   This method is derived from 
  *   COBOL Paragraph - 0010-POPULATE-IN-SRT-ARRAY COBOL Cyclomatic complexity - 7
      * Input  :  

      * - inSrtCnt                       COBOL Name: IN-SRT-CNT
      *
      * Output :  

      * - inputStatus                    COBOL Name: INPUT-STATUS
      * - outputStatus                   COBOL Name: OUTPUT-STATUS
      * - rc                             COBOL Name: RETURN-CODE
      * - eofFlag                        COBOL Name: WS-EOF-FLAG
      * - inputRec                       COBOL Name: INPUT-REC
      * - inSrtCnt                       COBOL Name: IN-SRT-CNT
      * - inSrtItem                      COBOL Name: IN-SRT-ITEM
      *
      * @throws CFException
      */
      private void populateInSrtArray() throws Exception {
			// Declare local variables used in the method
			int inputStatus = 0;
			int outputStatus = 0;
			// End of variable declaration

//  OPEN INPUT INPUT-FILE
          inputFile.open(new String(CONSTANTS.MODE_READ_ONLY_36242),inputFile.getFileName(),inputFile.getInputFileCharSet(),inputFile.getInputFileCrlfFlag());
          work.setInputStatus(inputFile.getStatus() );
          inputStatus = work.getInputStatus();
//  IF INPUT-STATUS NOT = 0
          if (	( inputStatus != 0 )) { 
              // MOVE 16 TO RETURN-CODE
              this.setRc( 16);
//  DISPLAY 'Error Opening INPUT File: ' INPUT-STATUS
              logger.info("Error Opening INPUT File: {}", String.valueOf(work.getInputStatus())); 
//  GOBACK
              setNotLogged(false); // no need to log, it is a normal termination
              this.setProgramEnded(true);
              return ;
          }
  
//  OPEN OUTPUT OUTPUT-FILE
          outputFile.open(new String(CONSTANTS.MODE_WRITE_ONLY_36397),outputFile.getFileName(),outputFile.getOutputFileCharSet(),outputFile.getOutputFileCrlfFlag());
          work.setOutputStatus(outputFile.getStatus() );
          outputStatus = work.getOutputStatus();
//  IF OUTPUT-STATUS NOT = 0
          if (	( outputStatus != 0 )) { 
              // MOVE 16 TO RETURN-CODE
              this.setRc( 16);
//  DISPLAY 'Error Opening OUTPUT File: ' OUTPUT-STATUS
              logger.info("Error Opening OUTPUT File: {}", String.valueOf(work.getOutputStatus())); 
//  GOBACK
              setNotLogged(false); // no need to log, it is a normal termination
              this.setProgramEnded(true);
              return ;
          }
  
//  MOVE +0 TO WS-EOF-FLAG
          work.setEofFlag((short)0);
//  PERFORM UNTIL NO-MORE-RECORDS
          while ((!(work.isNoMoreRecords()) )) {
//  READ INPUT-FILE
              	inputFile.read();
              work.setInputStatus(inputFile.getStatus() );
              if (!inputFile.hasEnded()) {
              	inputRec.setString(inputFile.getRecord());
              }
//  AT END
              if (inputFile.hasEnded()) {  
//  SET NO-MORE-RECORDS TO TRUE
                  work.setNoMoreRecordsTrue(); 
                  
                }
//  IF NO-MORE-RECORDS
//  ELSE
              if (!(work.isNoMoreRecords()) ) { 
//  ADD 1 TO IN-SRT-CNT
                  work.setInSrtCnt(work.getInSrtCnt()+1);
//  MOVE INPUT-REC TO IN-SRT-ITEM ( IN-SRT-CNT )
                  inSrtArray.setInSrtItem((work.getInSrtCnt() - 1),inputRec.getCharArray());
              }
          }
//  CLOSE INPUT-FILE
          inputFile.close(); 
          work.setInputStatus(inputFile.getStatus() );
      
      }
      /**
      * sortArray 
      *   This method is derived from 
  *   COBOL Paragraph - 0020-SORT-ARRAY COBOL Cyclomatic complexity - 11
      * Input  :  

      * - noItemsTobeSorted              COBOL Name: NO-ITEMS-TOBE-SORTED
      * - j                              COBOL Name: J
      * - inSrtItem                      COBOL Name: IN-SRT-ITEM
      *
      * Output :  

      * - outputRec                      COBOL Name: OUTPUT-REC
      * - srpSortRequestParameters       COBOL Name: SRP-SORT-REQUEST-PARAMETERS
      * - j                              COBOL Name: J
      * - dt1SrtItem                     COBOL Name: DT1-SRT-ITEM
      * - inSrtItem                      COBOL Name: IN-SRT-ITEM
      * - srpEntryCount                  COBOL Name: SRP-ENTRY-COUNT
      * - dt1SrtItemCnt                  COBOL Name: DT1-SRT-ITEM-CNT
      * - noItemsTobeSorted              COBOL Name: NO-ITEMS-TOBE-SORTED
      * - srpEntryLength                 COBOL Name: SRP-ENTRY-LENGTH
      * - srpKeyStart                    COBOL Name: SRP-KEY-START
      * - srpKeyLength                   COBOL Name: SRP-KEY-LENGTH
      * - srpSequence                    COBOL Name: SRP-SEQUENCE
      * - rc                             COBOL Name: RETURN-CODE
      * - srpReturnCode                  COBOL Name: SRP-RETURN-CODE
      *
      * @throws CFException
      */
      private void sortArray() throws Exception {
			// Declare local variables used in the method
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			Map<String,Object> updated = null;
			Dt1SrtFixedArea dt1SrtFixedArea = dt1SrtContainer.getDt1SrtFixedArea();
			Dt1SrtArray dt1SrtArray = dt1SrtContainer.getDt1SrtFixedArea().getDt1SrtArray();
			// End of variable declaration

//  MOVE SPACES TO OUTPUT-REC
          outputRec.setString(CONSTANTS.SPACE_132);
//  STRING '>>> Sorting ' NO-ITEMS-TOBE-SORTED ' Entries in table' DELIMITED BY SIZE INTO OUTPUT-REC END-STRING
          charArray = new ArrayList<char[]>();
             charArray.add(CONSTANTS.LITERAL_1341260190);
             charArray.add(String.valueOf(work.getNoItemsTobeSortedString()).toCharArray());
             charArray.add(CONSTANTS.LITERAL_B3_Entriesintable);
          joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2));
          updated = updateString(outputRec.toCharArray() ,joinCharArray);
          outputRec.setString(  (char[])updated.get("string"));
//  PERFORM 8000-WRITE-OUTPUT
          writeOutput();/*8000-WRITE-OUTPUT*/
          if (this.isProgramEnded()) {
              return ;
          }

// *
//  INITIALIZE SRP-SORT-REQUEST-PARAMETERS
          srpSortRequestParameters.initialize();
//  PERFORM VARYING J FROM 1 BY 1 UNTIL J > NO-ITEMS-TOBE-SORTED
          for (work.setJ(1); (	( work.getJ() <= work.getNoItemsTobeSorted() )) ; work.setJ(work.getJ() + 1) ) {
//  MOVE IN-SRT-ITEM (J) TO DT1-SRT-ITEM (J)
              dt1SrtContainer.getDt1SrtFixedArea().getDt1SrtArray().setDt1SrtItem((work.getJ() - 1),inSrtArray.getInSrtItem(work.getJ() - 1));
          }

// *
//  MOVE NO-ITEMS-TOBE-SORTED TO SRP-ENTRY-COUNT DT1-SRT-ITEM-CNT
          srpSortRequestParameters.setSrpEntryCount(work.getNoItemsTobeSorted());
          dt1SrtFixedArea.setDt1SrtItemCnt(work.getNoItemsTobeSorted());
  
//  MOVE LENGTH OF DT1-SRT-ITEM TO SRP-ENTRY-LENGTH
          srpSortRequestParameters.setSrpEntryLength(Dt1SrtItem.getDt1SrtItemFieldLength());

// * Sort by descending key
//  MOVE 1 TO SRP-KEY-START
          srpSortRequestParameters.setSrpKeyStart(1);
//  MOVE 62 TO SRP-KEY-LENGTH
          srpSortRequestParameters.setSrpKeyLength(62);
//  SET SRP-DESCENDING-SORT TO TRUE
          srpSortRequestParameters.setSrpDescendingSortTrue(); 
          
//  CALL VP776A00 USING OMITTED_1 , OMITTED_1 , SRP-SORT-REQUEST-PARAMETERS , DT1-SRT-ARRAY END-CALL
          // CALL VP776A00 USING OMITTED_1 , OMITTED_1 , SRP-SORT-REQUEST-PARAMETERS , DT1-SRT-ARRAY END-CALL
               this.setRc( vp776a00.call((Field)null,(Field)null,srpSortRequestParameters,dt1SrtFixedArea.getDt1SrtArray()));
  
//  EVALUATE TRUE
          if  ( srpSortRequestParameters.isSrpGoodReturn()  ) { 
              ;
          }
          else   { 
//  MOVE SPACES TO OUTPUT-REC
              outputRec.setString(CONSTANTS.SPACE_132);
//  STRING '>> SORT FAILED : SRP-RETURN-CODE = ' SRP-RETURN-CODE DELIMITED BY SIZE INTO OUTPUT-REC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_878792248);
                 charArray.add(srpSortRequestParameters.getSrpReturnCode());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1));
              updated = updateString(outputRec.toCharArray() ,joinCharArray);
              outputRec.setString(  (char[])updated.get("string"));
//  PERFORM 8000-WRITE-OUTPUT
              writeOutput();/*8000-WRITE-OUTPUT*/
              if (this.isProgramEnded()) {
                  return ;
              }
//  GOBACK
              setNotLogged(false); // no need to log, it is a normal termination
              this.setProgramEnded(true);
              return ;
          }

// *
//  DISPLAY ' '
          logger.info(" "); 
//  MOVE SPACES TO OUTPUT-REC
          outputRec.setString(CONSTANTS.SPACE_132);
//  PERFORM 8000-WRITE-OUTPUT
          writeOutput();/*8000-WRITE-OUTPUT*/
          if (this.isProgramEnded()) {
              return ;
          }

// *
          // MOVE 'Printing Sorted Items : ' TO OUTPUT-REC
          outputRec.setString(CONSTANTS.LITERAL_1336197854);
//  PERFORM 8000-WRITE-OUTPUT
          writeOutput();/*8000-WRITE-OUTPUT*/
          if (this.isProgramEnded()) {
              return ;
          }

// *
          // MOVE '>>>>>>>>>>>>>>>>>>>>>' TO OUTPUT-REC
          outputRec.setString(CONSTANTS.LITERAL_2082492998);
//  PERFORM 8000-WRITE-OUTPUT
          writeOutput();/*8000-WRITE-OUTPUT*/
          if (this.isProgramEnded()) {
              return ;
          }

// *
//  PERFORM VARYING J FROM 1 BY 1 UNTIL J > NO-ITEMS-TOBE-SORTED
          for (work.setJ(1); (	( work.getJ() <= work.getNoItemsTobeSorted() )) ; work.setJ(work.getJ() + 1) ) {
//  MOVE SPACES TO OUTPUT-REC
              outputRec.setString(CONSTANTS.SPACE_132);
//  STRING '    ' DT1-SRT-ITEM (J) DELIMITED BY SIZE INTO OUTPUT-REC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.SPACE_4);
                 charArray.add(dt1SrtContainer.getDt1SrtFixedArea().getDt1SrtArray().getDt1SrtItem(work.getJ() - 1).toCharArray());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1));
              updated = updateString(outputRec.toCharArray() ,joinCharArray);
              outputRec.setString(  (char[])updated.get("string"));
//  PERFORM 8000-WRITE-OUTPUT
              writeOutput();/*8000-WRITE-OUTPUT*/
              if (this.isProgramEnded()) {
                  return ;
              }
//  DISPLAY '    ' DT1-SRT-ITEM (J)
              logger.info("    {}", dt1SrtContainer.getDt1SrtFixedArea().getDt1SrtArray().getDt1SrtItem(work.getJ() - 1).toString()); 
          }

// *
          // MOVE '<<<<<<<<<<<<<<<<<<<<<' TO OUTPUT-REC
          outputRec.setString(CONSTANTS.LITERAL_1985733948);
//  PERFORM 8000-WRITE-OUTPUT
          writeOutput();/*8000-WRITE-OUTPUT*/
          if (this.isProgramEnded()) {
              return ;
          }
//  MOVE SPACES TO OUTPUT-REC
          outputRec.setString(CONSTANTS.SPACE_132);
//  PERFORM 8000-WRITE-OUTPUT
          writeOutput();/*8000-WRITE-OUTPUT*/
          if (this.isProgramEnded()) {
              return ;
          }
          ;
      
      }
      /**
      * writeOutput 
      *   This method is derived from 
  *   COBOL Paragraph - 8000-WRITE-OUTPUT COBOL Cyclomatic complexity - 3
      * Input  :  

      * - outputRec                      COBOL Name: OUTPUT-REC
      *
      * Output :  

      * - outputStatus                   COBOL Name: OUTPUT-STATUS
      * - rc                             COBOL Name: RETURN-CODE
      *
      * @throws CFException
      */
      private void writeOutput() throws Exception {
			// Declare local variables used in the method
			int outputStatus = 0;
			// End of variable declaration

      
// *
//  WRITE OUTPUT-REC
          outputFile.write(outputRec.toCharArray()); 
          outputRec.setString(CONSTANTS.LOW_VALUE_215751987);
          work.setOutputStatus(outputFile.getStatus() );
          outputStatus = work.getOutputStatus();
//  IF OUTPUT-STATUS NOT = 0
          if (	( outputStatus != 0 )) { 
              // MOVE 16 TO RETURN-CODE
              this.setRc( 16);
//  DISPLAY 'Error Writing OUTPUT File: ' OUTPUT-STATUS
              logger.info("Error Writing OUTPUT File: {}", String.valueOf(work.getOutputStatus())); 
//  GOBACK
              setNotLogged(false); // no need to log, it is a normal termination
              this.setProgramEnded(true);
              return ;
          }
  
      
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
      public void setFromVp776a00(Object[] params) {
      int len = params.length;
         if (len > 2)
         if(params[2] instanceof Field) 
   srpSortRequestParameters.setString(((Field)params[2] ).toCharArray());
 else    srpSortRequestParameters.setString((char[])params[2] );
         if (len > 3)
         if(params[3] instanceof Field) 
   dt1SrtContainer.getDt1SrtFixedArea().getDt1SrtArray().setString(((Field)params[3] ).toCharArray());
 else    dt1SrtContainer.getDt1SrtFixedArea().getDt1SrtArray().setString((char[])params[3] );
      }
  
  
  
  
  
  }
