  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-23 at 10:01. */
  
  import org.springframework.beans.factory.annotation.Autowired;
  import org.springframework.beans.factory.annotation.Qualifier;
  import com.cloudframe.app.vsammon4.file.*;
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.vsammon4.file.Scanner;
  import com.cloudframe.app.vsammon4.dto.Input;
  import com.cloudframe.app.data.Field;
  import java.util.ArrayList;
  import java.util.Map;
  import java.util.HashMap;
  import com.cloudframe.app.vsammon4.dto.OpenioData;
  import com.cloudframe.app.utility.CFUtil;
  import com.cloudframe.app.vsammon4.dto.InsertData;
  import com.cloudframe.app.vsammon4.dto.DeleteData;
  import com.cloudframe.app.vsammon4.dto.RewriteData;
  import com.cloudframe.app.vsammon4.dto.KeyreadData;
  import com.cloudframe.app.vsammon4.dto.ReadallData;
  import com.cloudframe.app.utility.CFConstants;
  import com.cloudframe.app.vsammon4.dto.StartData;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.vsammon4.dto.*;
  import com.cloudframe.app.vsammon4.file.records.VsvksdsRecordS;
  import com.cloudframe.app.vsammon4.dto.Sysid;
  import com.cloudframe.app.vsammon4.file.records.Reprec;
  import com.cloudframe.app.vsammon4.file.records.VsvksdsRecordO;
  import com.cloudframe.app.vsammon4.file.records.VsvksdsRecordIo;
  import com.cloudframe.app.vsammon4.dto.AcceptInputGroup;
  import com.cloudframe.app.vsammon4.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  
  @Component("vsammon4")
  
  public class Vsammon4 extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Vsammon4.class);
  
  private VsvksdsRecordS vsvksdsRecordS = new VsvksdsRecordS() ;
  private Sysid sysid = new Sysid() ;
  private Reprec reprec = new Reprec() ;
  private VsvksdsRecordO vsvksdsRecordO = new VsvksdsRecordO() ;
  private VsvksdsRecordIo vsvksdsRecordIo = new VsvksdsRecordIo() ;
  private AcceptInputGroup acceptInputGroup = new AcceptInputGroup() ;
  private Work work = new Work() ;
  
  
  
  @Autowired 
  @Qualifier("vsammon4_vsvksdsIo")
  VsvksdsIo vsvksdsIo;
  @Autowired 
  @Qualifier("vsammon4_vsvksdsSeq")
  VsvksdsSeq vsvksdsSeq;
  @Autowired 
  @Qualifier("vsammon4_repout")
  Repout repout;
  @Autowired 
  @Qualifier("vsammon4_vsvksdsOutput")
  VsvksdsOutput vsvksdsOutput;
  
  
  private boolean doNotSkipCloseo = true;
  private boolean doNotSkipOpenio = true;
  private boolean doNotSkipOpenextend = true;
  private boolean doNotSkipCloseio = true;
  private boolean doNotSkipOpeni = true;
  private boolean doNotSkipClosei = true;
  private boolean doNotSkipInsall = true;
  private boolean doNotSkipInsert = true;
  private boolean doNotSkipDelete = true;
  private boolean doNotSkipDlstrd = true;
  private boolean doNotSkipRewrite = true;
  private boolean doNotSkipKeyread = true;
  private boolean doNotSkipReadall = true;
  private boolean doNotSkipReadnxt = true;
  private boolean doNotSkipDelall = true;
  private boolean doNotSkipStart = true;
  
  
    @Autowired
    @Qualifier("vsammon4_scanner")
    Scanner scanner;
  
  
  
      /**
      * process 
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");
          ;
//  PERFORM 0000-MAINLINE
          mainline();/*0000-MAINLINE*/
          if (this.isProgramEnded()) {
              return getRc();
          }
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
        finally {
      		if(vsvksdsIo.hasOpened() && !vsvksdsIo.isReadOnly()) { 
      			vsvksdsIo.flush(); 
      		}
      		if(vsvksdsSeq.hasOpened() && !vsvksdsSeq.isReadOnly()) { 
      			vsvksdsSeq.flush(); 
      		}
      		if(repout.hasOpened() && !repout.isReadOnly()) { 
      			repout.flush(); 
      		}
      		if(vsvksdsOutput.hasOpened() && !vsvksdsOutput.isReadOnly()) { 
      			vsvksdsOutput.flush(); 
      		}
      		if(scanner.hasOpened()) { 
      			scanner.close();
      		}
      }
      
       return getRc(); // Exit with return code
      // end of process method
      }
      /**
      * mainline 
      *   This method is derived from 
  *   COBOL Paragraph - 0000-MAINLINE COBOL Cyclomatic complexity - 25
      * Input  : None 

      * Output :  

      * - input                          COBOL Name: WS-INPUT
      * - inputCommand                   COBOL Name: WS-INPUT-COMMAND
      * - acceptInput                    COBOL Name: WS-ACCEPT-INPUT
      * - reprec                         COBOL Name: REPREC
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      *
      * @throws CFException
      */
      private void mainline() throws Exception {
			// Declare local variables used in the method
			char[] consoleInput = null;
			char[] joinCharArray = null;
			Input input = acceptInputGroup.getInput();
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			Map<String,Object> updated = null;
			// End of variable declaration

//  PERFORM 0010-OPEN-FILES THRU 0010-EXIT
          openFiles();/*0010-OPEN-FILES*/
          if (this.isProgramEnded()) {
              return ;
          }
//  MOVE SPACES TO WS-INPUT
          acceptInputGroup.setInput(CONSTANTS.SPACE_80);
//  PERFORM UNTIL C-TERMINATE
          while ((!(acceptInputGroup.getInput().isCTerminate()) )) {
//  ACCEPT WS-ACCEPT-INPUT
              consoleInput = scanner.nextLine();
              if (!scanner.hasEnded()) {
                 acceptInputGroup.setAcceptInput(consoleInput);  // read from console and assign
              }
//  IF C-COMMENT THEN
              input = acceptInputGroup.getInput();
//  ELSE
              if (!(input.isCComment()) ) { 
//  MOVE SPACES TO REPREC
                  reprec.setString(CONSTANTS.SPACE_300);
//  WRITE REPREC
                  repout.write(reprec.toCharArray()); 
                  reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//  MOVE SPACES TO REPREC
                  reprec.setString(CONSTANTS.SPACE_300);
//  STRING '>>>>>>>> SYSIN Record >>>>>>> ' WS-ACCEPT-INPUT DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_401348835);
                     charArray.add(acceptInputGroup.getAcceptInput());
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
                  repout.write(reprec.toCharArray()); 
                  reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//  MOVE SPACES TO REPREC
                  reprec.setString(CONSTANTS.SPACE_300);
//  WRITE REPREC
                  repout.write(reprec.toCharArray()); 
                  reprec.setString(CONSTANTS.LOW_VALUE_215695350);
              }
  
              // MOVE SPACES TO VSVKSDS-FILE-STATUS
              work.setVsvksdsFileStatus(CONSTANTS.SPACE_2);
              input = acceptInputGroup.getInput();
//  EVALUATE TRUE
              if  ( input.isCOpeno()  ) { 
//  PERFORM 1000-OPENO THRU 1000-EXIT
                  openo();/*1000-OPENO*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCCloseo()  ) { 
//  PERFORM 2000-CLOSEO THRU 2000-EXIT
                  closeo();/*2000-CLOSEO*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCOpenio()  ) { 
//  PERFORM 3000-OPENIO THRU 3000-EXIT
                  openio();/*3000-OPENIO*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCOpenextend()  ) { 
//  PERFORM 3001-OPENEXTEND THRU 3001-EXIT
                  openextend();/*3001-OPENEXTEND*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCCloseio()  ) { 
//  PERFORM 4000-CLOSEIO THRU 4000-EXIT
                  closeio();/*4000-CLOSEIO*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCOpeni()  ) { 
//  PERFORM 5000-OPENI THRU 5000-EXIT
                  openi();/*5000-OPENI*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCClosei()   ||  input.isCCloseextended()  ) { 
//  PERFORM 6000-CLOSEI THRU 6000-EXIT
                  closei();/*6000-CLOSEI*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCInsall()  ) { 
//  PERFORM 7000-INSALL THRU 7000-EXIT
                  insall();/*7000-INSALL*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCInsert()  ) { 
//  PERFORM 8000-INSERT THRU 8000-EXIT
                  insert();/*8000-INSERT*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCDelete()  ) { 
//  PERFORM 9000-DELETE THRU 9000-EXIT
                  delete();/*9000-DELETE*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCDeleteLastRead()  ) { 
//  PERFORM A000-DLSTRD THRU A000-EXIT
                  dlstrd();/*A000-DLSTRD*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCRewrite()  ) { 
//  PERFORM B000-REWRITE THRU B000-EXIT
                  rewrite();/*B000-REWRITE*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCKeyread()  ) { 
//  PERFORM C000-KEYREAD THRU C000-EXIT
                  keyread();/*C000-KEYREAD*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCReadall()  ) { 
//  PERFORM D000-READALL THRU D000-EXIT
                  readall();/*D000-READALL*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCReadnxt()  ) { 
//  PERFORM E000-READNXT THRU E000-EXIT
                  readnxt();/*E000-READNXT*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCDelall()  ) { 
//  PERFORM F000-DELALL THRU F000-EXIT
                  delall();/*F000-DELALL*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  (( input.isCStart()  ) || ( input.isCStartAlternate()  )) { 
//  PERFORM G000-START THRU G000-EXIT
                  start();/*G000-START*/
                  if (this.isProgramEnded()) {
                      return ;
                  }
              }
              else if  ( input.isCComment()  ) { 
                  ;
              }
              else   { 
                  ;
              }
  
          }
//  PERFORM 0020-CLOSE-FILES THRU 0020-EXIT
          closeFiles();/*0020-CLOSE-FILES*/
          if (this.isProgramEnded()) {
              return ;
          }
//cobolCode::GO TO 9999-EXIT-PROGRAM
exitProgram();
 return ;
//cobolCodeEnds::GO TO 9999-EXIT-PROGRAM
      
      }
      /**
      * openFiles 
      *   This method is derived from 
  *   COBOL Paragraph - 0010-OPEN-FILES COBOL Cyclomatic complexity - 1
      * Input  : None 

      * Output :  

      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      *
      * @throws CFException
      */
      private void openFiles() throws Exception {
      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  OPEN OUTPUT REPOUT
          repout.open(new String(CONSTANTS.MODE_WRITE_ONLY_36397),repout.getFileName(),repout.getRepoutCharSet(),repout.getRepoutCrlfFlag());
          ;
      
      }
      /**
      * closeFiles 
      *   This method is derived from 
  *   COBOL Paragraph - 0020-CLOSE-FILES COBOL Cyclomatic complexity - 1
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      private void closeFiles() throws Exception {
      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  CLOSE REPOUT
          repout.close(); 
          ;
      
      }
      /**
      * openo 
      *   This method is derived from 
  *   COBOL Paragraph - 1000-OPENO COBOL Cyclomatic complexity - 3
      * Input  : None 

      * Output :  

      * - openFlags                      COBOL Name: OPEN-FLAGS
      * - vskOPrimaryKey                 COBOL Name: VSK-O-PRIMARY-KEY
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void openo() throws Exception {
			// Declare local variables used in the method
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  SET DYN-MODE TO TRUE
          work.setDynModeTrue(); 
          
          // MOVE 0 TO VSK-O-PRIMARY-KEY
          vsvksdsRecordO.setVskOPrimaryKey((long)0);
//  OPEN OUTPUT VSVKSDS-OUTPUT
          	vsvksdsOutput.setVsamCluster("VSAMMON4" /*program name*/);
          vsvksdsOutput.open("w"); 
          work.setVsvksdsFileStatus(vsvksdsOutput.getStatusString() );
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS NOT = '00'
//  LITERAL_00 = '00'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) != 0 ) { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'Error opening VSVKSDSO ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_Error_B3_openingVSVKSDSO);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO 9999-EXIT-PROGRAM
exitProgram();
 return ;
//cobolCodeEnds::GO TO 9999-EXIT-PROGRAM
          }
  
          // MOVE 'OPEN OUTPUT VSVKSDS Successful' TO REPREC
          reprec.setString(CONSTANTS.LITERAL_1362301232);
//  WRITE REPREC
          repout.write(reprec.toCharArray()); 
          reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          ;
      
      }
      /**
      * closeo 
      *   This method is derived from 
  *   COBOL Paragraph - 2000-CLOSEO COBOL Cyclomatic complexity - 3
      * Input  :  

      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      *
      * Output :  

      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void closeo() throws Exception {
			// Declare local variables used in the method
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  CLOSE VSVKSDS-OUTPUT
          vsvksdsOutput.close(); 
          work.setVsvksdsFileStatus(vsvksdsOutput.getStatusString() );
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS NOT = '00'
//  LITERAL_00 = '00'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) != 0 ) { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'Error Closing VSVKSDSO ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_Error_B3_ClosingVSVKSDSO);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO 9999-EXIT-PROGRAM
exitProgram();
 return ;
//cobolCodeEnds::GO TO 9999-EXIT-PROGRAM
          }
  
          // MOVE 'CLOSE VSVKSDS Successful' TO REPREC
          reprec.setString(CONSTANTS.LITERAL_707202897);
//  WRITE REPREC
          repout.write(reprec.toCharArray()); 
          reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          ;
      
      }
      /**
      * openio 
      *   This method is derived from 
  *   COBOL Paragraph - 3000-OPENIO COBOL Cyclomatic complexity - 4
      * Input  :  

      * - iOpenMode                      COBOL Name: WS-I-OPEN-MODE
      *
      * Output :  

      * - vskIoPrimaryKey                COBOL Name: VSK-IO-PRIMARY-KEY
      * - vskSPrimaryKey                 COBOL Name: VSK-S-PRIMARY-KEY
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      * - openFlags                      COBOL Name: OPEN-FLAGS
      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void openio() throws Exception {
			// Declare local variables used in the method
			OpenioData openioData = acceptInputGroup.getInput().getOpenioData();
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
          // MOVE 0 TO VSK-IO-PRIMARY-KEY VSK-S-PRIMARY-KEY
          vsvksdsRecordIo.setVskIoPrimaryKey((long)0);
          vsvksdsRecordS.setVskSPrimaryKey((long)0);
          openioData = acceptInputGroup.getInput().getOpenioData();
//  IF OPEN-SEQUENTIAL
          if ( openioData.isOpenSequential()  ) { 
//  OPEN I-O VSVKSDS-SEQ
              	vsvksdsSeq.setVsamCluster("VSAMMON4" /*program name*/);
              vsvksdsSeq.open("rw"); 
              work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
//  SET SEQ-MODE TO TRUE
              work.setSeqModeTrue(); 
              
          }
  
//  ELSE
          else { 
//  OPEN I-O VSVKSDS-IO
              	vsvksdsIo.setVsamCluster("VSAMMON4" /*program name*/);
              vsvksdsIo.open("rw"); 
              work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
//  SET DYN-MODE TO TRUE
              work.setDynModeTrue(); 
              
          }
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS NOT = '00'
//  LITERAL_00 = '00'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) != 0 ) { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'Error opening VSVKSDSB ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_Error_B3_openingVSVKSDSB);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO 9999-EXIT-PROGRAM
exitProgram();
 return ;
//cobolCodeEnds::GO TO 9999-EXIT-PROGRAM
          }
  
          // MOVE 'OPEN I-O VSVKSDS Successful' TO REPREC
          reprec.setString(CONSTANTS.LITERAL_1318124640);
//  WRITE REPREC
          repout.write(reprec.toCharArray()); 
          reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          ;
      
      }
      /**
      * openextend 
      *   This method is derived from 
  *   COBOL Paragraph - 3001-OPENEXTEND COBOL Cyclomatic complexity - 3
      * Input  : None 

      * Output :  

      * - openFlags                      COBOL Name: OPEN-FLAGS
      * - vskSPrimaryKey                 COBOL Name: VSK-S-PRIMARY-KEY
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void openextend() throws Exception {
			// Declare local variables used in the method
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  SET SEQ-MODE TO TRUE
          work.setSeqModeTrue(); 
          
          // MOVE 0 TO VSK-S-PRIMARY-KEY
          vsvksdsRecordS.setVskSPrimaryKey((long)0);
//  OPEN EXTEND VSVKSDS-SEQ
          	vsvksdsSeq.setVsamCluster("VSAMMON4" /*program name*/);
          vsvksdsSeq.open("w"); 
          work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS NOT = '00'
//  LITERAL_00 = '00'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) != 0 ) { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'Error opening VSVKSDSB ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_Error_B3_openingVSVKSDSB);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO 9999-EXIT-PROGRAM
exitProgram();
 return ;
//cobolCodeEnds::GO TO 9999-EXIT-PROGRAM
          }
  
          // MOVE 'OPEN EXTEND VSVKSDS Successful' TO REPREC
          reprec.setString(CONSTANTS.LITERAL_8531177);
//  WRITE REPREC
          repout.write(reprec.toCharArray()); 
          reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          ;
      
      }
      /**
      * closeio 
      *   This method is derived from 
  *   COBOL Paragraph - 4000-CLOSEIO COBOL Cyclomatic complexity - 4
      * Input  :  

      * - openFlags                      COBOL Name: OPEN-FLAGS
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      *
      * Output :  

      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void closeio() throws Exception {
			// Declare local variables used in the method
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  IF SEQ-MODE
          if ( work.isSeqMode()  ) { 
//  CLOSE VSVKSDS-SEQ
              vsvksdsSeq.close(); 
              work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
          }
//  ELSE
          else { 
//  CLOSE VSVKSDS-IO
              vsvksdsIo.close(); 
              work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
          }
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS NOT = '00'
//  LITERAL_00 = '00'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) != 0 ) { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'Error Closing VSVKSDSB ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_Error_B3_ClosingVSVKSDSB);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO 9999-EXIT-PROGRAM
exitProgram();
 return ;
//cobolCodeEnds::GO TO 9999-EXIT-PROGRAM
          }
  
          // MOVE 'CLOSE VSVKSDS Successful' TO REPREC
          reprec.setString(CONSTANTS.LITERAL_707202897);
//  WRITE REPREC
          repout.write(reprec.toCharArray()); 
          reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          ;
      
      }
      /**
      * openi 
      *   This method is derived from 
  *   COBOL Paragraph - 5000-OPENI COBOL Cyclomatic complexity - 4
      * Input  :  

      * - iOpenMode                      COBOL Name: WS-I-OPEN-MODE
      *
      * Output :  

      * - vskSPrimaryKey                 COBOL Name: VSK-S-PRIMARY-KEY
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      * - openFlags                      COBOL Name: OPEN-FLAGS
      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void openi() throws Exception {
			// Declare local variables used in the method
			OpenioData openioData = acceptInputGroup.getInput().getOpenioData();
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
          // MOVE 0 TO VSK-S-PRIMARY-KEY
          vsvksdsRecordS.setVskSPrimaryKey((long)0);
          openioData = acceptInputGroup.getInput().getOpenioData();
//  IF OPEN-DYNAMIC
          if ( openioData.isOpenDynamic()  ) { 
//  OPEN INPUT VSVKSDS-IO
              	vsvksdsIo.setVsamCluster("VSAMMON4" /*program name*/);
              vsvksdsIo.open("r"); 
              work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
//  SET DYN-MODE TO TRUE
              work.setDynModeTrue(); 
              
          }
  
//  ELSE
          else { 
//  OPEN INPUT VSVKSDS-SEQ
              	vsvksdsSeq.setVsamCluster("VSAMMON4" /*program name*/);
              vsvksdsSeq.open("r"); 
              work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
//  SET SEQ-MODE TO TRUE
              work.setSeqModeTrue(); 
              
          }
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS NOT = '00'
//  LITERAL_00 = '00'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) != 0 ) { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'Error opening VSVKSDSI ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_Error_B3_openingVSVKSDSI);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO 9999-EXIT-PROGRAM
exitProgram();
 return ;
//cobolCodeEnds::GO TO 9999-EXIT-PROGRAM
          }
  
          // MOVE 'OPEN INPUT VSVKSDS Successful' TO REPREC
          reprec.setString(CONSTANTS.LITERAL_345930975);
//  WRITE REPREC
          repout.write(reprec.toCharArray()); 
          reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          ;
      
      }
      /**
      * closei 
      *   This method is derived from 
  *   COBOL Paragraph - 6000-CLOSEI COBOL Cyclomatic complexity - 4
      * Input  :  

      * - openFlags                      COBOL Name: OPEN-FLAGS
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      *
      * Output :  

      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void closei() throws Exception {
			// Declare local variables used in the method
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  IF DYN-MODE
          if ( work.isDynMode()  ) { 
//  CLOSE VSVKSDS-IO
              vsvksdsIo.close(); 
              work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
          }
//  ELSE
          else { 
//  CLOSE VSVKSDS-SEQ
              vsvksdsSeq.close(); 
              work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
          }
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS NOT = '00'
//  LITERAL_00 = '00'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) != 0 ) { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'Error Closing VSVKSDSI ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_Error_B3_ClosingVSVKSDSI);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO 9999-EXIT-PROGRAM
exitProgram();
 return ;
//cobolCodeEnds::GO TO 9999-EXIT-PROGRAM
          }
  
          // MOVE 'CLOSE VSVKSDS Successful' TO REPREC
          reprec.setString(CONSTANTS.LITERAL_707202897);
//  WRITE REPREC
          repout.write(reprec.toCharArray()); 
          reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          ;
      
      }
      /**
      * insall 
      *   This method is derived from 
  *   COBOL Paragraph - 7000-INSALL COBOL Cyclomatic complexity - 7
      * Input  :  

      * - sub1                           COBOL Name: WS-SUB1
      * - sysid                          COBOL Name: WS-SYSID
      * - numstr                         COBOL Name: WS-NUMSTR
      * - charstr                        COBOL Name: WS-CHARSTR
      * - vsvksdsRecordO                 COBOL Name: VSVKSDS-RECORD-O
      *
      * Output :  

      * - sub1                           COBOL Name: WS-SUB1
      * - vskOPrimaryKey                 COBOL Name: VSK-O-PRIMARY-KEY
      * - sysidNnn                       COBOL Name: WS-SYSID-NNN
      * - vskOSysid                      COBOL Name: VSK-O-SYSID
      * - sysid                          COBOL Name: WS-SYSID
      * - taskNo                         COBOL Name: WS-TASK-NO
      * - numstr                         COBOL Name: WS-NUMSTR
      * - vskOTaskNo                     COBOL Name: VSK-O-TASK-NO
      * - reqSize                        COBOL Name: WS-REQ-SIZE
      * - vskOReqSize                    COBOL Name: VSK-O-REQ-SIZE
      * - vskOVarStruct                  COBOL Name: VSK-O-VAR-STRUCT
      * - charstr                        COBOL Name: WS-CHARSTR
      * - vskRecordSize                  COBOL Name: VSK-RECORD-SIZE
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void insall() throws Exception {
			// Declare local variables used in the method
			char[] vsvksdsFileStatus = null;
			char[] joinCharArray = null;
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  PERFORM VARYING WS-SUB1 FROM 1 BY 1 UNTIL WS-SUB1 > 100
          for (work.setSub1(1); (	( work.getSub1() <= 100 ) ) ; work.setSub1(work.getSub1() + 1) ) {
//  MOVE WS-SUB1 TO VSK-O-PRIMARY-KEY WS-SYSID-NNN
              vsvksdsRecordO.setVskOPrimaryKey( work.getSub1());
              sysid.setSysidNnn(work.getSub1());
              // MOVE WS-SYSID TO VSK-O-SYSID
              vsvksdsRecordO.setVskOSysid(sysid.toCharArray());
//  MOVE WS-NUMSTR ( WS-SUB1 : 7) TO WS-TASK-NO
              try {
              work.setTaskNo(CFUtil.getLong(substring(work.getNumstr(),(work.getSub1()-1), (work.getSub1() + 6) )));
              } catch(NumberFormatException e) {}
//  MOVE WS-TASK-NO TO VSK-O-TASK-NO
              vsvksdsRecordO.setVskOTaskNo( work.getTaskNo());
//  MOVE WS-NUMSTR ( WS-SUB1 : 9) TO WS-REQ-SIZE
              try {
              work.setReqSize(CFUtil.getLong(substring(work.getNumstr(),(work.getSub1()-1), (work.getSub1() + 8) )));
              } catch(NumberFormatException e) {}
//  MOVE WS-REQ-SIZE TO VSK-O-REQ-SIZE
              vsvksdsRecordO.setVskOReqSize((int) work.getReqSize());
//  MOVE WS-CHARSTR ( WS-SUB1 + 4 : WS-SUB1 ) TO VSK-O-VAR-STRUCT
              vsvksdsRecordO.setVskOVarStruct(work.getCharstr(),(work.getSub1() + 4-1)/* charstr */,work.getSub1(),0 /* vskOVarStruct */,vsvksdsRecordO.getVskOVarStruct().length /* replace,sourceIndex,sourceLen,targetIndex,targetLen */);
              work.setVskRecordSize((long) (Math.abs(21 + work.getSub1())));
//  WRITE VSVKSDS-RECORD-O
              vsvksdsOutput.setRecord(substring(vsvksdsRecordO.toCharArray(),0,(int) work.getVskRecordSize()));
              vsvksdsOutput.write((int) work.getVskRecordSize()); 
              work.setVsvksdsFileStatus(vsvksdsOutput.getStatusString() );
//  IF VSVKSDS-FILE-STATUS = '00' OR '02'
//  LITERAL_02 = '02'
              vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  ELSE
              if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) != 0  && 		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_02) != 0 ) { 
//  MOVE SPACES TO REPREC
                  reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'KEY = ' VSK-O-PRIMARY-KEY ' : ' 'Error Writing VSVKSDS Record: File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_972073668);
                     charArray.add(String.valueOf(vsvksdsRecordO.getVskOPrimaryKeyString()).toCharArray());
                     charArray.add(CONSTANTS.LITERAL_B2_CL_);
                     charArray.add(CONSTANTS.LITERAL_2136606000);
                     charArray.add(work.getVsvksdsFileStatus());
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
                  repout.write(reprec.toCharArray()); 
                  reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO 7000-EXIT
return ;
//cobolCodeEnds::GO TO 7000-EXIT
              }
  
          }
//  MOVE SPACES TO REPREC
          reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'WRITE of 100 records with KEY Range 1 to 100 ' 'Written successfully' DELIMITED BY SIZE INTO REPREC END-STRING
          charArray = new ArrayList<char[]>();
             charArray.add(CONSTANTS.LITERAL_2136255016);
             charArray.add(CONSTANTS.LITERAL_Written_B_successfully);
          joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1));
          updated = updateString(reprec.toCharArray() ,joinCharArray);
          reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
          repout.write(reprec.toCharArray()); 
          reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          ;
      
      }
      /**
      * insert 
      *   This method is derived from 
  *   COBOL Paragraph - 8000-INSERT COBOL Cyclomatic complexity - 6
      * Input  :  

      * - openFlags                      COBOL Name: OPEN-FLAGS
      * - iInsertKey                     COBOL Name: WS-I-INSERT-KEY
      * - iTaskNo                        COBOL Name: WS-I-TASK-NO
      * - iReqSize                       COBOL Name: WS-I-REQ-SIZE
      * - charstr                        COBOL Name: WS-CHARSTR
      * - iDataLen                       COBOL Name: WS-I-DATA-LEN
      * - vsvksdsRecordS                 COBOL Name: VSVKSDS-RECORD-S
      * - vsvksdsRecordIo                COBOL Name: VSVKSDS-RECORD-IO
      *
      * Output :  

      * - vskSPrimaryKey                 COBOL Name: VSK-S-PRIMARY-KEY
      * - iInsertKey                     COBOL Name: WS-I-INSERT-KEY
      * - vskSSysid                      COBOL Name: VSK-S-SYSID
      * - vskSTaskNo                     COBOL Name: VSK-S-TASK-NO
      * - iTaskNo                        COBOL Name: WS-I-TASK-NO
      * - vskSReqSize                    COBOL Name: VSK-S-REQ-SIZE
      * - iReqSize                       COBOL Name: WS-I-REQ-SIZE
      * - vskSVarStruct                  COBOL Name: VSK-S-VAR-STRUCT
      * - charstr                        COBOL Name: WS-CHARSTR
      * - vskRecordSize                  COBOL Name: VSK-RECORD-SIZE
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      * - vskIoPrimaryKey                COBOL Name: VSK-IO-PRIMARY-KEY
      * - vskIoSysid                     COBOL Name: VSK-IO-SYSID
      * - vskIoTaskNo                    COBOL Name: VSK-IO-TASK-NO
      * - vskIoReqSize                   COBOL Name: VSK-IO-REQ-SIZE
      * - vskIoVarStruct                 COBOL Name: VSK-IO-VAR-STRUCT
      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void insert() throws Exception {
			// Declare local variables used in the method
			InsertData insertData = acceptInputGroup.getInput().getInsertData();
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  IF SEQ-MODE
          if ( work.isSeqMode()  ) { 
              // MOVE WS-I-INSERT-KEY TO VSK-S-PRIMARY-KEY
              vsvksdsRecordS.setVskSPrimaryKey(acceptInputGroup.getInput().getInsertData().getIInsertKey());
  
//  MOVE 'SEQ5' TO VSK-S-SYSID
//  LITERAL_SEQ5 = 'SEQ5'
              vsvksdsRecordS.setVskSSysid(CONSTANTS.LITERAL_SEQ5);
//  MOVE WS-I-TASK-NO TO VSK-S-TASK-NO
              vsvksdsRecordS.setVskSTaskNo( insertData.getITaskNo());
  
//  MOVE WS-I-REQ-SIZE TO VSK-S-REQ-SIZE
              vsvksdsRecordS.setVskSReqSize((int) insertData.getIReqSize());
  
//  MOVE WS-CHARSTR (1 : WS-I-DATA-LEN ) TO VSK-S-VAR-STRUCT
              vsvksdsRecordS.setVskSVarStruct(work.getCharstr(),0/* charstr */,acceptInputGroup.getInput().getInsertData().getIDataLen(),0 /* vskSVarStruct */,vsvksdsRecordS.getVskSVarStruct().length /* replace,sourceIndex,sourceLen,targetIndex,targetLen */);
              work.setVskRecordSize((long) (Math.abs(21 + acceptInputGroup.getInput().getInsertData().getIDataLen())));
//  WRITE VSVKSDS-RECORD-S
              vsvksdsSeq.setRecord(substring(vsvksdsRecordS.toCharArray(),0,(int) work.getVskRecordSize()));
              vsvksdsSeq.write((int) work.getVskRecordSize()); 
              work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
          }
//  ELSE
          else { 
              // MOVE WS-I-INSERT-KEY TO VSK-IO-PRIMARY-KEY
              vsvksdsRecordIo.setVskIoPrimaryKey(acceptInputGroup.getInput().getInsertData().getIInsertKey());
  
//  MOVE 'S0W1' TO VSK-IO-SYSID
//  LITERAL_S0W1 = 'S0W1'
              vsvksdsRecordIo.setVskIoSysid(CONSTANTS.LITERAL_S0W1);
//  MOVE WS-I-TASK-NO TO VSK-IO-TASK-NO
              vsvksdsRecordIo.setVskIoTaskNo( insertData.getITaskNo());
  
//  MOVE WS-I-REQ-SIZE TO VSK-IO-REQ-SIZE
              vsvksdsRecordIo.setVskIoReqSize((int) insertData.getIReqSize());
  
//  MOVE WS-CHARSTR (1 : WS-I-DATA-LEN ) TO VSK-IO-VAR-STRUCT
              vsvksdsRecordIo.setVskIoVarStruct(work.getCharstr(),0/* charstr */,acceptInputGroup.getInput().getInsertData().getIDataLen(),0 /* vskIoVarStruct */,vsvksdsRecordIo.getVskIoVarStruct().length /* replace,sourceIndex,sourceLen,targetIndex,targetLen */);
              work.setVskRecordSize((long) (Math.abs(21 + acceptInputGroup.getInput().getInsertData().getIDataLen())));
//  WRITE VSVKSDS-RECORD-IO
              vsvksdsIo.setRecord(substring(vsvksdsRecordIo.toCharArray(),0,(int) work.getVskRecordSize()));
              vsvksdsIo.write((int) work.getVskRecordSize()); 
              work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
          }
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS = '00' OR '02'
//  LITERAL_02 = '02'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) == 0  || 		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_02) == 0 ) { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'WRITE KEY = ' WS-I-INSERT-KEY ' Successful : ' ' File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_250737499);
                 charArray.add(String.valueOf(insertData.getIInsertKeyString()).toCharArray());
                 charArray.add(CONSTANTS.LITERAL_B3_SuccessfulCL_);
                 charArray.add(CONSTANTS.LITERAL_382262025);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
  
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          }
  
//  ELSE
          else { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'KEY = ' WS-I-INSERT-KEY ' : ' 'Error Writing VSVKSDS Record: File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_972073668);
                 charArray.add(String.valueOf(insertData.getIInsertKeyString()).toCharArray());
                 charArray.add(CONSTANTS.LITERAL_B2_CL_);
                 charArray.add(CONSTANTS.LITERAL_2136606000);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
  
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO 8000-EXIT
return ;
//cobolCodeEnds::GO TO 8000-EXIT
          }
          ;
      
      }
      /**
      * delete 
      *   This method is derived from 
  *   COBOL Paragraph - 9000-DELETE COBOL Cyclomatic complexity - 7
      * Input  :  

      * - openFlags                      COBOL Name: OPEN-FLAGS
      * - vskSPrimaryKey                 COBOL Name: VSK-S-PRIMARY-KEY
      * - dKey                           COBOL Name: WS-D-KEY
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      *
      * Output :  

      * - primaryKeyDisp                 COBOL Name: PRIMARY-KEY-DISP
      * - vskSPrimaryKey                 COBOL Name: VSK-S-PRIMARY-KEY
      * - dKey                           COBOL Name: WS-D-KEY
      * - vskIoPrimaryKey                COBOL Name: VSK-IO-PRIMARY-KEY
      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void delete() throws Exception {
			// Declare local variables used in the method
			DeleteData deleteData = acceptInputGroup.getInput().getDeleteData();
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  IF SEQ-MODE
          if ( work.isSeqMode()  ) { 
              // MOVE VSK-S-PRIMARY-KEY TO PRIMARY-KEY-DISP
              work.setPrimaryKeyDisp(vsvksdsRecordS.getVskSPrimaryKey());
              // MOVE WS-D-KEY TO VSK-S-PRIMARY-KEY
              vsvksdsRecordS.setVskSPrimaryKey(acceptInputGroup.getInput().getDeleteData().getDKey());
  
//  DELETE VSVKSDS-SEQ
              vsvksdsSeq.deleteLastReadRecord();
              work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
          }
//  ELSE
          else { 
              // MOVE WS-D-KEY TO VSK-IO-PRIMARY-KEY
              vsvksdsRecordIo.setVskIoPrimaryKey(acceptInputGroup.getInput().getDeleteData().getDKey());
  
//  DELETE VSVKSDS-IO
              vsvksdsIo.delete(String.valueOf(vsvksdsRecordIo.getVskIoPrimaryKeyString()).toCharArray());
              work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
          }
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS = '00'
//  LITERAL_00 = '00'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) == 0 ) { 
//  IF SEQ-MODE
              if ( work.isSeqMode()  ) { 
//  MOVE SPACES TO REPREC
                  reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'DELETE KEY = ' WS-D-KEY '(Key ignored) ; DELETE LAST READ KEY = ' PRIMARY-KEY-DISP ' Successful' DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_1231571253);
                     charArray.add(String.valueOf(deleteData.getDKeyString()).toCharArray());
                     charArray.add(CONSTANTS.LITERAL_930782512);
                     charArray.add(String.valueOf(work.getPrimaryKeyDispString()).toCharArray());
                     charArray.add(CONSTANTS.LITERAL_B_Successful);
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
  
              }
//  ELSE
              else { 
//  MOVE SPACES TO REPREC
                  reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'DELETE KEY = ' WS-D-KEY ' Successful' DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_1231571253);
                     charArray.add(String.valueOf(deleteData.getDKeyString()).toCharArray());
                     charArray.add(CONSTANTS.LITERAL_B_Successful);
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
  
              }
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          }
  
//  ELSE
          else { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'KEY = ' WS-D-KEY ' : ' 'Error Deleting VSVKSDS Record: File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_972073668);
                 charArray.add(String.valueOf(deleteData.getDKeyString()).toCharArray());
                 charArray.add(CONSTANTS.LITERAL_B2_CL_);
                 charArray.add(CONSTANTS.LITERAL_1488218928);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
  
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO 9000-EXIT
return ;
//cobolCodeEnds::GO TO 9000-EXIT
          }
          ;
      
      }
      /**
      * dlstrd 
      *   This method is derived from 
  *   COBOL Paragraph - A000-DLSTRD COBOL Cyclomatic complexity - 5
      * Input  :  

      * - openFlags                      COBOL Name: OPEN-FLAGS
      * - vskSPrimaryKey                 COBOL Name: VSK-S-PRIMARY-KEY
      * - vskIoPrimaryKey                COBOL Name: VSK-IO-PRIMARY-KEY
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      *
      * Output :  

      * - primaryKeyDisp                 COBOL Name: PRIMARY-KEY-DISP
      * - vskSPrimaryKey                 COBOL Name: VSK-S-PRIMARY-KEY
      * - vskIoPrimaryKey                COBOL Name: VSK-IO-PRIMARY-KEY
      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void dlstrd() throws Exception {
			// Declare local variables used in the method
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  IF SEQ-MODE
          if ( work.isSeqMode()  ) { 
//  DELETE VSVKSDS-SEQ
              vsvksdsSeq.deleteLastReadRecord();
              work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
              // MOVE VSK-S-PRIMARY-KEY TO PRIMARY-KEY-DISP
              work.setPrimaryKeyDisp(vsvksdsRecordS.getVskSPrimaryKey());
          }
//  ELSE
          else { 
//  DELETE VSVKSDS-IO
              vsvksdsIo.delete(String.valueOf(vsvksdsRecordIo.getVskIoPrimaryKeyString()).toCharArray());
              work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
              // MOVE VSK-IO-PRIMARY-KEY TO PRIMARY-KEY-DISP
              work.setPrimaryKeyDisp(vsvksdsRecordIo.getVskIoPrimaryKey());
          }
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS = '00'
//  LITERAL_00 = '00'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) == 0 ) { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'DELETE LAST READ KEY = ' PRIMARY-KEY-DISP ' Successful' DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_349176651);
                 charArray.add(String.valueOf(work.getPrimaryKeyDispString()).toCharArray());
                 charArray.add(CONSTANTS.LITERAL_B_Successful);
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          }
  
//  ELSE
          else { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'LAST READ KEY = ' PRIMARY-KEY-DISP ' : ' 'Error Deleting VSVKSDS Record: File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_1523386492);
                 charArray.add(String.valueOf(work.getPrimaryKeyDispString()).toCharArray());
                 charArray.add(CONSTANTS.LITERAL_B2_CL_);
                 charArray.add(CONSTANTS.LITERAL_1488218928);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO A000-EXIT
return ;
//cobolCodeEnds::GO TO A000-EXIT
          }
          ;
      
      }
      /**
      * rewrite 
      *   This method is derived from 
  *   COBOL Paragraph - B000-REWRITE COBOL Cyclomatic complexity - 8
      * Input  :  

      * - openFlags                      COBOL Name: OPEN-FLAGS
      * - rwKey                          COBOL Name: WS-RW-KEY
      * - rwTaskNo                       COBOL Name: WS-RW-TASK-NO
      * - rwReqSize                      COBOL Name: WS-RW-REQ-SIZE
      * - charstr                        COBOL Name: WS-CHARSTR
      * - rwDataLen                      COBOL Name: WS-RW-DATA-LEN
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      *
      * Output :  

      * - vskSPrimaryKey                 COBOL Name: VSK-S-PRIMARY-KEY
      * - rwKey                          COBOL Name: WS-RW-KEY
      * - vskSSysid                      COBOL Name: VSK-S-SYSID
      * - vskSTaskNo                     COBOL Name: VSK-S-TASK-NO
      * - rwTaskNo                       COBOL Name: WS-RW-TASK-NO
      * - vskSReqSize                    COBOL Name: VSK-S-REQ-SIZE
      * - rwReqSize                      COBOL Name: WS-RW-REQ-SIZE
      * - vskSVarStruct                  COBOL Name: VSK-S-VAR-STRUCT
      * - charstr                        COBOL Name: WS-CHARSTR
      * - vskRecordSize                  COBOL Name: VSK-RECORD-SIZE
      * - vskIoPrimaryKey                COBOL Name: VSK-IO-PRIMARY-KEY
      * - vskIoSysid                     COBOL Name: VSK-IO-SYSID
      * - vskIoTaskNo                    COBOL Name: VSK-IO-TASK-NO
      * - vskIoReqSize                   COBOL Name: VSK-IO-REQ-SIZE
      * - vskIoVarStruct                 COBOL Name: VSK-IO-VAR-STRUCT
      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void rewrite() throws Exception {
			// Declare local variables used in the method
			RewriteData rewriteData = acceptInputGroup.getInput().getRewriteData();
			long rwKey = 0;
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  IF SEQ-MODE
          if ( work.isSeqMode()  ) { 
//  IF WS-RW-KEY = 0 THEN
              rwKey = rewriteData.getRwKey();
//  ELSE
              if (	( rwKey != 0 ) ) { 
                  // MOVE WS-RW-KEY TO VSK-S-PRIMARY-KEY
                  vsvksdsRecordS.setVskSPrimaryKey(acceptInputGroup.getInput().getRewriteData().getRwKey());
  
              }
  
//  MOVE 'S0W2' TO VSK-S-SYSID
//  LITERAL_S0W2 = 'S0W2'
              vsvksdsRecordS.setVskSSysid(CONSTANTS.LITERAL_S0W2);
//  MOVE WS-RW-TASK-NO TO VSK-S-TASK-NO
              vsvksdsRecordS.setVskSTaskNo( rewriteData.getRwTaskNo());
  
//  MOVE WS-RW-REQ-SIZE TO VSK-S-REQ-SIZE
              vsvksdsRecordS.setVskSReqSize((int) rewriteData.getRwReqSize());
  
//  MOVE WS-CHARSTR (1 : WS-RW-DATA-LEN ) TO VSK-S-VAR-STRUCT
              vsvksdsRecordS.setVskSVarStruct(work.getCharstr(),0/* charstr */,acceptInputGroup.getInput().getRewriteData().getRwDataLen(),0 /* vskSVarStruct */,vsvksdsRecordS.getVskSVarStruct().length /* replace,sourceIndex,sourceLen,targetIndex,targetLen */);
              work.setVskRecordSize((long) (Math.abs(21 + acceptInputGroup.getInput().getRewriteData().getRwDataLen())));
//  REWRITE VSVKSDS-RECORD-S
              vsvksdsSeq.setRecord(substring(vsvksdsRecordS.toCharArray(),0,(int) work.getVskRecordSize()));
              vsvksdsSeq.rewrite((int) work.getVskRecordSize());
              work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
          }
//  ELSE
          else { 
//  IF WS-RW-KEY = 0 THEN
              rwKey = rewriteData.getRwKey();
//  ELSE
              if (	( rwKey != 0 ) ) { 
                  // MOVE WS-RW-KEY TO VSK-IO-PRIMARY-KEY
                  vsvksdsRecordIo.setVskIoPrimaryKey(acceptInputGroup.getInput().getRewriteData().getRwKey());
  
              }
  
//  MOVE 'S0W2' TO VSK-IO-SYSID
//  LITERAL_S0W2 = 'S0W2'
              vsvksdsRecordIo.setVskIoSysid(CONSTANTS.LITERAL_S0W2);
//  MOVE WS-RW-TASK-NO TO VSK-IO-TASK-NO
              vsvksdsRecordIo.setVskIoTaskNo( rewriteData.getRwTaskNo());
  
//  MOVE WS-RW-REQ-SIZE TO VSK-IO-REQ-SIZE
              vsvksdsRecordIo.setVskIoReqSize((int) rewriteData.getRwReqSize());
  
//  MOVE WS-CHARSTR (1 : WS-RW-DATA-LEN ) TO VSK-IO-VAR-STRUCT
              vsvksdsRecordIo.setVskIoVarStruct(work.getCharstr(),0/* charstr */,acceptInputGroup.getInput().getRewriteData().getRwDataLen(),0 /* vskIoVarStruct */,vsvksdsRecordIo.getVskIoVarStruct().length /* replace,sourceIndex,sourceLen,targetIndex,targetLen */);
              work.setVskRecordSize((long) (Math.abs(21 + acceptInputGroup.getInput().getRewriteData().getRwDataLen())));
//  REWRITE VSVKSDS-RECORD-IO
              vsvksdsIo.setRecord(substring(vsvksdsRecordIo.toCharArray(),0,(int) work.getVskRecordSize()));
              vsvksdsIo.rewrite((int) work.getVskRecordSize());
              work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
          }
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS = '00' OR '02'
//  LITERAL_02 = '02'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) == 0  || 		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_02) == 0 ) { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'REWRITE KEY = ' WS-RW-KEY ' Successful' ' <Status=' VSVKSDS-FILE-STATUS '>' DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_1377041272);
                 charArray.add(String.valueOf(rewriteData.getRwKeyString()).toCharArray());
                 charArray.add(CONSTANTS.LITERAL_B_Successful);
                 charArray.add(CONSTANTS.LITERAL_1644948693);
                 charArray.add(work.getVsvksdsFileStatus());
                 charArray.add(CONSTANTS.LITERAL_34630);
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4),charArray.get(5));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
  
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          }
  
//  ELSE
          else { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'KEY = ' WS-RW-KEY ' : ' 'Error Re-Writing VSVKSDS Record: File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_972073668);
                 charArray.add(String.valueOf(rewriteData.getRwKeyString()).toCharArray());
                 charArray.add(CONSTANTS.LITERAL_B2_CL_);
                 charArray.add(CONSTANTS.LITERAL_1415458358);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
  
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO B000-EXIT
return ;
//cobolCodeEnds::GO TO B000-EXIT
          }
          ;
      
      }
      /**
      * keyread 
      *   This method is derived from 
  *   COBOL Paragraph - C000-KEYREAD COBOL Cyclomatic complexity - 5
      * Input  :  

      * - openFlags                      COBOL Name: OPEN-FLAGS
      * - kKey                           COBOL Name: WS-K-KEY
      *
      * Output :  

      * - vsvksdsRecordIo                COBOL Name: VSVKSDS-RECORD-IO
      * - vskRecordSize                  COBOL Name: VSK-RECORD-SIZE
      * - reprec                         COBOL Name: REPREC
      * - vskIoPrimaryKey                COBOL Name: VSK-IO-PRIMARY-KEY
      * - kKey                           COBOL Name: WS-K-KEY
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      *
      * @throws CFException
      */
      private void keyread() throws Exception {
			// Declare local variables used in the method
			KeyreadData keyreadData = acceptInputGroup.getInput().getKeyreadData();
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  INITIALIZE VSVKSDS-RECORD-IO
          vsvksdsRecordIo.initialize();
          // MOVE 0 TO VSK-RECORD-SIZE
          work.setVskRecordSize((long)0);
//  IF SEQ-MODE
          if ( work.isSeqMode()  ) { 
              // MOVE '** Error ** Cannot do Random Read in SEQ Mode' TO REPREC
              reprec.setString(CONSTANTS.LITERAL_1487141059);
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO C000-EXIT
return ;
//cobolCodeEnds::GO TO C000-EXIT
          }
          // MOVE WS-K-KEY TO VSK-IO-PRIMARY-KEY
          vsvksdsRecordIo.setVskIoPrimaryKey(acceptInputGroup.getInput().getKeyreadData().getKKey());
  
//  READ VSVKSDS-IO END-READ
          	vsvksdsIo.readDirect(String.valueOf(vsvksdsRecordIo.getVskIoPrimaryKeyString()).toCharArray(),vsvksdsIo.getKeyInfo().getVskIoPrimaryKey());
          work.setVskRecordSize(vsvksdsIo.getRecLen());
          work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
          if (!vsvksdsIo.hasEnded()) {
          	vsvksdsRecordIo.setString(vsvksdsIo.getRecord());
          }
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS = '00'
//  LITERAL_00 = '00'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) == 0 ) { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'READ KEY = ' VSK-IO-PRIMARY-KEY ' <' VSVKSDS-RECORD-IO (1 : VSK-RECORD-SIZE ) '>' DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_1525533642);
                 charArray.add(String.valueOf(vsvksdsRecordIo.getVskIoPrimaryKeyString()).toCharArray());
                 charArray.add(CONSTANTS.LITERAL_1045540);
                 charArray.add(substring(vsvksdsRecordIo.getCharArray(),0,(int) work.getVskRecordSize()));
                 charArray.add(CONSTANTS.LITERAL_34630);
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          }
  
//  ELSE
          else { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'KEY = ' VSK-IO-PRIMARY-KEY ' : ' 'Error Reading VSVKSDS Record: File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_972073668);
                 charArray.add(String.valueOf(vsvksdsRecordIo.getVskIoPrimaryKeyString()).toCharArray());
                 charArray.add(CONSTANTS.LITERAL_B2_CL_);
                 charArray.add(CONSTANTS.LITERAL_1052962688);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO C000-EXIT
return ;
//cobolCodeEnds::GO TO C000-EXIT
          }
          ;
      
      }
      /**
      * readall 
      *   This method is derived from 
  *   COBOL Paragraph - D000-READALL COBOL Cyclomatic complexity - 10
      * Input  :  

      * - rKey                           COBOL Name: WS-R-KEY
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      *
      * Output :  

      * - eofSw                          COBOL Name: WS-EOF-SW
      * - vskSPrimaryKey                 COBOL Name: VSK-S-PRIMARY-KEY
      * - rKey                           COBOL Name: WS-R-KEY
      * - reprec                         COBOL Name: REPREC
      * - vsvksdsRecordS                 COBOL Name: VSVKSDS-RECORD-S
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      * - vskRecordSize                  COBOL Name: VSK-RECORD-SIZE
      *
      * @throws CFException
      */
      private void readall() throws Exception {
			// Declare local variables used in the method
			ReadallData readallData = acceptInputGroup.getInput().getReadallData();
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			long rKey = 0;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  MOVE SPACES TO WS-EOF-SW
          work.setEofSw(CONSTANTS.SPACE);
          rKey = readallData.getRKey();
//  IF WS-R-KEY IS NUMERIC AND WS-R-KEY > 0
          if (        readallData.rKeyIsNumeric()  && 	( rKey > 0 ) ) { 
              // MOVE WS-R-KEY TO VSK-S-PRIMARY-KEY
              vsvksdsRecordS.setVskSPrimaryKey(acceptInputGroup.getInput().getReadallData().getRKey());
  
//  START VSVKSDS-SEQ KEY IS >= VSK-S-PRIMARY-KEY END-START
              vsvksdsSeq.seek(vsvksdsRecordS.toCharArray(),CFConstants.CONDITION_GREATER_THAN_OR_EQUAL,vsvksdsSeq.getKeyInfo().getVskSPrimaryKey()); 
              work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
              vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS = '00'
//  LITERAL_00 = '00'
              if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) == 0 ) { 
//  MOVE SPACES TO REPREC
                  reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'START VSVKSDS Key >= ' VSK-S-PRIMARY-KEY ' Successful' DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_1875726706);
                     charArray.add(String.valueOf(vsvksdsRecordS.getVskSPrimaryKeyString()).toCharArray());
                     charArray.add(CONSTANTS.LITERAL_B_Successful);
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
                  repout.write(reprec.toCharArray()); 
                  reprec.setString(CONSTANTS.LOW_VALUE_215695350);
              }
  
//  ELSE
              else { 
//  MOVE SPACES TO REPREC
                  reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'START VSVKSDS Key >= ' VSK-S-PRIMARY-KEY ': Error : File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_1875726706);
                     charArray.add(String.valueOf(vsvksdsRecordS.getVskSPrimaryKeyString()).toCharArray());
                     charArray.add(CONSTANTS.LITERAL_1165281551);
                     charArray.add(work.getVsvksdsFileStatus());
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
                  repout.write(reprec.toCharArray()); 
                  reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO D000-EXIT
return ;
//cobolCodeEnds::GO TO D000-EXIT
              }
          }
  
//  PERFORM UNTIL END-OF-VSVKSDS
          while ((!(work.isEndOfVsvksds()) )) {
//  READ VSVKSDS-SEQ NEXT
              vsvksdsSeq.readNext();
              work.setVskRecordSize(vsvksdsSeq.getRecLen());
              work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
              if (!vsvksdsSeq.hasEnded()) {
              	vsvksdsRecordS.setString(vsvksdsSeq.getRecord());
              }
//  AT END
              if (vsvksdsSeq.hasEnded()) {  
//  SET END-OF-VSVKSDS TO TRUE
                  work.setEndOfVsvksdsTrue(); 
                  
//cobolCode::GO TO D000-EXIT
return ;
//cobolCodeEnds::GO TO D000-EXIT
                }
              vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS = '00'
//  LITERAL_00 = '00'
              if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) == 0 ) { 
//  MOVE SPACES TO REPREC
                  reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'READ NEXT KEY = ' VSK-S-PRIMARY-KEY ' <' VSVKSDS-RECORD-S (1 : VSK-RECORD-SIZE ) '>' DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_1843340761);
                     charArray.add(String.valueOf(vsvksdsRecordS.getVskSPrimaryKeyString()).toCharArray());
                     charArray.add(CONSTANTS.LITERAL_1045540);
                     charArray.add(substring(vsvksdsRecordS.getCharArray(),0,(int) work.getVskRecordSize()));
                     charArray.add(CONSTANTS.LITERAL_34630);
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
                  repout.write(reprec.toCharArray()); 
                  reprec.setString(CONSTANTS.LOW_VALUE_215695350);
              }
  
//  ELSE
              else { 
//  MOVE SPACES TO REPREC
                  reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'KEY = ' VSK-S-PRIMARY-KEY ' : ' 'Error Reading VSVKSDS Record: File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_972073668);
                     charArray.add(String.valueOf(vsvksdsRecordS.getVskSPrimaryKeyString()).toCharArray());
                     charArray.add(CONSTANTS.LITERAL_B2_CL_);
                     charArray.add(CONSTANTS.LITERAL_1052962688);
                     charArray.add(work.getVsvksdsFileStatus());
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
                  repout.write(reprec.toCharArray()); 
                  reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO D000-EXIT
return ;
//cobolCodeEnds::GO TO D000-EXIT
              }
          }
          ;
      
      }
      /**
      * readnxt 
      *   This method is derived from 
  *   COBOL Paragraph - E000-READNXT COBOL Cyclomatic complexity - 7
      * Input  :  

      * - openFlags                      COBOL Name: OPEN-FLAGS
      *
      * Output :  

      * - vsvksdsRecordS                 COBOL Name: VSVKSDS-RECORD-S
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      * - vskRecordSize                  COBOL Name: VSK-RECORD-SIZE
      * - vskSPrimaryKey                 COBOL Name: VSK-S-PRIMARY-KEY
      * - vsvksdsRecordIo                COBOL Name: VSVKSDS-RECORD-IO
      * - vskIoPrimaryKey                COBOL Name: VSK-IO-PRIMARY-KEY
      * - primaryKeyDisp                 COBOL Name: PRIMARY-KEY-DISP
      * - vsamRecordDisp                 COBOL Name: VSAM-RECORD-DISP
      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void readnxt() throws Exception {
			// Declare local variables used in the method
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  IF SEQ-MODE
          if ( work.isSeqMode()  ) { 
//  READ VSVKSDS-SEQ NEXT END-READ
              vsvksdsSeq.readNext();
              work.setVskRecordSize(vsvksdsSeq.getRecLen());
              work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
              if (!vsvksdsSeq.hasEnded()) {
              	vsvksdsRecordS.setString(vsvksdsSeq.getRecord());
              }
          }
//  ELSE
          else { 
//  READ VSVKSDS-IO NEXT END-READ
              vsvksdsIo.readNext();
              work.setVskRecordSize(vsvksdsIo.getRecLen());
              work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
              if (!vsvksdsIo.hasEnded()) {
              	vsvksdsRecordIo.setString(vsvksdsIo.getRecord());
              }
          }
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS = '00' OR '02' THEN
//  LITERAL_02 = '02'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) == 0  || 		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_02) == 0 ) { 
//  IF SEQ-MODE
              if ( work.isSeqMode()  ) { 
                  // MOVE VSK-S-PRIMARY-KEY TO PRIMARY-KEY-DISP
                  work.setPrimaryKeyDisp(vsvksdsRecordS.getVskSPrimaryKey());
//  MOVE VSVKSDS-RECORD-S (1 : VSK-RECORD-SIZE ) TO VSAM-RECORD-DISP
                  work.setVsamRecordDisp(substring(vsvksdsRecordS.getCharArray(),0,(int) work.getVskRecordSize()));
              }
//  ELSE
              else { 
                  // MOVE VSK-IO-PRIMARY-KEY TO PRIMARY-KEY-DISP
                  work.setPrimaryKeyDisp(vsvksdsRecordIo.getVskIoPrimaryKey());
//  MOVE VSVKSDS-RECORD-IO (1 : VSK-RECORD-SIZE ) TO VSAM-RECORD-DISP
                  work.setVsamRecordDisp(substring(vsvksdsRecordIo.getCharArray(),0,(int) work.getVskRecordSize()));
              }
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'READ NEXT KEY = ' PRIMARY-KEY-DISP ' <Status=' VSVKSDS-FILE-STATUS '>' ' <' VSAM-RECORD-DISP (1 : VSK-RECORD-SIZE ) '>' DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_1843340761);
                 charArray.add(String.valueOf(work.getPrimaryKeyDispString()).toCharArray());
                 charArray.add(CONSTANTS.LITERAL_1644948693);
                 charArray.add(work.getVsvksdsFileStatus());
                 charArray.add(CONSTANTS.LITERAL_34630);
                 charArray.add(CONSTANTS.LITERAL_1045540);
                 charArray.add(substring(work.getVsamRecordDisp(),0,(int) work.getVskRecordSize()));
                 charArray.add(CONSTANTS.LITERAL_34630);
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4),charArray.get(5),charArray.get(6),charArray.get(7));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          }
  
//  ELSE
          else { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'KEY = ' PRIMARY-KEY-DISP ' : ' 'Error Reading VSVKSDS Record: File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
              charArray = new ArrayList<char[]>();
                 charArray.add(CONSTANTS.LITERAL_972073668);
                 charArray.add(String.valueOf(work.getPrimaryKeyDispString()).toCharArray());
                 charArray.add(CONSTANTS.LITERAL_B2_CL_);
                 charArray.add(CONSTANTS.LITERAL_1052962688);
                 charArray.add(work.getVsvksdsFileStatus());
              joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
              updated = updateString(reprec.toCharArray() ,joinCharArray);
              reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO E000-EXIT
return ;
//cobolCodeEnds::GO TO E000-EXIT
          }
          ;
      
      }
      /**
      * delall 
      *   This method is derived from 
  *   COBOL Paragraph - F000-DELALL COBOL Cyclomatic complexity - 7
      * Input  : None 

      * Output :  

      * - eofSw                          COBOL Name: WS-EOF-SW
      * - vsvksdsRecordIo                COBOL Name: VSVKSDS-RECORD-IO
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      * - vskRecordSize                  COBOL Name: VSK-RECORD-SIZE
      * - vskIoPrimaryKey                COBOL Name: VSK-IO-PRIMARY-KEY
      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void delall() throws Exception {
			// Declare local variables used in the method
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  MOVE SPACES TO WS-EOF-SW
          work.setEofSw(CONSTANTS.SPACE);
//  PERFORM UNTIL END-OF-VSVKSDS
          while ((!(work.isEndOfVsvksds()) )) {
//  READ VSVKSDS-IO NEXT
              vsvksdsIo.readNext();
              work.setVskRecordSize(vsvksdsIo.getRecLen());
              work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
              if (!vsvksdsIo.hasEnded()) {
              	vsvksdsRecordIo.setString(vsvksdsIo.getRecord());
              }
//  AT END
              if (vsvksdsIo.hasEnded()) {  
//  SET END-OF-VSVKSDS TO TRUE
                  work.setEndOfVsvksdsTrue(); 
                  
//cobolCode::GO TO F000-EXIT
return ;
//cobolCodeEnds::GO TO F000-EXIT
                }
              vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS = '00'
//  LITERAL_00 = '00'
              if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) == 0 ) { 
//  DELETE VSVKSDS-IO
                  vsvksdsIo.delete(String.valueOf(vsvksdsRecordIo.getVskIoPrimaryKeyString()).toCharArray());
                  work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
                  vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS = '00'
//  LITERAL_00 = '00'
                  if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) == 0 ) { 
//  MOVE SPACES TO REPREC
                      reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'DELETE LAST READ KEY = ' VSK-IO-PRIMARY-KEY ' Successful' DELIMITED BY SIZE INTO REPREC END-STRING
                      charArray = new ArrayList<char[]>();
                         charArray.add(CONSTANTS.LITERAL_349176651);
                         charArray.add(String.valueOf(vsvksdsRecordIo.getVskIoPrimaryKeyString()).toCharArray());
                         charArray.add(CONSTANTS.LITERAL_B_Successful);
                      joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2));
                      updated = updateString(reprec.toCharArray() ,joinCharArray);
                      reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
                      repout.write(reprec.toCharArray()); 
                      reprec.setString(CONSTANTS.LOW_VALUE_215695350);
                  }
  
//  ELSE
                  else { 
//  MOVE SPACES TO REPREC
                      reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'LAST READ KEY = ' VSK-IO-PRIMARY-KEY ' : ' 'Error Deleting VSVKSDS Record: File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
                      charArray = new ArrayList<char[]>();
                         charArray.add(CONSTANTS.LITERAL_1523386492);
                         charArray.add(String.valueOf(vsvksdsRecordIo.getVskIoPrimaryKeyString()).toCharArray());
                         charArray.add(CONSTANTS.LITERAL_B2_CL_);
                         charArray.add(CONSTANTS.LITERAL_1488218928);
                         charArray.add(work.getVsvksdsFileStatus());
                      joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
                      updated = updateString(reprec.toCharArray() ,joinCharArray);
                      reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
                      repout.write(reprec.toCharArray()); 
                      reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO F000-EXIT
return ;
//cobolCodeEnds::GO TO F000-EXIT
                  }
              }
  
//  ELSE
              else { 
//  MOVE SPACES TO REPREC
                  reprec.setString(CONSTANTS.SPACE_300);
//  STRING 'KEY = ' VSK-IO-PRIMARY-KEY ' : ' 'Error Reading VSVKSDS Record: File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_972073668);
                     charArray.add(String.valueOf(vsvksdsRecordIo.getVskIoPrimaryKeyString()).toCharArray());
                     charArray.add(CONSTANTS.LITERAL_B2_CL_);
                     charArray.add(CONSTANTS.LITERAL_1052962688);
                     charArray.add(work.getVsvksdsFileStatus());
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
//  WRITE REPREC
                  repout.write(reprec.toCharArray()); 
                  reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO F000-EXIT
return ;
//cobolCodeEnds::GO TO F000-EXIT
              }
          }
          ;
      
      }
      /**
      * start 
      *   This method is derived from 
  *   COBOL Paragraph - G000-START COBOL Cyclomatic complexity - 25
      * Input  :  

      * - openFlags                      COBOL Name: OPEN-FLAGS
      * - inputCommand                   COBOL Name: WS-INPUT-COMMAND
      * - sKey                           COBOL Name: WS-S-KEY
      * - sOperation                     COBOL Name: WS-S-OPERATION
      * - vsvksdsFileStatus              COBOL Name: VSVKSDS-FILE-STATUS
      *
      * Output :  

      * - vsvksdsRecordIo                COBOL Name: VSVKSDS-RECORD-IO
      * - vsvksdsRecordS                 COBOL Name: VSVKSDS-RECORD-S
      * - vskRecordSize                  COBOL Name: VSK-RECORD-SIZE
      * - vskSPrimaryKey                 COBOL Name: VSK-S-PRIMARY-KEY
      * - sKey                           COBOL Name: WS-S-KEY
      * - startKeytype                   COBOL Name: WS-START-KEYTYPE
      * - vskSTaskNo                     COBOL Name: VSK-S-TASK-NO
      * - vskIoPrimaryKey                COBOL Name: VSK-IO-PRIMARY-KEY
      * - vskIoTaskNo                    COBOL Name: VSK-IO-TASK-NO
      * - reprec                         COBOL Name: REPREC
      *
      * @throws CFException
      */
      private void start() throws Exception {
			// Declare local variables used in the method
			StartData startData = acceptInputGroup.getInput().getStartData();
			Input input = acceptInputGroup.getInput();
			ArrayList<char[]> charArray = new ArrayList<char[]>();
			char[] joinCharArray = null;
			char[] vsvksdsFileStatus = null;
			Map<String,Object> updated = null;
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  INITIALIZE VSVKSDS-RECORD-IO VSVKSDS-RECORD-S
          vsvksdsRecordIo.initialize();
          vsvksdsRecordS.initialize();
          // MOVE 0 TO VSK-RECORD-SIZE
          work.setVskRecordSize((long)0);
//  IF SEQ-MODE
          if ( work.isSeqMode()  ) { 
              input = acceptInputGroup.getInput();
//  IF C-START THEN
              if ( input.isCStart()  ) { 
                  // MOVE WS-S-KEY TO VSK-S-PRIMARY-KEY
                  vsvksdsRecordS.setVskSPrimaryKey(acceptInputGroup.getInput().getStartData().getSKey());
  
                  // MOVE ' (PRIMARY) ' TO WS-START-KEYTYPE
                  work.setStartKeytype(CONSTANTS.LITERAL_B6_PRO_PRIMARY_PRC_);
                  startData = acceptInputGroup.getInput().getStartData();
//  EVALUATE TRUE
                  if  ( startData.isEq()  ) { 
//  START VSVKSDS-SEQ KEY = VSK-S-PRIMARY-KEY
                      vsvksdsSeq.seek(vsvksdsRecordS.toCharArray(),CFConstants.CONDITION_EQUAL,vsvksdsSeq.getKeyInfo().getVskSPrimaryKey()); 
                      work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
                  }
                  else if  ( startData.isGt()  ) { 
//  START VSVKSDS-SEQ KEY > VSK-S-PRIMARY-KEY
                      vsvksdsSeq.seek(vsvksdsRecordS.toCharArray(),CFConstants.CONDITION_GREATER_THAN,vsvksdsSeq.getKeyInfo().getVskSPrimaryKey()); 
                      work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
                  }
                  else if  ( startData.isGe()  ) { 
//  START VSVKSDS-SEQ KEY >= VSK-S-PRIMARY-KEY
                      vsvksdsSeq.seek(vsvksdsRecordS.toCharArray(),CFConstants.CONDITION_GREATER_THAN_OR_EQUAL,vsvksdsSeq.getKeyInfo().getVskSPrimaryKey()); 
                      work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
                  }
  
              }
  
//  ELSE
              else { 

// *          Alternate key based start
//  MOVE WS-S-KEY TO VSK-S-TASK-NO
                  vsvksdsRecordS.setVskSTaskNo( startData.getSKey());
  
                  // MOVE ' (ALTERNATE) ' TO WS-START-KEYTYPE
                  work.setStartKeytype(CONSTANTS.LITERAL_B4_PRO_ALTERNATE_PRC_);
                  startData = acceptInputGroup.getInput().getStartData();
//  EVALUATE TRUE
                  if  ( startData.isEq()  ) { 
//  START VSVKSDS-SEQ KEY IS EQUAL TO VSK-S-TASK-NO
                      vsvksdsSeq.seek(vsvksdsRecordS.toCharArray(),CFConstants.CONDITION_EQUAL,vsvksdsSeq.getKeyInfo().getVskSTaskNo()); 
                      work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
                  }
                  else if  ( startData.isGt()  ) { 
//  START VSVKSDS-SEQ KEY IS GREATER THAN VSK-S-TASK-NO
                      vsvksdsSeq.seek(vsvksdsRecordS.toCharArray(),CFConstants.CONDITION_GREATER_THAN,vsvksdsSeq.getKeyInfo().getVskSTaskNo()); 
                      work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
                  }
                  else if  ( startData.isGe()  ) { 
//  START VSVKSDS-SEQ KEY IS GREATER THAN OR EQUAL TO VSK-S-TASK-NO
                      vsvksdsSeq.seek(vsvksdsRecordS.toCharArray(),CFConstants.CONDITION_GREATER_THAN_OR_EQUAL,vsvksdsSeq.getKeyInfo().getVskSTaskNo()); 
                      work.setVsvksdsFileStatus(vsvksdsSeq.getStatusString() );
                  }
  
              }
          }
//  ELSE
          else { 
              input = acceptInputGroup.getInput();
//  IF C-START THEN
              if ( input.isCStart()  ) { 
                  // MOVE WS-S-KEY TO VSK-IO-PRIMARY-KEY
                  vsvksdsRecordIo.setVskIoPrimaryKey(acceptInputGroup.getInput().getStartData().getSKey());
  
                  // MOVE ' (PRIMARY) ' TO WS-START-KEYTYPE
                  work.setStartKeytype(CONSTANTS.LITERAL_B6_PRO_PRIMARY_PRC_);
                  startData = acceptInputGroup.getInput().getStartData();
//  EVALUATE TRUE
                  if  ( startData.isEq()  ) { 
//  START VSVKSDS-IO KEY = VSK-IO-PRIMARY-KEY
                      vsvksdsIo.seek(vsvksdsRecordIo.toCharArray(),CFConstants.CONDITION_EQUAL,vsvksdsIo.getKeyInfo().getVskIoPrimaryKey()); 
                      work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
                  }
                  else if  ( startData.isGt()  ) { 
//  START VSVKSDS-IO KEY > VSK-IO-PRIMARY-KEY
                      vsvksdsIo.seek(vsvksdsRecordIo.toCharArray(),CFConstants.CONDITION_GREATER_THAN,vsvksdsIo.getKeyInfo().getVskIoPrimaryKey()); 
                      work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
                  }
                  else if  ( startData.isGe()  ) { 
//  START VSVKSDS-IO KEY >= VSK-IO-PRIMARY-KEY
                      vsvksdsIo.seek(vsvksdsRecordIo.toCharArray(),CFConstants.CONDITION_GREATER_THAN_OR_EQUAL,vsvksdsIo.getKeyInfo().getVskIoPrimaryKey()); 
                      work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
                  }
  
              }
  
//  ELSE
              else { 

// *          Alternate key based start
//  MOVE WS-S-KEY TO VSK-IO-TASK-NO
                  vsvksdsRecordIo.setVskIoTaskNo( startData.getSKey());
  
                  // MOVE ' (ALTERNATE) ' TO WS-START-KEYTYPE
                  work.setStartKeytype(CONSTANTS.LITERAL_B4_PRO_ALTERNATE_PRC_);
                  startData = acceptInputGroup.getInput().getStartData();
//  EVALUATE TRUE
                  if  ( startData.isEq()  ) { 
//  START VSVKSDS-IO KEY IS EQUAL TO VSK-IO-TASK-NO
                      vsvksdsIo.seek(vsvksdsRecordIo.toCharArray(),CFConstants.CONDITION_EQUAL,vsvksdsIo.getKeyInfo().getVskIoTaskNo()); 
                      work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
                  }
                  else if  ( startData.isGt()  ) { 
//  START VSVKSDS-IO KEY IS GREATER THAN VSK-IO-TASK-NO
                      vsvksdsIo.seek(vsvksdsRecordIo.toCharArray(),CFConstants.CONDITION_GREATER_THAN,vsvksdsIo.getKeyInfo().getVskIoTaskNo()); 
                      work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
                  }
                  else if  ( startData.isGe()  ) { 
//  START VSVKSDS-IO KEY IS GREATER THAN OR EQUAL TO VSK-IO-TASK-NO
                      vsvksdsIo.seek(vsvksdsRecordIo.toCharArray(),CFConstants.CONDITION_GREATER_THAN_OR_EQUAL,vsvksdsIo.getKeyInfo().getVskIoTaskNo()); 
                      work.setVsvksdsFileStatus(vsvksdsIo.getStatusString() );
                  }
  
              }
          }
          vsvksdsFileStatus = work.getVsvksdsFileStatus();
//  IF VSVKSDS-FILE-STATUS = '00'
//  LITERAL_00 = '00'
          if (		compareChars(vsvksdsFileStatus,CONSTANTS.LITERAL_00) == 0 ) { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  IF SEQ-MODE
              if ( work.isSeqMode()  ) { 
//  STRING 'START SEQUENTIAL ' WS-S-OPERATION ' ' WS-S-KEY WS-START-KEYTYPE DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_START_B2_SEQUENTIAL);
                     charArray.add(startData.getSOperation());
                     charArray.add(CONSTANTS.SPACE);
                     charArray.add(String.valueOf(startData.getSKeyString()).toCharArray());
                     charArray.add(work.getStartKeytype());
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
  
              }
//  ELSE
              else { 
//  STRING 'START DYNAMIC ' WS-S-OPERATION ' ' WS-S-KEY WS-START-KEYTYPE DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_START_B2_DYNAMIC);
                     charArray.add(startData.getSOperation());
                     charArray.add(CONSTANTS.SPACE);
                     charArray.add(String.valueOf(startData.getSKeyString()).toCharArray());
                     charArray.add(work.getStartKeytype());
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
  
              }
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
          }
  
//  ELSE
          else { 
//  MOVE SPACES TO REPREC
              reprec.setString(CONSTANTS.SPACE_300);
//  IF SEQ-MODE
              if ( work.isSeqMode()  ) { 
//  STRING 'Error START SEQUENTIAL ' WS-S-OPERATION ' ' WS-S-KEY WS-START-KEYTYPE ' : File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_Error_B3_STARTSEQUENTIAL);
                     charArray.add(startData.getSOperation());
                     charArray.add(CONSTANTS.SPACE);
                     charArray.add(String.valueOf(startData.getSKeyString()).toCharArray());
                     charArray.add(work.getStartKeytype());
                     charArray.add(CONSTANTS.LITERAL_1543582865);
                     charArray.add(work.getVsvksdsFileStatus());
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4),charArray.get(5),charArray.get(6));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
  
              }
//  ELSE
              else { 
//  STRING 'Error START DYNAMIC ' WS-S-OPERATION ' ' WS-S-KEY WS-START-KEYTYPE ' : File Status = ' VSVKSDS-FILE-STATUS DELIMITED BY SIZE INTO REPREC END-STRING
                  charArray = new ArrayList<char[]>();
                     charArray.add(CONSTANTS.LITERAL_Error_B3_STARTDYNAMIC);
                     charArray.add(startData.getSOperation());
                     charArray.add(CONSTANTS.SPACE);
                     charArray.add(String.valueOf(startData.getSKeyString()).toCharArray());
                     charArray.add(work.getStartKeytype());
                     charArray.add(CONSTANTS.LITERAL_1543582865);
                     charArray.add(work.getVsvksdsFileStatus());
                  joinCharArray = Field.mergeArrays(charArray.get(0),charArray.get(1),charArray.get(2),charArray.get(3),charArray.get(4),charArray.get(5),charArray.get(6));
                  updated = updateString(reprec.toCharArray() ,joinCharArray);
                  reprec.setString(  (char[])updated.get("string"));
  
              }
//  WRITE REPREC
              repout.write(reprec.toCharArray()); 
              reprec.setString(CONSTANTS.LOW_VALUE_215695350);
//cobolCode::GO TO G000-EXIT
return ;
//cobolCodeEnds::GO TO G000-EXIT
          }
          ;
      
      }
      /**
      * exitProgram 
      *   This method is derived from 
  *   COBOL Paragraph - 9999-EXIT-PROGRAM COBOL Cyclomatic complexity - 2
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      private void exitProgram() throws Exception {
      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          this.setProgramEnded(true);
          return ;
      
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
       doNotSkipCloseo = true;
       doNotSkipOpenio = true;
       doNotSkipOpenextend = true;
       doNotSkipCloseio = true;
       doNotSkipOpeni = true;
       doNotSkipClosei = true;
       doNotSkipInsall = true;
       doNotSkipInsert = true;
       doNotSkipDelete = true;
       doNotSkipDlstrd = true;
       doNotSkipRewrite = true;
       doNotSkipKeyread = true;
       doNotSkipReadall = true;
       doNotSkipReadnxt = true;
       doNotSkipDelall = true;
       doNotSkipStart = true;
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
  
  
  
  
  
  }
