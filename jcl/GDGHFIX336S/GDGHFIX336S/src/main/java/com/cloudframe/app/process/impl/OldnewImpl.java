  package com.cloudframe.app.process.impl;
  /* Java code generated by CloudFrame on 2025-04-28 at 08:08. */
  
  import org.springframework.beans.factory.annotation.Autowired;
  import org.springframework.beans.factory.annotation.Qualifier;
  import com.cloudframe.app.oldnew.file.*;
  import com.cloudframe.app.oldnew.OldnewCtx.*;
  import com.cloudframe.app.oldnew.OldnewCtx;
  import com.cloudframe.app.process.Oldnew;
  import com.cloudframe.app.process.BaseProcess;
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.dto.GlobalExecutorCtx;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.data.Field;
  import com.cloudframe.app.oldnew.dto.*;
  import com.cloudframe.app.oldnew.file.records.FsOutrec;
  import com.cloudframe.app.oldnew.file.records.FsRecord;
  import com.cloudframe.app.oldnew.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("oldnew")
  
  public class OldnewImpl extends CommonProcess implements Oldnew {
  
  Logger logger = LoggerFactory.getLogger(OldnewImpl.class);
  
  
  
  
  @Autowired 
  @Qualifier("oldnew_sys001MipSpecrtOld")
  Sys001MipSpecrtOld sys001MipSpecrtOld;
  @Autowired 
  @Qualifier("oldnew_sys002MipSpecrtNew")
  Sys002MipSpecrtNew sys002MipSpecrtNew;
  
  
  
  
  
  
      /**
      * process 
      * Input  :  

      * - eof                            COBOL Name: WS-EOF
      *
      * Output :  

      * - fsRecord                       COBOL Name: FS-RECORD
      * - eof                            COBOL Name: WS-EOF
      *
      * @throws CFException
      */
      public int process(OldnewCtx programCtx) throws Exception {
       try {
       setCodePage("1047");
            // Reset program ended flag
           programCtx.setProgramEnded(false);
ProcessInCtx methodIn = programCtx.getProcessInCtx();
ProcessOutCtx methodOut = methodIn.getProcessOutCtx();
//  OPEN INPUT SYS001-MIP-SPECRT-OLD
          sys001MipSpecrtOld.open(new String(CONSTANTS.MODE_READ_ONLY_36242),sys001MipSpecrtOld.getFileName(),sys001MipSpecrtOld.getSys001MipSpecrtOldCharSet(),sys001MipSpecrtOld.getSys001MipSpecrtOldCrlfFlag());
//  OPEN OUTPUT SYS002-MIP-SPECRT-NEW
          sys002MipSpecrtNew.open(new String(CONSTANTS.MODE_WRITE_ONLY_36397),sys002MipSpecrtNew.getFileName(),sys002MipSpecrtNew.getSys002MipSpecrtNewCharSet(),sys002MipSpecrtNew.getSys002MipSpecrtNewCrlfFlag());
//  PERFORM UNTIL WS-EOF = 'Y'
          while ((methodOut.getEof()[0] != 'Y')) {
//  READ SYS001-MIP-SPECRT-OLD INTO WS-RECORD
              	sys001MipSpecrtOld.read();
              if (!sys001MipSpecrtOld.hasEnded()) {
                 methodOut.getFsRecord().setString(sys001MipSpecrtOld.getRecord());
                 methodIn.setRecordWs(methodOut.getFsRecord().toCharArray());
              }
//  AT END
              if (sys001MipSpecrtOld.hasEnded()) {  
                  // MOVE 'Y' TO WS-EOF
                  //  LITERAL_Y = 'Y'
                  methodOut.setEof(CONSTANTS.LITERAL_Y);
                }
//  NOT AT END
              else  {
//  PERFORM 1000-WRITE-PARA
                  writePara(programCtx.getWriteParaInCtx());/*1000-WRITE-PARA*/
                  if (programCtx.isProgramEnded()) {
                      return programCtx.getRc();
                  }
                }
          }
//  CLOSE SYS001-MIP-SPECRT-OLD
          sys001MipSpecrtOld.close(); 
//  CLOSE SYS002-MIP-SPECRT-NEW
          sys002MipSpecrtNew.close(); 
//  STOP RUN
          throw Terminate.TERMINATE;
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
        finally {
      		if(sys001MipSpecrtOld.hasOpened() && !sys001MipSpecrtOld.isReadOnly()) { 
      			sys001MipSpecrtOld.flush(); 
      		}
      		if(sys002MipSpecrtNew.hasOpened() && !sys002MipSpecrtNew.isReadOnly()) { 
      			sys002MipSpecrtNew.flush(); 
      		}
      }
      
      // end of process method
      }
      /**
      * writePara 
      *   This method is derived from 
  *   COBOL Paragraph - 1000-WRITE-PARA COBOL Cyclomatic complexity - 1
      * Input  :  

      * - recordWs                       COBOL Name: WS-RECORD
      *
      * Output :  

      * - fsOutrec                       COBOL Name: FS-OUTREC
      * - recordWs                       COBOL Name: WS-RECORD
      *
      * @throws CFException
      */
      @Override
      public WriteParaOutCtx writePara(WriteParaInCtx methodIn) throws Exception {
OldnewCtx programCtx = methodIn.getOldnewCtx();
WriteParaOutCtx methodOut = methodIn.getWriteParaOutCtx();
//  MOVE WS-RECORD TO FS-OUTREC
          methodOut.getFsOutrec().setString(methodOut.getRecordWs());
//  WRITE FS-OUTREC END-WRITE
          sys002MipSpecrtNew.write(methodOut.getFsOutrec().toCharArray()); 
          methodOut.getFsOutrec().setString(CONSTANTS.LOW_VALUE_1253879107);
      
      return methodOut;
      }
  
  
  
  
  
  
  
  
  }
