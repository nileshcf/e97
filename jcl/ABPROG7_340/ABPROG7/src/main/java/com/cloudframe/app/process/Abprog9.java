  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-23 at 09:57. */
  
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import org.springframework.web.bind.annotation.RequestParam;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.data.Field;
  import com.cloudframe.app.abprog9.dto.*;
  import com.cloudframe.app.abprog9.dto.EndFlagGroup;
  import com.cloudframe.app.abprog9.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("abprog9")
  
  public class Abprog9 extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Abprog9.class);
  
  private EndFlagGroup endFlagGroup = new EndFlagGroup() ;
  private Work work = new Work() ;
  
  
  
  
  
  
  
  
  
      public int setParameter(String endFlagGroup) throws Exception {
      		if(endFlagGroup != null)
      		    this.endFlagGroup.setString(com.cloudframe.app.data.Field.getParm(endFlagGroup),new String(CONSTANTS.EBCDIC_ENCODING));
      		setInitDone(false);
      		process();
      		return getRc();
      }
      /**
      * process 
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");
          ;
//  PERFORM MAINLINE
          mainline();/*MAINLINE*/
          if (this.isProgramEnded()) {
              return getRc();
          }
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
      
       return getRc(); // Exit with return code
      // end of process method
      }
      /**
      * mainline 
      *   This method is derived from 
  *   COBOL Paragraph - MAINLINE COBOL Cyclomatic complexity - 4
      * Input  :  

      * - endFlag                        COBOL Name: WS-END-FLAG
      *
      * Output : None 

      * @throws CFException
      */
      private void mainline() throws Exception {
      
// *
//  DISPLAY 'In          ABPROG9'
          logger.info("In          ABPROG9"); 
//  IF ABNORMAL-END
          if ( endFlagGroup.isAbnormalEnd()  ) { 
//  PERFORM 0000-RAISE-ABEND THRU 0000-EXIT
              raiseAbend();/*0000-RAISE-ABEND*/
              if (this.isProgramEnded()) {
                  return ;
              }
//  DISPLAY 'If you see this message then it is EXCEPTION'
              logger.info("If you see this message then it is EXCEPTION"); 
          }
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          this.setProgramEnded(true);
          return ;
      
      }
      /**
      * raiseAbend 
      *   This method is derived from 
  *   COBOL Paragraph - 0000-RAISE-ABEND COBOL Cyclomatic complexity - 2
      * Input  : None 

      * Output :  

      * - rc                             COBOL Name: RETURN-CODE
      *
      * @throws CFException
      */
      private void raiseAbend() throws Exception {
      
// *

// *

// *****Call 'ilboabn0' using return-code.
          // MOVE +10 TO RETURN-CODE
          this.setRc( 10);
//  DISPLAY '** ERROR: PROGRAM ENDING WITH RETURN CODE 10'
          logger.info("** ERROR: PROGRAM ENDING WITH RETURN CODE 10"); 
//  STOP RUN
          throw Terminate.TERMINATE;
      
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
      public int call(Object[] params) throws Exception {
      
      int len = params.length;
         if (len > 0 && params[0] != null )
            endFlagGroup.set((Field)params[0]);
         // invoke the process and return rc
         return process();
         
      }
      
      public int call(Field... parameters) throws Exception {
         for (int index = 0; index < parameters.length;index++) {
             switch(index) {
              case 0:
                      if(parameters[index] != null ) {
              		if (parameters[index] instanceof EndFlagGroup) {
                       	this.endFlagGroup = ((EndFlagGroup) parameters[index]);
                  	} else {
                       	this.endFlagGroup.set(parameters[index]);
                  	}
                  }
                
                  break;
            }
         }
          return process();
      }
      
      
  
  
  
  
  
  }
