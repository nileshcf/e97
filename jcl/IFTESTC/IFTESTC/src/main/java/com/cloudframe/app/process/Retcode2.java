  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-29 at 08:26. */
  
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import org.springframework.web.bind.annotation.RequestParam;
  import com.cloudframe.app.process.Retcode1;
  import org.springframework.beans.factory.annotation.Autowired;
  import org.springframework.beans.factory.annotation.Qualifier;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.data.Field;
  import com.cloudframe.app.retcode2.dto.*;
  import com.cloudframe.app.retcode2.dto.Parm;
  import com.cloudframe.app.retcode2.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("retcode2")
  
  public class Retcode2 extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Retcode2.class);
  
  private Parm parm = new Parm() ;
  private Work work = new Work() ;
  
  
  
  
  
  
  
  
  
      public int setParameter(String parm) throws Exception {
      		if(parm != null)
      		    this.parm.setString(com.cloudframe.app.data.Field.getParm(parm),new String(CONSTANTS.EBCDIC_ENCODING));
      		setInitDone(false);
      		process();
      		return getRc();
      }
      /**
      * process 
      * Input  : None 

      * Output :  

      * - rc                             COBOL Name: RETURN-CODE
      *
      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");

// *
// * This program move 1 to return-code
// * but the caller program overrides it
// * so final return-code should be what the caller program passed.
// *
          // MOVE 1 TO RETURN-CODE
          this.setRc( 1);
//  CALL WS-RETCODE USING LK-PARM
          // CALL WS-RETCODE USING LK-PARM
               this.setRc( retcode1.call(parm));
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          this.setProgramEnded(true);
          return getRc();
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
      
      // end of process method
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
      public int call(Object[] params) throws Exception {
      
      int len = params.length;
         if (len > 0 && params[0] != null )
            parm.set((Field)params[0]);
         // invoke the process and return rc
         return process();
         
      }
      
      public int call(Field... parameters) throws Exception {
         for (int index = 0; index < parameters.length;index++) {
             switch(index) {
              case 0:
                      if(parameters[index] != null ) {
              		if (parameters[index] instanceof Parm) {
                       	this.parm = ((Parm) parameters[index]);
                  	} else {
                       	this.parm.set(parameters[index]);
                  	}
                  }
                
                  break;
            }
         }
          return process();
      }
      
      
      public void setFromRetcode1(Object[] params) {
      int len = params.length;
         if (len > 0)
         if(params[0] instanceof Field) 
   parm.setString(((Field)params[0] ).toCharArray());
 else    parm.setString((char[])params[0] );
      }
  
  
  
  
  
  }
