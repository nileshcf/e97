  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-29 at 05:28. */
  
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.init4.dto.*;
  import com.cloudframe.app.init4.dto.DataTable;
  import com.cloudframe.app.init4.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("init4")
  
  public class Init4 extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Init4.class);
  
  private DataTable dataTable = new DataTable() ;
  private Work work = new Work() ;
  
  
  
  
  
  
  
  
  
      /**
      * process 
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");
          ;
//  PERFORM 0000-MAINLINE
          mainline();/*0000-MAINLINE*/
          if (this.isProgramEnded()) {
              return getRc();
          }
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
      
       return getRc(); // Exit with return code
      // end of process method
      }
      /**
      * mainline 
      *   This method is derived from 
  *   COBOL Paragraph - 0000-MAINLINE COBOL Cyclomatic complexity - 9
      * Input  :  

      * - i                              COBOL Name: WS-I
      *
      * Output :  

      * - i                              COBOL Name: WS-I
      * - charWs                         COBOL Name: WS-CHAR
      * - dispNum                        COBOL Name: WS-DISP-NUM
      * - smallint                       COBOL Name: WS-SMALLINT
      * - dataTable                      COBOL Name: WS-DATA-TABLE
      *
      * @throws CFException
      */
      private void mainline() throws Exception {
      
// *
//  PERFORM VARYING WS-I FROM 1 BY 1 UNTIL WS-I > 5
          for (work.setI(1); (	( work.getI() <= 5 )) ; work.setI(work.getI() + 1) ) {
//  EVALUATE WS-I
              switch(work.getI()){
              	case 1:
//  MOVE ALL 'A' TO WS-CHAR ( WS-I )
                  dataTable.getDataTableArray1(work.getI() - 1).setCharWs(CONSTANTS.LITERAL_AAAAAAAAAA);
              break;
              	case 2:
//  MOVE ALL 'B' TO WS-CHAR ( WS-I )
                  dataTable.getDataTableArray1(work.getI() - 1).setCharWs(CONSTANTS.LITERAL_BBBBBBBBBB);
              break;
              	case 3:
//  MOVE ALL 'C' TO WS-CHAR ( WS-I )
                  dataTable.getDataTableArray1(work.getI() - 1).setCharWs(CONSTANTS.LITERAL_CCCCCCCCCC);
              break;
              	case 4:
//  MOVE ALL 'D' TO WS-CHAR ( WS-I )
                  dataTable.getDataTableArray1(work.getI() - 1).setCharWs(CONSTANTS.LITERAL_DDDDDDDDDD);
              break;
              	case 5:
//  MOVE ALL 'E' TO WS-CHAR ( WS-I )
                  dataTable.getDataTableArray1(work.getI() - 1).setCharWs(CONSTANTS.LITERAL_EEEEEEEEEE);
              break;
              }
//  MOVE WS-I TO WS-DISP-NUM ( WS-I ) WS-SMALLINT ( WS-I )
              dataTable.getDataTableArray1(work.getI() - 1).setDispNum(work.getI());
              dataTable.getDataTableArray1(work.getI() - 1).setSmallint(work.getI());
          }

// *
//  DISPLAY '>>>> PRINTING WS-DATA-TABLE BEFORE INITIALIZE'
          logger.info(">>>> PRINTING WS-DATA-TABLE BEFORE INITIALIZE"); 
//  DISPLAY ' '
          logger.info(" "); 
//  PERFORM 1000-PRINT THRU 1000-EXIT
          print();/*1000-PRINT*/
          if (this.isProgramEnded()) {
              return ;
          }

// *

// *
//  INITIALIZE WS-DATA-TABLE
          dataTable.initialize();
//  DISPLAY '>>>> PRINTING WS-DATA-TABLE AFTER  INITIALIZE'
          logger.info(">>>> PRINTING WS-DATA-TABLE AFTER  INITIALIZE"); 
//  DISPLAY ' '
          logger.info(" "); 
//  PERFORM 1000-PRINT THRU 1000-EXIT
          print();/*1000-PRINT*/
          if (this.isProgramEnded()) {
              return ;
          }

// *

// *

// *
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          this.setProgramEnded(true);
          return ;
      
      }
      /**
      * print 
      *   This method is derived from 
  *   COBOL Paragraph - 1000-PRINT COBOL Cyclomatic complexity - 3
      * Input  :  

      * - i                              COBOL Name: WS-I
      * - charWs                         COBOL Name: WS-CHAR
      * - dispNum                        COBOL Name: WS-DISP-NUM
      * - smallint                       COBOL Name: WS-SMALLINT
      *
      * Output :  

      * - i                              COBOL Name: WS-I
      *
      * @throws CFException
      */
      private void print() throws Exception {
//  PERFORM VARYING WS-I FROM 1 BY 1 UNTIL WS-I > 5
          for (work.setI(1); (	( work.getI() <= 5 )) ; work.setI(work.getI() + 1) ) {
//  DISPLAY 'WS-CHAR     (' WS-I ') = ' WS-CHAR ( WS-I )
              logger.info("WS-CHAR     ({}) = {}", String.valueOf(work.getI()), new String(dataTable.getDataTableArray1(work.getI() - 1).getCharWs())); 
//  DISPLAY 'WS-DISP-NUM (' WS-I ') = ' WS-DISP-NUM ( WS-I )
              logger.info("WS-DISP-NUM ({}) = {}", String.valueOf(work.getI()), String.valueOf(dataTable.getDataTableArray1(work.getI() - 1).getDispNum())); 
//  DISPLAY 'WS-SMALLINT (' WS-I ') = ' WS-SMALLINT ( WS-I )
              logger.info("WS-SMALLINT ({}) = {}", String.valueOf(work.getI()), String.valueOf(dataTable.getDataTableArray1(work.getI() - 1).getSmallint())); 
          }
      
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
  
  
  
  
  
  }
