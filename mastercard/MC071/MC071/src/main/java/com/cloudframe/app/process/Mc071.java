  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-23 at 23:19. */
  
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import org.springframework.beans.factory.annotation.Autowired;
  import org.springframework.beans.factory.annotation.Qualifier;
  import com.cloudframe.app.mc071.file.*;
  import com.cloudframe.app.data.Field;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.mc071.dto.*;
  import com.cloudframe.app.mc071.file.records.OutputRecord;
  import com.cloudframe.app.mc071.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("mc071")
  
  public class Mc071 extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Mc071.class);
  
  private OutputRecord outputRecord = new OutputRecord() ;
  private Work work = new Work() ;
  
  
  
  @Autowired 
  @Qualifier("mc071_outputFile")
  OutputFile outputFile;
  
  
  
  
  
  
      /**
      * process 
      * Input  : None 

      * Output :  

      * - outputFileStatus               COBOL Name: WS-OUTPUT-FILE-STATUS
      * - outputRecord                   COBOL Name: OUTPUT-RECORD
      *
      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");
//  OPEN OUTPUT OUTPUT-FILE
          outputFile.open(new String(CONSTANTS.MODE_WRITE_ONLY_36397),outputFile.getFileName(),outputFile.getOutputFileCharSet(),outputFile.getOutputFileCrlfFlag());
          work.setOutputFileStatus(outputFile.getStatusString() );
//  MOVE ALL 'A' TO OUTPUT-RECORD
          outputRecord.setString(CONSTANTS.LITERAL_1780051308);
//  WRITE OUTPUT-RECORD
          outputFile.write(outputRecord.toCharArray()); 
          outputRecord.setString(CONSTANTS.LOW_VALUE_1253879107);
          work.setOutputFileStatus(outputFile.getStatusString() );
//  MOVE ALL 'B' TO OUTPUT-RECORD
          outputRecord.setString(CONSTANTS.LITERAL_705982260);
//  WRITE OUTPUT-RECORD
          outputFile.write(outputRecord.toCharArray()); 
          outputRecord.setString(CONSTANTS.LOW_VALUE_1253879107);
          work.setOutputFileStatus(outputFile.getStatusString() );
//  MOVE ALL 'C' TO OUTPUT-RECORD
          outputRecord.setString(CONSTANTS.LITERAL_1102951468);
//  WRITE OUTPUT-RECORD
          outputFile.write(outputRecord.toCharArray()); 
          outputRecord.setString(CONSTANTS.LOW_VALUE_1253879107);
          work.setOutputFileStatus(outputFile.getStatusString() );
//  MOVE ALL 'D' TO OUTPUT-RECORD
          outputRecord.setString(CONSTANTS.LITERAL_1383082100);
//  WRITE OUTPUT-RECORD
          outputFile.write(outputRecord.toCharArray()); 
          outputRecord.setString(CONSTANTS.LOW_VALUE_1253879107);
          work.setOutputFileStatus(outputFile.getStatusString() );
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          this.setProgramEnded(true);
          return getRc();
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
        finally {
      		if(outputFile.hasOpened() && !outputFile.isReadOnly()) { 
      			outputFile.flush(); 
      		}
      }
      
      // end of process method
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
  
  
  
  
  
  }
