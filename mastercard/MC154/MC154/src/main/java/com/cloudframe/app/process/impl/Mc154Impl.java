  package com.cloudframe.app.process.impl;
  /* Java code generated by CloudFrame on 2025-04-24 at 16:31. */
  
  import com.cloudframe.app.mc154.Mc154Ctx.*;
  import com.cloudframe.app.mc154.Mc154Ctx;
  import com.cloudframe.app.process.Mc154;
  import com.cloudframe.app.process.BaseProcess;
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.dto.GlobalExecutorCtx;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.mc154.dto.*;
  import com.cloudframe.app.mc154.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("mc154")
  
  public class Mc154Impl extends CommonProcess implements Mc154 {
  
  Logger logger = LoggerFactory.getLogger(Mc154Impl.class);
  
  
  
  
  
  
  
  
  
  
      /**
      * process 
      * Input  :  

      * - cnst9999300                    COBOL Name: 300-CNST-9999
      * - cnst1300                       COBOL Name: 300-CNST-1
      * - cnst4300                       COBOL Name: 300-CNST-4
      *
      * Output :  

      * - sys009CntlKey                  COBOL Name: SYS009-CNTL-KEY
      * - cnst9999300                    COBOL Name: 300-CNST-9999
      *
      * @throws CFException
      */
      public int process(Mc154Ctx programCtx) throws Exception {
       try {
       setCodePage("1047");
            // Reset program ended flag
           programCtx.setProgramEnded(false);
ProcessInCtx methodIn = programCtx.getProcessInCtx();
ProcessOutCtx methodOut = methodIn.getProcessOutCtx();
          // MOVE SPACES TO SYS009-CNTL-KEY
          methodOut.setSys009CntlKey(CONSTANTS.SPACE_10);
          // MOVE 300-CNST-9999 TO SYS009-CNTL-KEY
          methodOut.setSys009CntlKey(methodOut.getCnst9999300ActualString());
//  DISPLAY 'SYS009-CNTL-KEY = ' SYS009-CNTL-KEY
          logger.info("SYS009-CNTL-KEY = {}", new String(methodOut.getSys009CntlKey())); 
//  IF SYS009-CNTL-KEY ( 300-CNST-1 : 300-CNST-4 ) IS EQUAL TO 300-CNST-9999
          if (compareChars(substring(methodOut.getSys009CntlKey(),(methodIn.getCnst1300()-1), (methodIn.getCnst4300() + (methodIn.getCnst1300() - 1)) ),methodOut.getCnst9999300ActualString()) == 0) { 
//  DISPLAY '*IF SYS009-CNTL-KEY = 9999 * SUCCESS *'
              logger.info("*IF SYS009-CNTL-KEY = 9999 * SUCCESS *"); 
          }
//  ELSE
          else { 
//  DISPLAY '*IF SYS009-CNTL-KEY = 9999 * FAILURE *'
              logger.info("*IF SYS009-CNTL-KEY = 9999 * FAILURE *"); 
          }
//  MOVE -9999 TO 300-CNST-9999
          methodOut.setCnst9999300(-9999);
          // MOVE SPACES TO SYS009-CNTL-KEY
          methodOut.setSys009CntlKey(CONSTANTS.SPACE_10);
          // MOVE 300-CNST-9999 TO SYS009-CNTL-KEY
          methodOut.setSys009CntlKey(methodOut.getCnst9999300ActualString());
//  DISPLAY 'SYS009-CNTL-KEY = ' SYS009-CNTL-KEY
          logger.info("SYS009-CNTL-KEY = {}", new String(methodOut.getSys009CntlKey())); 
//  IF SYS009-CNTL-KEY ( 300-CNST-1 : 300-CNST-4 ) IS EQUAL TO 300-CNST-9999
          if (compareChars(substring(methodOut.getSys009CntlKey(),(methodIn.getCnst1300()-1), (methodIn.getCnst4300() + (methodIn.getCnst1300() - 1)) ),methodOut.getCnst9999300ActualString()) == 0) { 
//  DISPLAY '*IF SYS009-CNTL-KEY = 9999 * SUCCESS *'
              logger.info("*IF SYS009-CNTL-KEY = 9999 * SUCCESS *"); 
          }
//  ELSE
          else { 
//  DISPLAY '*IF SYS009-CNTL-KEY = 9999 * FAILURE *'
              logger.info("*IF SYS009-CNTL-KEY = 9999 * FAILURE *"); 
          }
//  STOP RUN
          throw Terminate.TERMINATE;
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
      
      // end of process method
      }
  
  
  
  
  
  
  
  
  }
