  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-22 at 07:10. */
  
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import org.springframework.beans.factory.annotation.Autowired;
  import org.springframework.beans.factory.annotation.Qualifier;
  import com.cloudframe.app.mc081.file.*;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.data.Field;
  import com.cloudframe.app.mc081.dto.*;
  import com.cloudframe.app.mc081.file.records.FbRecordOut;
  import com.cloudframe.app.mc081.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("mc081")
  
  public class Mc081 extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Mc081.class);
  
  private FbRecordOut fbRecordOut = new FbRecordOut() ;
  private Work work = new Work() ;
  
  
  
  @Autowired 
  @Qualifier("mc081_fbFile")
  FbFile fbFile;
  
  
  
  
  
  
      /**
      * process 
      * Input  : None 

      * Output :  

      * - fbFileStatus                   COBOL Name: WS-FB-FILE-STATUS
      * - fbRecordOut                    COBOL Name: FB-RECORD-OUT
      * - fbBigRec                       COBOL Name: FB-BIG-REC
      *
      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
			// Declare local variables used in the method
			char[] fbFileStatus = null;
			// End of variable declaration

       try {
       setCodePage("1047");

// *
//  OPEN OUTPUT FB-FILE
          fbFile.open(new String(CONSTANTS.MODE_WRITE_ONLY_36397),fbFile.getFileName(),fbFile.getFbFileCharSet(),fbFile.getFbFileCrlfFlag());
          work.setFbFileStatus(fbFile.getStatusString() );
          fbFileStatus = work.getFbFileStatus();
//  IF NOT ( WS-FB-FILE-STATUS = ZERO)
          if ((!( allZeros(fbFileStatus) ) /*  !=  zeros*/)) { 
//  DISPLAY 'FB-FILE OPEN FAILED' WS-FB-FILE-STATUS
              logger.info("FB-FILE OPEN FAILED{}", new String(work.getFbFileStatus())); 
//  GOBACK
              setNotLogged(false); // no need to log, it is a normal termination
              this.setProgramEnded(true);
              return getRc();
          }
  
//  MOVE ALL 'A' TO FB-RECORD-OUT
          fbRecordOut.setString(CONSTANTS.LITERAL_551705848);
//  WRITE FB-RECORD-OUT
          fbFile.write(fbRecordOut.toCharArray()); 
          fbRecordOut.setString(CONSTANTS.LOW_VALUE_215751987);
          work.setFbFileStatus(fbFile.getStatusString() );
//  MOVE ALL 'B' TO FB-RECORD-OUT
          fbRecordOut.setString(CONSTANTS.LITERAL_1257227880);
//  WRITE FB-RECORD-OUT
          fbFile.write(fbRecordOut.toCharArray()); 
          fbRecordOut.setString(CONSTANTS.LOW_VALUE_215751987);
          work.setFbFileStatus(fbFile.getStatusString() );
          // MOVE ALL 'C' TO FB-BIG-REC
          work.setFbBigRec(CONSTANTS.LITERAL_157397706);
//  WRITE FB-RECORD-OUT FROM FB-BIG-REC
          fbFile.write(work.getFbBigRec()); 
          fbRecordOut.setString(CONSTANTS.LOW_VALUE_215751987);
          work.setFbFileStatus(fbFile.getStatusString() );
//  CLOSE FB-FILE
          fbFile.close(); 
          work.setFbFileStatus(fbFile.getStatusString() );
          fbFileStatus = work.getFbFileStatus();
//  IF NOT ( WS-FB-FILE-STATUS = ZERO)
          if ((!( allZeros(fbFileStatus) ) /*  !=  zeros*/)) { 
//  DISPLAY 'FB-FILE CLOSE FAILED' WS-FB-FILE-STATUS
              logger.info("FB-FILE CLOSE FAILED{}", new String(work.getFbFileStatus())); 
//  GOBACK
              setNotLogged(false); // no need to log, it is a normal termination
              this.setProgramEnded(true);
              return getRc();
          }
  
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          this.setProgramEnded(true);
          return getRc();
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
        finally {
      		if(fbFile.hasOpened() && !fbFile.isReadOnly()) { 
      			fbFile.flush(); 
      		}
      }
      
      // end of process method
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
  
  
  
  
  
  }
