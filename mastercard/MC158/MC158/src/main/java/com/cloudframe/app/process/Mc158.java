  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-29 at 05:29. */
  
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.data.Field;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.mc158.dto.*;
  import com.cloudframe.app.mc158.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("mc158")
  
  public class Mc158 extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Mc158.class);
  
  private Work work = new Work() ;
  
  
  
  
  
  
  
  
  
      /**
      * process 
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");
          ;
//  PERFORM MAINLINE
          mainline();/*MAINLINE*/
          if (this.isProgramEnded()) {
              return getRc();
          }
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
      
       return getRc(); // Exit with return code
      // end of process method
      }
      /**
      * mainline 
      *   This method is derived from 
  *   COBOL Paragraph - MAINLINE COBOL Cyclomatic complexity - 7
      * Input  :  

      * - normalIpmMsg                   COBOL Name: WS-NORMAL-IPM-MSG
      * - val1                           COBOL Name: WS-VAL-1
      *
      * Output :  

      * - posS                           COBOL Name: WS-POS-S
      * - posL                           COBOL Name: WS-POS-L
      *
      * @throws CFException
      */
      private void mainline() throws Exception {
			// Declare local variables used in the method
			char[] normalIpmMsg = null;
			char[] val1 = null;
			// End of variable declaration

      
// *
          // MOVE ZEROES TO WS-POS-S WS-POS-L
          work.setPosS((short) 0);
          work.setPosL((short) 0);
//  DISPLAY 'WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = (' WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) ')'
          logger.info("WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = ({})", new String(substring(work.getNormalIpmMsg(),(work.getPosS()-1), (work.getPosL() + (work.getPosS() - 1)) ))); 

// *
          normalIpmMsg = work.getNormalIpmMsg();
//  IF WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) = ' ' THEN
          if (Field.allSpaces(normalIpmMsg,( (work.getPosS() - 1) /*start*/ ), work.getPosL() /*len*/)) { 
//  DISPLAY '*SUCCESS*' 'WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = (' WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) ')'
              logger.info("*SUCCESS*WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = ({})", new String(substring(work.getNormalIpmMsg(),(work.getPosS()-1), (work.getPosL() + (work.getPosS() - 1)) ))); 
          }
  
//  ELSE
          else { 
//  DISPLAY '*FAILURE* ' 'WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = (' WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) ')'
              logger.info("*FAILURE* WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = ({})", new String(substring(work.getNormalIpmMsg(),(work.getPosS()-1), (work.getPosL() + (work.getPosS() - 1)) ))); 
          }

// *
          normalIpmMsg = work.getNormalIpmMsg();
//  IF WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) = LOW-VALUES
          if (Field.compareAll(normalIpmMsg,CONSTANTS.LOW_VALUE_STRING,( (work.getPosS() - 1) /*start*/ ), work.getPosL() /*len*/)) { 
//  DISPLAY '*SUCCESS*' 'WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = (' WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) ')'
              logger.info("*SUCCESS*WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = ({})", new String(substring(work.getNormalIpmMsg(),(work.getPosS()-1), (work.getPosL() + (work.getPosS() - 1)) ))); 
          }
  
//  ELSE
          else { 
//  DISPLAY '*FAILURE* ' 'WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = (' WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) ')'
              logger.info("*FAILURE* WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = ({})", new String(substring(work.getNormalIpmMsg(),(work.getPosS()-1), (work.getPosL() + (work.getPosS() - 1)) ))); 
          }

// *
          normalIpmMsg = work.getNormalIpmMsg();
//  IF WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) = HIGH-VALUES
          if (Field.allHighValues(normalIpmMsg,( (work.getPosS() - 1) /*start*/ ), work.getPosL() /*len*/)) { 
//  DISPLAY '*SUCCESS*' 'WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = (' WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) ')'
              logger.info("*SUCCESS*WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = ({})", new String(substring(work.getNormalIpmMsg(),(work.getPosS()-1), (work.getPosL() + (work.getPosS() - 1)) ))); 
          }
  
//  ELSE
          else { 
//  DISPLAY '*FAILURE* ' 'WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = (' WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) ')'
              logger.info("*FAILURE* WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = ({})", new String(substring(work.getNormalIpmMsg(),(work.getPosS()-1), (work.getPosL() + (work.getPosS() - 1)) ))); 
          }

// *
          normalIpmMsg = work.getNormalIpmMsg();
//  IF WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) = '1' THEN
          if ((work.getPosS()> 0 &&normalIpmMsg[getIndex(work.getPosS())] == '1' )) { 
//  DISPLAY '*FAILURE* ' 'WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = (' WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) ')'
              logger.info("*FAILURE* WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = ({})", new String(substring(work.getNormalIpmMsg(),(work.getPosS()-1), (work.getPosL() + (work.getPosS() - 1)) ))); 
          }
  
//  ELSE
          else { 
//  DISPLAY '*SUCCESS*' 'WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = (' WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) ')'
              logger.info("*SUCCESS*WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = ({})", new String(substring(work.getNormalIpmMsg(),(work.getPosS()-1), (work.getPosL() + (work.getPosS() - 1)) ))); 
          }

// *
          normalIpmMsg = work.getNormalIpmMsg();
          val1 = work.getVal1();
//  IF WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) = WS-VAL-1 THEN
          if (Field.compareChar(normalIpmMsg , val1 , ( (work.getPosS() - 1) /*start*/ ), work.getPosL() /*left len*/ , 1 /*right len*/)) { 
//  DISPLAY '*FAILURE* ' 'WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = (' WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) ')'
              logger.info("*FAILURE* WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = ({})", new String(substring(work.getNormalIpmMsg(),(work.getPosS()-1), (work.getPosL() + (work.getPosS() - 1)) ))); 
          }
  
//  ELSE
          else { 
//  DISPLAY '*SUCCESS*' 'WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = (' WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L ) ')'
              logger.info("*SUCCESS*WS-NORMAL-IPM-MSG ( WS-POS-S : WS-POS-L) = ({})", new String(substring(work.getNormalIpmMsg(),(work.getPosS()-1), (work.getPosL() + (work.getPosS() - 1)) ))); 
          }

// *
//  STOP RUN
          throw Terminate.TERMINATE;
      
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
  
  
  
  
  
  }
