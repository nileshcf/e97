package com.cloudframe.app.dto.serialize.d5427pol;

/**
 * The class SviTablesDataSerialized is used to define offsets in order to serialize in a fixed
 * String
 *
 * @author CloudFrame Inc. created on 2025-03-25 at 08:31. using version 5.0.0.231
 */
import com.cloudframe.app.data.Field;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class SviTablesDataSerialized extends Field {

  protected Logger logger = LoggerFactory.getLogger(SviTablesDataSerialized.class);
  /*  Length of the field, if serialized as a String */
  protected static final int SVI_TABLES_DATA_LENGTH = 496;
  /*  offset of each of Child Fields when serialized as a String */
  protected int beginSlot001;
  protected int beginSlot001Ind;
  protected int beginEff001;
  protected int beginEff001Ind;
  protected int beginCanc001;
  protected int beginCanc001Ind;
  protected int beginSlot002;
  protected int beginSlot002Ind;
  protected int beginEff002;
  protected int beginEff002Ind;
  protected int beginCanc002;
  protected int beginCanc002Ind;
  protected int beginSlot003;
  protected int beginSlot003Ind;
  protected int beginEff003;
  protected int beginEff003Ind;
  protected int beginCanc003;
  protected int beginCanc003Ind;
  protected int beginSlot004;
  protected int beginSlot004Ind;
  protected int beginEff004;
  protected int beginEff004Ind;
  protected int beginCanc004;
  protected int beginCanc004Ind;
  protected int beginSlot005;
  protected int beginSlot005Ind;
  protected int beginEff005;
  protected int beginEff005Ind;
  protected int beginCanc005;
  protected int beginCanc005Ind;
  protected int beginSlot006;
  protected int beginSlot006Ind;
  protected int beginEff006;
  protected int beginEff006Ind;
  protected int beginCanc006;
  protected int beginCanc006Ind;
  protected int beginSlot007;
  protected int beginSlot007Ind;
  protected int beginEff007;
  protected int beginEff007Ind;
  protected int beginCanc007;
  protected int beginCanc007Ind;
  protected int beginSlot008;
  protected int beginSlot008Ind;
  protected int beginEff008;
  protected int beginEff008Ind;
  protected int beginCanc008;
  protected int beginCanc008Ind;
  protected int beginSlot009;
  protected int beginSlot009Ind;
  protected int beginEff009;
  protected int beginEff009Ind;
  protected int beginCanc009;
  protected int beginCanc009Ind;
  protected int beginSlot010;
  protected int beginSlot010Ind;
  protected int beginEff010;
  protected int beginEff010Ind;
  protected int beginCanc010;
  protected int beginCanc010Ind;
  protected int beginSlot011;
  protected int beginSlot011Ind;
  protected int beginEff011;
  protected int beginEff011Ind;
  protected int beginCanc011;
  protected int beginCanc011Ind;
  protected int beginSlot012;
  protected int beginSlot012Ind;
  protected int beginEff012;
  protected int beginEff012Ind;
  protected int beginCanc012;
  protected int beginCanc012Ind;
  protected int beginSlot013;
  protected int beginSlot013Ind;
  protected int beginEff013;
  protected int beginEff013Ind;
  protected int beginCanc013;
  protected int beginCanc013Ind;
  protected int beginSlot014;
  protected int beginSlot014Ind;
  protected int beginEff014;
  protected int beginEff014Ind;
  protected int beginCanc014;
  protected int beginCanc014Ind;
  protected int beginSlot015;
  protected int beginSlot015Ind;
  protected int beginSlot016;
  protected int beginSlot016Ind;
  protected int beginSlot017;
  protected int beginSlot017Ind;
  protected int beginSlot018;
  protected int beginSlot018Ind;
  protected int beginSlot019;
  protected int beginSlot019Ind;
  protected int beginSlot020;
  protected int beginSlot020Ind;

  /** Constructor for SviTablesDataSerialized */
  public SviTablesDataSerialized() {
    init(0);
  }

  /** initializes the field in SviTablesDataSerialized */
  @Override
  protected void init(int begin) {
    setStartOffset(begin);
    setLength(SVI_TABLES_DATA_LENGTH);
    /*  set the offset/position of each field when this object is serialized as String */
    beginSlot001 = getStartOffset() + 0; // set offset for serialization

    beginSlot001Ind = getStartOffset() + 6; // set offset for serialization

    beginEff001 = getStartOffset() + 8; // set offset for serialization

    beginEff001Ind = getStartOffset() + 18; // set offset for serialization

    beginCanc001 = getStartOffset() + 20; // set offset for serialization

    beginCanc001Ind = getStartOffset() + 30; // set offset for serialization

    beginSlot002 = getStartOffset() + 32; // set offset for serialization

    beginSlot002Ind = getStartOffset() + 38; // set offset for serialization

    beginEff002 = getStartOffset() + 40; // set offset for serialization

    beginEff002Ind = getStartOffset() + 50; // set offset for serialization

    beginCanc002 = getStartOffset() + 52; // set offset for serialization

    beginCanc002Ind = getStartOffset() + 62; // set offset for serialization

    beginSlot003 = getStartOffset() + 64; // set offset for serialization

    beginSlot003Ind = getStartOffset() + 70; // set offset for serialization

    beginEff003 = getStartOffset() + 72; // set offset for serialization

    beginEff003Ind = getStartOffset() + 82; // set offset for serialization

    beginCanc003 = getStartOffset() + 84; // set offset for serialization

    beginCanc003Ind = getStartOffset() + 94; // set offset for serialization

    beginSlot004 = getStartOffset() + 96; // set offset for serialization

    beginSlot004Ind = getStartOffset() + 102; // set offset for serialization

    beginEff004 = getStartOffset() + 104; // set offset for serialization

    beginEff004Ind = getStartOffset() + 114; // set offset for serialization

    beginCanc004 = getStartOffset() + 116; // set offset for serialization

    beginCanc004Ind = getStartOffset() + 126; // set offset for serialization

    beginSlot005 = getStartOffset() + 128; // set offset for serialization

    beginSlot005Ind = getStartOffset() + 134; // set offset for serialization

    beginEff005 = getStartOffset() + 136; // set offset for serialization

    beginEff005Ind = getStartOffset() + 146; // set offset for serialization

    beginCanc005 = getStartOffset() + 148; // set offset for serialization

    beginCanc005Ind = getStartOffset() + 158; // set offset for serialization

    beginSlot006 = getStartOffset() + 160; // set offset for serialization

    beginSlot006Ind = getStartOffset() + 166; // set offset for serialization

    beginEff006 = getStartOffset() + 168; // set offset for serialization

    beginEff006Ind = getStartOffset() + 178; // set offset for serialization

    beginCanc006 = getStartOffset() + 180; // set offset for serialization

    beginCanc006Ind = getStartOffset() + 190; // set offset for serialization

    beginSlot007 = getStartOffset() + 192; // set offset for serialization

    beginSlot007Ind = getStartOffset() + 198; // set offset for serialization

    beginEff007 = getStartOffset() + 200; // set offset for serialization

    beginEff007Ind = getStartOffset() + 210; // set offset for serialization

    beginCanc007 = getStartOffset() + 212; // set offset for serialization

    beginCanc007Ind = getStartOffset() + 222; // set offset for serialization

    beginSlot008 = getStartOffset() + 224; // set offset for serialization

    beginSlot008Ind = getStartOffset() + 230; // set offset for serialization

    beginEff008 = getStartOffset() + 232; // set offset for serialization

    beginEff008Ind = getStartOffset() + 242; // set offset for serialization

    beginCanc008 = getStartOffset() + 244; // set offset for serialization

    beginCanc008Ind = getStartOffset() + 254; // set offset for serialization

    beginSlot009 = getStartOffset() + 256; // set offset for serialization

    beginSlot009Ind = getStartOffset() + 262; // set offset for serialization

    beginEff009 = getStartOffset() + 264; // set offset for serialization

    beginEff009Ind = getStartOffset() + 274; // set offset for serialization

    beginCanc009 = getStartOffset() + 276; // set offset for serialization

    beginCanc009Ind = getStartOffset() + 286; // set offset for serialization

    beginSlot010 = getStartOffset() + 288; // set offset for serialization

    beginSlot010Ind = getStartOffset() + 294; // set offset for serialization

    beginEff010 = getStartOffset() + 296; // set offset for serialization

    beginEff010Ind = getStartOffset() + 306; // set offset for serialization

    beginCanc010 = getStartOffset() + 308; // set offset for serialization

    beginCanc010Ind = getStartOffset() + 318; // set offset for serialization

    beginSlot011 = getStartOffset() + 320; // set offset for serialization

    beginSlot011Ind = getStartOffset() + 326; // set offset for serialization

    beginEff011 = getStartOffset() + 328; // set offset for serialization

    beginEff011Ind = getStartOffset() + 338; // set offset for serialization

    beginCanc011 = getStartOffset() + 340; // set offset for serialization

    beginCanc011Ind = getStartOffset() + 350; // set offset for serialization

    beginSlot012 = getStartOffset() + 352; // set offset for serialization

    beginSlot012Ind = getStartOffset() + 358; // set offset for serialization

    beginEff012 = getStartOffset() + 360; // set offset for serialization

    beginEff012Ind = getStartOffset() + 370; // set offset for serialization

    beginCanc012 = getStartOffset() + 372; // set offset for serialization

    beginCanc012Ind = getStartOffset() + 382; // set offset for serialization

    beginSlot013 = getStartOffset() + 384; // set offset for serialization

    beginSlot013Ind = getStartOffset() + 390; // set offset for serialization

    beginEff013 = getStartOffset() + 392; // set offset for serialization

    beginEff013Ind = getStartOffset() + 402; // set offset for serialization

    beginCanc013 = getStartOffset() + 404; // set offset for serialization

    beginCanc013Ind = getStartOffset() + 414; // set offset for serialization

    beginSlot014 = getStartOffset() + 416; // set offset for serialization

    beginSlot014Ind = getStartOffset() + 422; // set offset for serialization

    beginEff014 = getStartOffset() + 424; // set offset for serialization

    beginEff014Ind = getStartOffset() + 434; // set offset for serialization

    beginCanc014 = getStartOffset() + 436; // set offset for serialization

    beginCanc014Ind = getStartOffset() + 446; // set offset for serialization

    beginSlot015 = getStartOffset() + 448; // set offset for serialization

    beginSlot015Ind = getStartOffset() + 454; // set offset for serialization

    beginSlot016 = getStartOffset() + 456; // set offset for serialization

    beginSlot016Ind = getStartOffset() + 462; // set offset for serialization

    beginSlot017 = getStartOffset() + 464; // set offset for serialization

    beginSlot017Ind = getStartOffset() + 470; // set offset for serialization

    beginSlot018 = getStartOffset() + 472; // set offset for serialization

    beginSlot018Ind = getStartOffset() + 478; // set offset for serialization

    beginSlot019 = getStartOffset() + 480; // set offset for serialization

    beginSlot019Ind = getStartOffset() + 486; // set offset for serialization

    beginSlot020 = getStartOffset() + 488; // set offset for serialization

    beginSlot020Ind = getStartOffset() + 494; // set offset for serialization

    /*  end of offset */
  }

  int localSlot001Counter = -1;

  public boolean isSlot001Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot001Counter != sharedCounter;
    localSlot001Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_001_LEN = 6;
  /** serialize this Slot001 */
  protected void serializeSlot001(char[] slot001) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot001, 0, getStringValue(), beginSlot001, SLOT_001_LEN);
    localSlot001Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot001Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot001 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot001() {
    return (substring(getStringValue(), beginSlot001, beginSlot001 + SLOT_001_LEN));
  }

  int localSlot001IndCounter = -1;

  public boolean isSlot001IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot001IndCounter != sharedCounter;
    localSlot001IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_001_IND_LEN = 2;
  /** serializeSlot001Ind */
  protected void serializeSlot001Ind(short slot001Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot001Ind, SLOT_001_IND_LEN), beginSlot001Ind, SLOT_001_IND_LEN);
    localSlot001IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot001IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot001Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot001Ind() {
    return (getShort(beginSlot001Ind));
  }

  int localEff001Counter = -1;

  public boolean isEff001Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff001Counter != sharedCounter;
    localEff001Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_001_LEN = 10;
  /** serialize this Eff001 */
  protected void serializeEff001(char[] eff001) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff001, 0, getStringValue(), beginEff001, EFF_001_LEN);
    localEff001Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff001Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff001 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff001() {
    return (substring(getStringValue(), beginEff001, beginEff001 + EFF_001_LEN));
  }

  int localEff001IndCounter = -1;

  public boolean isEff001IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff001IndCounter != sharedCounter;
    localEff001IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_001_IND_LEN = 2;
  /** serializeEff001Ind */
  protected void serializeEff001Ind(short eff001Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff001Ind, EFF_001_IND_LEN), beginEff001Ind, EFF_001_IND_LEN);
    localEff001IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff001IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff001Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff001Ind() {
    return (getShort(beginEff001Ind));
  }

  int localCanc001Counter = -1;

  public boolean isCanc001Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc001Counter != sharedCounter;
    localCanc001Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_001_LEN = 10;
  /** serialize this Canc001 */
  protected void serializeCanc001(char[] canc001) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc001, 0, getStringValue(), beginCanc001, CANC_001_LEN);
    localCanc001Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc001Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc001 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc001() {
    return (substring(getStringValue(), beginCanc001, beginCanc001 + CANC_001_LEN));
  }

  int localCanc001IndCounter = -1;

  public boolean isCanc001IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc001IndCounter != sharedCounter;
    localCanc001IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_001_IND_LEN = 2;
  /** serializeCanc001Ind */
  protected void serializeCanc001Ind(short canc001Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc001Ind, CANC_001_IND_LEN), beginCanc001Ind, CANC_001_IND_LEN);
    localCanc001IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc001IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc001Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc001Ind() {
    return (getShort(beginCanc001Ind));
  }

  int localSlot002Counter = -1;

  public boolean isSlot002Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot002Counter != sharedCounter;
    localSlot002Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_002_LEN = 6;
  /** serialize this Slot002 */
  protected void serializeSlot002(char[] slot002) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot002, 0, getStringValue(), beginSlot002, SLOT_002_LEN);
    localSlot002Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot002Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot002 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot002() {
    return (substring(getStringValue(), beginSlot002, beginSlot002 + SLOT_002_LEN));
  }

  int localSlot002IndCounter = -1;

  public boolean isSlot002IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot002IndCounter != sharedCounter;
    localSlot002IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_002_IND_LEN = 2;
  /** serializeSlot002Ind */
  protected void serializeSlot002Ind(short slot002Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot002Ind, SLOT_002_IND_LEN), beginSlot002Ind, SLOT_002_IND_LEN);
    localSlot002IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot002IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot002Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot002Ind() {
    return (getShort(beginSlot002Ind));
  }

  int localEff002Counter = -1;

  public boolean isEff002Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff002Counter != sharedCounter;
    localEff002Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_002_LEN = 10;
  /** serialize this Eff002 */
  protected void serializeEff002(char[] eff002) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff002, 0, getStringValue(), beginEff002, EFF_002_LEN);
    localEff002Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff002Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff002 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff002() {
    return (substring(getStringValue(), beginEff002, beginEff002 + EFF_002_LEN));
  }

  int localEff002IndCounter = -1;

  public boolean isEff002IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff002IndCounter != sharedCounter;
    localEff002IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_002_IND_LEN = 2;
  /** serializeEff002Ind */
  protected void serializeEff002Ind(short eff002Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff002Ind, EFF_002_IND_LEN), beginEff002Ind, EFF_002_IND_LEN);
    localEff002IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff002IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff002Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff002Ind() {
    return (getShort(beginEff002Ind));
  }

  int localCanc002Counter = -1;

  public boolean isCanc002Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc002Counter != sharedCounter;
    localCanc002Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_002_LEN = 10;
  /** serialize this Canc002 */
  protected void serializeCanc002(char[] canc002) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc002, 0, getStringValue(), beginCanc002, CANC_002_LEN);
    localCanc002Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc002Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc002 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc002() {
    return (substring(getStringValue(), beginCanc002, beginCanc002 + CANC_002_LEN));
  }

  int localCanc002IndCounter = -1;

  public boolean isCanc002IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc002IndCounter != sharedCounter;
    localCanc002IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_002_IND_LEN = 2;
  /** serializeCanc002Ind */
  protected void serializeCanc002Ind(short canc002Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc002Ind, CANC_002_IND_LEN), beginCanc002Ind, CANC_002_IND_LEN);
    localCanc002IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc002IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc002Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc002Ind() {
    return (getShort(beginCanc002Ind));
  }

  int localSlot003Counter = -1;

  public boolean isSlot003Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot003Counter != sharedCounter;
    localSlot003Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_003_LEN = 6;
  /** serialize this Slot003 */
  protected void serializeSlot003(char[] slot003) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot003, 0, getStringValue(), beginSlot003, SLOT_003_LEN);
    localSlot003Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot003Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot003 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot003() {
    return (substring(getStringValue(), beginSlot003, beginSlot003 + SLOT_003_LEN));
  }

  int localSlot003IndCounter = -1;

  public boolean isSlot003IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot003IndCounter != sharedCounter;
    localSlot003IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_003_IND_LEN = 2;
  /** serializeSlot003Ind */
  protected void serializeSlot003Ind(short slot003Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot003Ind, SLOT_003_IND_LEN), beginSlot003Ind, SLOT_003_IND_LEN);
    localSlot003IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot003IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot003Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot003Ind() {
    return (getShort(beginSlot003Ind));
  }

  int localEff003Counter = -1;

  public boolean isEff003Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff003Counter != sharedCounter;
    localEff003Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_003_LEN = 10;
  /** serialize this Eff003 */
  protected void serializeEff003(char[] eff003) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff003, 0, getStringValue(), beginEff003, EFF_003_LEN);
    localEff003Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff003Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff003 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff003() {
    return (substring(getStringValue(), beginEff003, beginEff003 + EFF_003_LEN));
  }

  int localEff003IndCounter = -1;

  public boolean isEff003IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff003IndCounter != sharedCounter;
    localEff003IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_003_IND_LEN = 2;
  /** serializeEff003Ind */
  protected void serializeEff003Ind(short eff003Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff003Ind, EFF_003_IND_LEN), beginEff003Ind, EFF_003_IND_LEN);
    localEff003IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff003IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff003Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff003Ind() {
    return (getShort(beginEff003Ind));
  }

  int localCanc003Counter = -1;

  public boolean isCanc003Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc003Counter != sharedCounter;
    localCanc003Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_003_LEN = 10;
  /** serialize this Canc003 */
  protected void serializeCanc003(char[] canc003) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc003, 0, getStringValue(), beginCanc003, CANC_003_LEN);
    localCanc003Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc003Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc003 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc003() {
    return (substring(getStringValue(), beginCanc003, beginCanc003 + CANC_003_LEN));
  }

  int localCanc003IndCounter = -1;

  public boolean isCanc003IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc003IndCounter != sharedCounter;
    localCanc003IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_003_IND_LEN = 2;
  /** serializeCanc003Ind */
  protected void serializeCanc003Ind(short canc003Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc003Ind, CANC_003_IND_LEN), beginCanc003Ind, CANC_003_IND_LEN);
    localCanc003IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc003IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc003Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc003Ind() {
    return (getShort(beginCanc003Ind));
  }

  int localSlot004Counter = -1;

  public boolean isSlot004Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot004Counter != sharedCounter;
    localSlot004Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_004_LEN = 6;
  /** serialize this Slot004 */
  protected void serializeSlot004(char[] slot004) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot004, 0, getStringValue(), beginSlot004, SLOT_004_LEN);
    localSlot004Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot004Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot004 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot004() {
    return (substring(getStringValue(), beginSlot004, beginSlot004 + SLOT_004_LEN));
  }

  int localSlot004IndCounter = -1;

  public boolean isSlot004IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot004IndCounter != sharedCounter;
    localSlot004IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_004_IND_LEN = 2;
  /** serializeSlot004Ind */
  protected void serializeSlot004Ind(short slot004Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot004Ind, SLOT_004_IND_LEN), beginSlot004Ind, SLOT_004_IND_LEN);
    localSlot004IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot004IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot004Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot004Ind() {
    return (getShort(beginSlot004Ind));
  }

  int localEff004Counter = -1;

  public boolean isEff004Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff004Counter != sharedCounter;
    localEff004Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_004_LEN = 10;
  /** serialize this Eff004 */
  protected void serializeEff004(char[] eff004) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff004, 0, getStringValue(), beginEff004, EFF_004_LEN);
    localEff004Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff004Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff004 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff004() {
    return (substring(getStringValue(), beginEff004, beginEff004 + EFF_004_LEN));
  }

  int localEff004IndCounter = -1;

  public boolean isEff004IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff004IndCounter != sharedCounter;
    localEff004IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_004_IND_LEN = 2;
  /** serializeEff004Ind */
  protected void serializeEff004Ind(short eff004Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff004Ind, EFF_004_IND_LEN), beginEff004Ind, EFF_004_IND_LEN);
    localEff004IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff004IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff004Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff004Ind() {
    return (getShort(beginEff004Ind));
  }

  int localCanc004Counter = -1;

  public boolean isCanc004Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc004Counter != sharedCounter;
    localCanc004Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_004_LEN = 10;
  /** serialize this Canc004 */
  protected void serializeCanc004(char[] canc004) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc004, 0, getStringValue(), beginCanc004, CANC_004_LEN);
    localCanc004Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc004Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc004 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc004() {
    return (substring(getStringValue(), beginCanc004, beginCanc004 + CANC_004_LEN));
  }

  int localCanc004IndCounter = -1;

  public boolean isCanc004IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc004IndCounter != sharedCounter;
    localCanc004IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_004_IND_LEN = 2;
  /** serializeCanc004Ind */
  protected void serializeCanc004Ind(short canc004Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc004Ind, CANC_004_IND_LEN), beginCanc004Ind, CANC_004_IND_LEN);
    localCanc004IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc004IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc004Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc004Ind() {
    return (getShort(beginCanc004Ind));
  }

  int localSlot005Counter = -1;

  public boolean isSlot005Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot005Counter != sharedCounter;
    localSlot005Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_005_LEN = 6;
  /** serialize this Slot005 */
  protected void serializeSlot005(char[] slot005) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot005, 0, getStringValue(), beginSlot005, SLOT_005_LEN);
    localSlot005Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot005Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot005 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot005() {
    return (substring(getStringValue(), beginSlot005, beginSlot005 + SLOT_005_LEN));
  }

  int localSlot005IndCounter = -1;

  public boolean isSlot005IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot005IndCounter != sharedCounter;
    localSlot005IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_005_IND_LEN = 2;
  /** serializeSlot005Ind */
  protected void serializeSlot005Ind(short slot005Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot005Ind, SLOT_005_IND_LEN), beginSlot005Ind, SLOT_005_IND_LEN);
    localSlot005IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot005IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot005Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot005Ind() {
    return (getShort(beginSlot005Ind));
  }

  int localEff005Counter = -1;

  public boolean isEff005Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff005Counter != sharedCounter;
    localEff005Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_005_LEN = 10;
  /** serialize this Eff005 */
  protected void serializeEff005(char[] eff005) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff005, 0, getStringValue(), beginEff005, EFF_005_LEN);
    localEff005Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff005Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff005 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff005() {
    return (substring(getStringValue(), beginEff005, beginEff005 + EFF_005_LEN));
  }

  int localEff005IndCounter = -1;

  public boolean isEff005IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff005IndCounter != sharedCounter;
    localEff005IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_005_IND_LEN = 2;
  /** serializeEff005Ind */
  protected void serializeEff005Ind(short eff005Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff005Ind, EFF_005_IND_LEN), beginEff005Ind, EFF_005_IND_LEN);
    localEff005IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff005IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff005Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff005Ind() {
    return (getShort(beginEff005Ind));
  }

  int localCanc005Counter = -1;

  public boolean isCanc005Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc005Counter != sharedCounter;
    localCanc005Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_005_LEN = 10;
  /** serialize this Canc005 */
  protected void serializeCanc005(char[] canc005) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc005, 0, getStringValue(), beginCanc005, CANC_005_LEN);
    localCanc005Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc005Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc005 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc005() {
    return (substring(getStringValue(), beginCanc005, beginCanc005 + CANC_005_LEN));
  }

  int localCanc005IndCounter = -1;

  public boolean isCanc005IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc005IndCounter != sharedCounter;
    localCanc005IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_005_IND_LEN = 2;
  /** serializeCanc005Ind */
  protected void serializeCanc005Ind(short canc005Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc005Ind, CANC_005_IND_LEN), beginCanc005Ind, CANC_005_IND_LEN);
    localCanc005IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc005IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc005Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc005Ind() {
    return (getShort(beginCanc005Ind));
  }

  int localSlot006Counter = -1;

  public boolean isSlot006Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot006Counter != sharedCounter;
    localSlot006Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_006_LEN = 6;
  /** serialize this Slot006 */
  protected void serializeSlot006(char[] slot006) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot006, 0, getStringValue(), beginSlot006, SLOT_006_LEN);
    localSlot006Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot006Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot006 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot006() {
    return (substring(getStringValue(), beginSlot006, beginSlot006 + SLOT_006_LEN));
  }

  int localSlot006IndCounter = -1;

  public boolean isSlot006IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot006IndCounter != sharedCounter;
    localSlot006IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_006_IND_LEN = 2;
  /** serializeSlot006Ind */
  protected void serializeSlot006Ind(short slot006Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot006Ind, SLOT_006_IND_LEN), beginSlot006Ind, SLOT_006_IND_LEN);
    localSlot006IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot006IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot006Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot006Ind() {
    return (getShort(beginSlot006Ind));
  }

  int localEff006Counter = -1;

  public boolean isEff006Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff006Counter != sharedCounter;
    localEff006Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_006_LEN = 10;
  /** serialize this Eff006 */
  protected void serializeEff006(char[] eff006) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff006, 0, getStringValue(), beginEff006, EFF_006_LEN);
    localEff006Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff006Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff006 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff006() {
    return (substring(getStringValue(), beginEff006, beginEff006 + EFF_006_LEN));
  }

  int localEff006IndCounter = -1;

  public boolean isEff006IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff006IndCounter != sharedCounter;
    localEff006IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_006_IND_LEN = 2;
  /** serializeEff006Ind */
  protected void serializeEff006Ind(short eff006Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff006Ind, EFF_006_IND_LEN), beginEff006Ind, EFF_006_IND_LEN);
    localEff006IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff006IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff006Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff006Ind() {
    return (getShort(beginEff006Ind));
  }

  int localCanc006Counter = -1;

  public boolean isCanc006Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc006Counter != sharedCounter;
    localCanc006Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_006_LEN = 10;
  /** serialize this Canc006 */
  protected void serializeCanc006(char[] canc006) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc006, 0, getStringValue(), beginCanc006, CANC_006_LEN);
    localCanc006Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc006Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc006 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc006() {
    return (substring(getStringValue(), beginCanc006, beginCanc006 + CANC_006_LEN));
  }

  int localCanc006IndCounter = -1;

  public boolean isCanc006IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc006IndCounter != sharedCounter;
    localCanc006IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_006_IND_LEN = 2;
  /** serializeCanc006Ind */
  protected void serializeCanc006Ind(short canc006Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc006Ind, CANC_006_IND_LEN), beginCanc006Ind, CANC_006_IND_LEN);
    localCanc006IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc006IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc006Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc006Ind() {
    return (getShort(beginCanc006Ind));
  }

  int localSlot007Counter = -1;

  public boolean isSlot007Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot007Counter != sharedCounter;
    localSlot007Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_007_LEN = 6;
  /** serialize this Slot007 */
  protected void serializeSlot007(char[] slot007) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot007, 0, getStringValue(), beginSlot007, SLOT_007_LEN);
    localSlot007Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot007Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot007 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot007() {
    return (substring(getStringValue(), beginSlot007, beginSlot007 + SLOT_007_LEN));
  }

  int localSlot007IndCounter = -1;

  public boolean isSlot007IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot007IndCounter != sharedCounter;
    localSlot007IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_007_IND_LEN = 2;
  /** serializeSlot007Ind */
  protected void serializeSlot007Ind(short slot007Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot007Ind, SLOT_007_IND_LEN), beginSlot007Ind, SLOT_007_IND_LEN);
    localSlot007IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot007IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot007Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot007Ind() {
    return (getShort(beginSlot007Ind));
  }

  int localEff007Counter = -1;

  public boolean isEff007Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff007Counter != sharedCounter;
    localEff007Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_007_LEN = 10;
  /** serialize this Eff007 */
  protected void serializeEff007(char[] eff007) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff007, 0, getStringValue(), beginEff007, EFF_007_LEN);
    localEff007Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff007Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff007 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff007() {
    return (substring(getStringValue(), beginEff007, beginEff007 + EFF_007_LEN));
  }

  int localEff007IndCounter = -1;

  public boolean isEff007IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff007IndCounter != sharedCounter;
    localEff007IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_007_IND_LEN = 2;
  /** serializeEff007Ind */
  protected void serializeEff007Ind(short eff007Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff007Ind, EFF_007_IND_LEN), beginEff007Ind, EFF_007_IND_LEN);
    localEff007IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff007IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff007Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff007Ind() {
    return (getShort(beginEff007Ind));
  }

  int localCanc007Counter = -1;

  public boolean isCanc007Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc007Counter != sharedCounter;
    localCanc007Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_007_LEN = 10;
  /** serialize this Canc007 */
  protected void serializeCanc007(char[] canc007) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc007, 0, getStringValue(), beginCanc007, CANC_007_LEN);
    localCanc007Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc007Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc007 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc007() {
    return (substring(getStringValue(), beginCanc007, beginCanc007 + CANC_007_LEN));
  }

  int localCanc007IndCounter = -1;

  public boolean isCanc007IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc007IndCounter != sharedCounter;
    localCanc007IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_007_IND_LEN = 2;
  /** serializeCanc007Ind */
  protected void serializeCanc007Ind(short canc007Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc007Ind, CANC_007_IND_LEN), beginCanc007Ind, CANC_007_IND_LEN);
    localCanc007IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc007IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc007Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc007Ind() {
    return (getShort(beginCanc007Ind));
  }

  int localSlot008Counter = -1;

  public boolean isSlot008Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot008Counter != sharedCounter;
    localSlot008Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_008_LEN = 6;
  /** serialize this Slot008 */
  protected void serializeSlot008(char[] slot008) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot008, 0, getStringValue(), beginSlot008, SLOT_008_LEN);
    localSlot008Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot008Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot008 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot008() {
    return (substring(getStringValue(), beginSlot008, beginSlot008 + SLOT_008_LEN));
  }

  int localSlot008IndCounter = -1;

  public boolean isSlot008IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot008IndCounter != sharedCounter;
    localSlot008IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_008_IND_LEN = 2;
  /** serializeSlot008Ind */
  protected void serializeSlot008Ind(short slot008Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot008Ind, SLOT_008_IND_LEN), beginSlot008Ind, SLOT_008_IND_LEN);
    localSlot008IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot008IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot008Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot008Ind() {
    return (getShort(beginSlot008Ind));
  }

  int localEff008Counter = -1;

  public boolean isEff008Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff008Counter != sharedCounter;
    localEff008Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_008_LEN = 10;
  /** serialize this Eff008 */
  protected void serializeEff008(char[] eff008) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff008, 0, getStringValue(), beginEff008, EFF_008_LEN);
    localEff008Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff008Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff008 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff008() {
    return (substring(getStringValue(), beginEff008, beginEff008 + EFF_008_LEN));
  }

  int localEff008IndCounter = -1;

  public boolean isEff008IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff008IndCounter != sharedCounter;
    localEff008IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_008_IND_LEN = 2;
  /** serializeEff008Ind */
  protected void serializeEff008Ind(short eff008Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff008Ind, EFF_008_IND_LEN), beginEff008Ind, EFF_008_IND_LEN);
    localEff008IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff008IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff008Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff008Ind() {
    return (getShort(beginEff008Ind));
  }

  int localCanc008Counter = -1;

  public boolean isCanc008Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc008Counter != sharedCounter;
    localCanc008Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_008_LEN = 10;
  /** serialize this Canc008 */
  protected void serializeCanc008(char[] canc008) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc008, 0, getStringValue(), beginCanc008, CANC_008_LEN);
    localCanc008Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc008Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc008 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc008() {
    return (substring(getStringValue(), beginCanc008, beginCanc008 + CANC_008_LEN));
  }

  int localCanc008IndCounter = -1;

  public boolean isCanc008IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc008IndCounter != sharedCounter;
    localCanc008IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_008_IND_LEN = 2;
  /** serializeCanc008Ind */
  protected void serializeCanc008Ind(short canc008Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc008Ind, CANC_008_IND_LEN), beginCanc008Ind, CANC_008_IND_LEN);
    localCanc008IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc008IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc008Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc008Ind() {
    return (getShort(beginCanc008Ind));
  }

  int localSlot009Counter = -1;

  public boolean isSlot009Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot009Counter != sharedCounter;
    localSlot009Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_009_LEN = 6;
  /** serialize this Slot009 */
  protected void serializeSlot009(char[] slot009) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot009, 0, getStringValue(), beginSlot009, SLOT_009_LEN);
    localSlot009Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot009Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot009 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot009() {
    return (substring(getStringValue(), beginSlot009, beginSlot009 + SLOT_009_LEN));
  }

  int localSlot009IndCounter = -1;

  public boolean isSlot009IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot009IndCounter != sharedCounter;
    localSlot009IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_009_IND_LEN = 2;
  /** serializeSlot009Ind */
  protected void serializeSlot009Ind(short slot009Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot009Ind, SLOT_009_IND_LEN), beginSlot009Ind, SLOT_009_IND_LEN);
    localSlot009IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot009IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot009Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot009Ind() {
    return (getShort(beginSlot009Ind));
  }

  int localEff009Counter = -1;

  public boolean isEff009Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff009Counter != sharedCounter;
    localEff009Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_009_LEN = 10;
  /** serialize this Eff009 */
  protected void serializeEff009(char[] eff009) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff009, 0, getStringValue(), beginEff009, EFF_009_LEN);
    localEff009Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff009Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff009 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff009() {
    return (substring(getStringValue(), beginEff009, beginEff009 + EFF_009_LEN));
  }

  int localEff009IndCounter = -1;

  public boolean isEff009IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff009IndCounter != sharedCounter;
    localEff009IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_009_IND_LEN = 2;
  /** serializeEff009Ind */
  protected void serializeEff009Ind(short eff009Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff009Ind, EFF_009_IND_LEN), beginEff009Ind, EFF_009_IND_LEN);
    localEff009IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff009IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff009Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff009Ind() {
    return (getShort(beginEff009Ind));
  }

  int localCanc009Counter = -1;

  public boolean isCanc009Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc009Counter != sharedCounter;
    localCanc009Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_009_LEN = 10;
  /** serialize this Canc009 */
  protected void serializeCanc009(char[] canc009) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc009, 0, getStringValue(), beginCanc009, CANC_009_LEN);
    localCanc009Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc009Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc009 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc009() {
    return (substring(getStringValue(), beginCanc009, beginCanc009 + CANC_009_LEN));
  }

  int localCanc009IndCounter = -1;

  public boolean isCanc009IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc009IndCounter != sharedCounter;
    localCanc009IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_009_IND_LEN = 2;
  /** serializeCanc009Ind */
  protected void serializeCanc009Ind(short canc009Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc009Ind, CANC_009_IND_LEN), beginCanc009Ind, CANC_009_IND_LEN);
    localCanc009IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc009IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc009Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc009Ind() {
    return (getShort(beginCanc009Ind));
  }

  int localSlot010Counter = -1;

  public boolean isSlot010Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot010Counter != sharedCounter;
    localSlot010Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_010_LEN = 6;
  /** serialize this Slot010 */
  protected void serializeSlot010(char[] slot010) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot010, 0, getStringValue(), beginSlot010, SLOT_010_LEN);
    localSlot010Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot010Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot010 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot010() {
    return (substring(getStringValue(), beginSlot010, beginSlot010 + SLOT_010_LEN));
  }

  int localSlot010IndCounter = -1;

  public boolean isSlot010IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot010IndCounter != sharedCounter;
    localSlot010IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_010_IND_LEN = 2;
  /** serializeSlot010Ind */
  protected void serializeSlot010Ind(short slot010Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot010Ind, SLOT_010_IND_LEN), beginSlot010Ind, SLOT_010_IND_LEN);
    localSlot010IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot010IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot010Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot010Ind() {
    return (getShort(beginSlot010Ind));
  }

  int localEff010Counter = -1;

  public boolean isEff010Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff010Counter != sharedCounter;
    localEff010Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_010_LEN = 10;
  /** serialize this Eff010 */
  protected void serializeEff010(char[] eff010) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff010, 0, getStringValue(), beginEff010, EFF_010_LEN);
    localEff010Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff010Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff010 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff010() {
    return (substring(getStringValue(), beginEff010, beginEff010 + EFF_010_LEN));
  }

  int localEff010IndCounter = -1;

  public boolean isEff010IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff010IndCounter != sharedCounter;
    localEff010IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_010_IND_LEN = 2;
  /** serializeEff010Ind */
  protected void serializeEff010Ind(short eff010Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff010Ind, EFF_010_IND_LEN), beginEff010Ind, EFF_010_IND_LEN);
    localEff010IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff010IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff010Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff010Ind() {
    return (getShort(beginEff010Ind));
  }

  int localCanc010Counter = -1;

  public boolean isCanc010Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc010Counter != sharedCounter;
    localCanc010Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_010_LEN = 10;
  /** serialize this Canc010 */
  protected void serializeCanc010(char[] canc010) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc010, 0, getStringValue(), beginCanc010, CANC_010_LEN);
    localCanc010Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc010Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc010 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc010() {
    return (substring(getStringValue(), beginCanc010, beginCanc010 + CANC_010_LEN));
  }

  int localCanc010IndCounter = -1;

  public boolean isCanc010IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc010IndCounter != sharedCounter;
    localCanc010IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_010_IND_LEN = 2;
  /** serializeCanc010Ind */
  protected void serializeCanc010Ind(short canc010Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc010Ind, CANC_010_IND_LEN), beginCanc010Ind, CANC_010_IND_LEN);
    localCanc010IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc010IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc010Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc010Ind() {
    return (getShort(beginCanc010Ind));
  }

  int localSlot011Counter = -1;

  public boolean isSlot011Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot011Counter != sharedCounter;
    localSlot011Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_011_LEN = 6;
  /** serialize this Slot011 */
  protected void serializeSlot011(char[] slot011) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot011, 0, getStringValue(), beginSlot011, SLOT_011_LEN);
    localSlot011Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot011Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot011 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot011() {
    return (substring(getStringValue(), beginSlot011, beginSlot011 + SLOT_011_LEN));
  }

  int localSlot011IndCounter = -1;

  public boolean isSlot011IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot011IndCounter != sharedCounter;
    localSlot011IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_011_IND_LEN = 2;
  /** serializeSlot011Ind */
  protected void serializeSlot011Ind(short slot011Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot011Ind, SLOT_011_IND_LEN), beginSlot011Ind, SLOT_011_IND_LEN);
    localSlot011IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot011IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot011Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot011Ind() {
    return (getShort(beginSlot011Ind));
  }

  int localEff011Counter = -1;

  public boolean isEff011Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff011Counter != sharedCounter;
    localEff011Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_011_LEN = 10;
  /** serialize this Eff011 */
  protected void serializeEff011(char[] eff011) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff011, 0, getStringValue(), beginEff011, EFF_011_LEN);
    localEff011Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff011Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff011 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff011() {
    return (substring(getStringValue(), beginEff011, beginEff011 + EFF_011_LEN));
  }

  int localEff011IndCounter = -1;

  public boolean isEff011IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff011IndCounter != sharedCounter;
    localEff011IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_011_IND_LEN = 2;
  /** serializeEff011Ind */
  protected void serializeEff011Ind(short eff011Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff011Ind, EFF_011_IND_LEN), beginEff011Ind, EFF_011_IND_LEN);
    localEff011IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff011IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff011Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff011Ind() {
    return (getShort(beginEff011Ind));
  }

  int localCanc011Counter = -1;

  public boolean isCanc011Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc011Counter != sharedCounter;
    localCanc011Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_011_LEN = 10;
  /** serialize this Canc011 */
  protected void serializeCanc011(char[] canc011) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc011, 0, getStringValue(), beginCanc011, CANC_011_LEN);
    localCanc011Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc011Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc011 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc011() {
    return (substring(getStringValue(), beginCanc011, beginCanc011 + CANC_011_LEN));
  }

  int localCanc011IndCounter = -1;

  public boolean isCanc011IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc011IndCounter != sharedCounter;
    localCanc011IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_011_IND_LEN = 2;
  /** serializeCanc011Ind */
  protected void serializeCanc011Ind(short canc011Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc011Ind, CANC_011_IND_LEN), beginCanc011Ind, CANC_011_IND_LEN);
    localCanc011IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc011IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc011Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc011Ind() {
    return (getShort(beginCanc011Ind));
  }

  int localSlot012Counter = -1;

  public boolean isSlot012Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot012Counter != sharedCounter;
    localSlot012Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_012_LEN = 6;
  /** serialize this Slot012 */
  protected void serializeSlot012(char[] slot012) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot012, 0, getStringValue(), beginSlot012, SLOT_012_LEN);
    localSlot012Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot012Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot012 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot012() {
    return (substring(getStringValue(), beginSlot012, beginSlot012 + SLOT_012_LEN));
  }

  int localSlot012IndCounter = -1;

  public boolean isSlot012IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot012IndCounter != sharedCounter;
    localSlot012IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_012_IND_LEN = 2;
  /** serializeSlot012Ind */
  protected void serializeSlot012Ind(short slot012Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot012Ind, SLOT_012_IND_LEN), beginSlot012Ind, SLOT_012_IND_LEN);
    localSlot012IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot012IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot012Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot012Ind() {
    return (getShort(beginSlot012Ind));
  }

  int localEff012Counter = -1;

  public boolean isEff012Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff012Counter != sharedCounter;
    localEff012Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_012_LEN = 10;
  /** serialize this Eff012 */
  protected void serializeEff012(char[] eff012) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff012, 0, getStringValue(), beginEff012, EFF_012_LEN);
    localEff012Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff012Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff012 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff012() {
    return (substring(getStringValue(), beginEff012, beginEff012 + EFF_012_LEN));
  }

  int localEff012IndCounter = -1;

  public boolean isEff012IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff012IndCounter != sharedCounter;
    localEff012IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_012_IND_LEN = 2;
  /** serializeEff012Ind */
  protected void serializeEff012Ind(short eff012Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff012Ind, EFF_012_IND_LEN), beginEff012Ind, EFF_012_IND_LEN);
    localEff012IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff012IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff012Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff012Ind() {
    return (getShort(beginEff012Ind));
  }

  int localCanc012Counter = -1;

  public boolean isCanc012Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc012Counter != sharedCounter;
    localCanc012Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_012_LEN = 10;
  /** serialize this Canc012 */
  protected void serializeCanc012(char[] canc012) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc012, 0, getStringValue(), beginCanc012, CANC_012_LEN);
    localCanc012Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc012Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc012 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc012() {
    return (substring(getStringValue(), beginCanc012, beginCanc012 + CANC_012_LEN));
  }

  int localCanc012IndCounter = -1;

  public boolean isCanc012IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc012IndCounter != sharedCounter;
    localCanc012IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_012_IND_LEN = 2;
  /** serializeCanc012Ind */
  protected void serializeCanc012Ind(short canc012Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc012Ind, CANC_012_IND_LEN), beginCanc012Ind, CANC_012_IND_LEN);
    localCanc012IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc012IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc012Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc012Ind() {
    return (getShort(beginCanc012Ind));
  }

  int localSlot013Counter = -1;

  public boolean isSlot013Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot013Counter != sharedCounter;
    localSlot013Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_013_LEN = 6;
  /** serialize this Slot013 */
  protected void serializeSlot013(char[] slot013) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot013, 0, getStringValue(), beginSlot013, SLOT_013_LEN);
    localSlot013Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot013Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot013 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot013() {
    return (substring(getStringValue(), beginSlot013, beginSlot013 + SLOT_013_LEN));
  }

  int localSlot013IndCounter = -1;

  public boolean isSlot013IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot013IndCounter != sharedCounter;
    localSlot013IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_013_IND_LEN = 2;
  /** serializeSlot013Ind */
  protected void serializeSlot013Ind(short slot013Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot013Ind, SLOT_013_IND_LEN), beginSlot013Ind, SLOT_013_IND_LEN);
    localSlot013IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot013IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot013Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot013Ind() {
    return (getShort(beginSlot013Ind));
  }

  int localEff013Counter = -1;

  public boolean isEff013Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff013Counter != sharedCounter;
    localEff013Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_013_LEN = 10;
  /** serialize this Eff013 */
  protected void serializeEff013(char[] eff013) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff013, 0, getStringValue(), beginEff013, EFF_013_LEN);
    localEff013Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff013Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff013 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff013() {
    return (substring(getStringValue(), beginEff013, beginEff013 + EFF_013_LEN));
  }

  int localEff013IndCounter = -1;

  public boolean isEff013IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff013IndCounter != sharedCounter;
    localEff013IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_013_IND_LEN = 2;
  /** serializeEff013Ind */
  protected void serializeEff013Ind(short eff013Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff013Ind, EFF_013_IND_LEN), beginEff013Ind, EFF_013_IND_LEN);
    localEff013IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff013IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff013Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff013Ind() {
    return (getShort(beginEff013Ind));
  }

  int localCanc013Counter = -1;

  public boolean isCanc013Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc013Counter != sharedCounter;
    localCanc013Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_013_LEN = 10;
  /** serialize this Canc013 */
  protected void serializeCanc013(char[] canc013) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc013, 0, getStringValue(), beginCanc013, CANC_013_LEN);
    localCanc013Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc013Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc013 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc013() {
    return (substring(getStringValue(), beginCanc013, beginCanc013 + CANC_013_LEN));
  }

  int localCanc013IndCounter = -1;

  public boolean isCanc013IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc013IndCounter != sharedCounter;
    localCanc013IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_013_IND_LEN = 2;
  /** serializeCanc013Ind */
  protected void serializeCanc013Ind(short canc013Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc013Ind, CANC_013_IND_LEN), beginCanc013Ind, CANC_013_IND_LEN);
    localCanc013IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc013IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc013Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc013Ind() {
    return (getShort(beginCanc013Ind));
  }

  int localSlot014Counter = -1;

  public boolean isSlot014Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot014Counter != sharedCounter;
    localSlot014Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_014_LEN = 6;
  /** serialize this Slot014 */
  protected void serializeSlot014(char[] slot014) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot014, 0, getStringValue(), beginSlot014, SLOT_014_LEN);
    localSlot014Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot014Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot014 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot014() {
    return (substring(getStringValue(), beginSlot014, beginSlot014 + SLOT_014_LEN));
  }

  int localSlot014IndCounter = -1;

  public boolean isSlot014IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot014IndCounter != sharedCounter;
    localSlot014IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_014_IND_LEN = 2;
  /** serializeSlot014Ind */
  protected void serializeSlot014Ind(short slot014Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot014Ind, SLOT_014_IND_LEN), beginSlot014Ind, SLOT_014_IND_LEN);
    localSlot014IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot014IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot014Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot014Ind() {
    return (getShort(beginSlot014Ind));
  }

  int localEff014Counter = -1;

  public boolean isEff014Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff014Counter != sharedCounter;
    localEff014Counter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_014_LEN = 10;
  /** serialize this Eff014 */
  protected void serializeEff014(char[] eff014) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(eff014, 0, getStringValue(), beginEff014, EFF_014_LEN);
    localEff014Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkEff014Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshEff014 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshEff014() {
    return (substring(getStringValue(), beginEff014, beginEff014 + EFF_014_LEN));
  }

  int localEff014IndCounter = -1;

  public boolean isEff014IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localEff014IndCounter != sharedCounter;
    localEff014IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int EFF_014_IND_LEN = 2;
  /** serializeEff014Ind */
  protected void serializeEff014Ind(short eff014Ind) {
    replaceValue( //  save the value as string
        getBinaryString(eff014Ind, EFF_014_IND_LEN), beginEff014Ind, EFF_014_IND_LEN);
    localEff014IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkEff014IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshEff014Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshEff014Ind() {
    return (getShort(beginEff014Ind));
  }

  int localCanc014Counter = -1;

  public boolean isCanc014Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc014Counter != sharedCounter;
    localCanc014Counter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_014_LEN = 10;
  /** serialize this Canc014 */
  protected void serializeCanc014(char[] canc014) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(canc014, 0, getStringValue(), beginCanc014, CANC_014_LEN);
    localCanc014Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkCanc014Constraints(char[] value) {
    return super.checkConstraints(value, 10, false, false);
  }
  /**
   * refreshCanc014 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshCanc014() {
    return (substring(getStringValue(), beginCanc014, beginCanc014 + CANC_014_LEN));
  }

  int localCanc014IndCounter = -1;

  public boolean isCanc014IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localCanc014IndCounter != sharedCounter;
    localCanc014IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int CANC_014_IND_LEN = 2;
  /** serializeCanc014Ind */
  protected void serializeCanc014Ind(short canc014Ind) {
    replaceValue( //  save the value as string
        getBinaryString(canc014Ind, CANC_014_IND_LEN), beginCanc014Ind, CANC_014_IND_LEN);
    localCanc014IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkCanc014IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshCanc014Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshCanc014Ind() {
    return (getShort(beginCanc014Ind));
  }

  int localSlot015Counter = -1;

  public boolean isSlot015Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot015Counter != sharedCounter;
    localSlot015Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_015_LEN = 6;
  /** serialize this Slot015 */
  protected void serializeSlot015(char[] slot015) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot015, 0, getStringValue(), beginSlot015, SLOT_015_LEN);
    localSlot015Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot015Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot015 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot015() {
    return (substring(getStringValue(), beginSlot015, beginSlot015 + SLOT_015_LEN));
  }

  int localSlot015IndCounter = -1;

  public boolean isSlot015IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot015IndCounter != sharedCounter;
    localSlot015IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_015_IND_LEN = 2;
  /** serializeSlot015Ind */
  protected void serializeSlot015Ind(short slot015Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot015Ind, SLOT_015_IND_LEN), beginSlot015Ind, SLOT_015_IND_LEN);
    localSlot015IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot015IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot015Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot015Ind() {
    return (getShort(beginSlot015Ind));
  }

  int localSlot016Counter = -1;

  public boolean isSlot016Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot016Counter != sharedCounter;
    localSlot016Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_016_LEN = 6;
  /** serialize this Slot016 */
  protected void serializeSlot016(char[] slot016) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot016, 0, getStringValue(), beginSlot016, SLOT_016_LEN);
    localSlot016Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot016Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot016 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot016() {
    return (substring(getStringValue(), beginSlot016, beginSlot016 + SLOT_016_LEN));
  }

  int localSlot016IndCounter = -1;

  public boolean isSlot016IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot016IndCounter != sharedCounter;
    localSlot016IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_016_IND_LEN = 2;
  /** serializeSlot016Ind */
  protected void serializeSlot016Ind(short slot016Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot016Ind, SLOT_016_IND_LEN), beginSlot016Ind, SLOT_016_IND_LEN);
    localSlot016IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot016IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot016Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot016Ind() {
    return (getShort(beginSlot016Ind));
  }

  int localSlot017Counter = -1;

  public boolean isSlot017Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot017Counter != sharedCounter;
    localSlot017Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_017_LEN = 6;
  /** serialize this Slot017 */
  protected void serializeSlot017(char[] slot017) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot017, 0, getStringValue(), beginSlot017, SLOT_017_LEN);
    localSlot017Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot017Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot017 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot017() {
    return (substring(getStringValue(), beginSlot017, beginSlot017 + SLOT_017_LEN));
  }

  int localSlot017IndCounter = -1;

  public boolean isSlot017IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot017IndCounter != sharedCounter;
    localSlot017IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_017_IND_LEN = 2;
  /** serializeSlot017Ind */
  protected void serializeSlot017Ind(short slot017Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot017Ind, SLOT_017_IND_LEN), beginSlot017Ind, SLOT_017_IND_LEN);
    localSlot017IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot017IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot017Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot017Ind() {
    return (getShort(beginSlot017Ind));
  }

  int localSlot018Counter = -1;

  public boolean isSlot018Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot018Counter != sharedCounter;
    localSlot018Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_018_LEN = 6;
  /** serialize this Slot018 */
  protected void serializeSlot018(char[] slot018) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot018, 0, getStringValue(), beginSlot018, SLOT_018_LEN);
    localSlot018Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot018Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot018 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot018() {
    return (substring(getStringValue(), beginSlot018, beginSlot018 + SLOT_018_LEN));
  }

  int localSlot018IndCounter = -1;

  public boolean isSlot018IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot018IndCounter != sharedCounter;
    localSlot018IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_018_IND_LEN = 2;
  /** serializeSlot018Ind */
  protected void serializeSlot018Ind(short slot018Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot018Ind, SLOT_018_IND_LEN), beginSlot018Ind, SLOT_018_IND_LEN);
    localSlot018IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot018IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot018Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot018Ind() {
    return (getShort(beginSlot018Ind));
  }

  int localSlot019Counter = -1;

  public boolean isSlot019Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot019Counter != sharedCounter;
    localSlot019Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_019_LEN = 6;
  /** serialize this Slot019 */
  protected void serializeSlot019(char[] slot019) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot019, 0, getStringValue(), beginSlot019, SLOT_019_LEN);
    localSlot019Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot019Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot019 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot019() {
    return (substring(getStringValue(), beginSlot019, beginSlot019 + SLOT_019_LEN));
  }

  int localSlot019IndCounter = -1;

  public boolean isSlot019IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot019IndCounter != sharedCounter;
    localSlot019IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_019_IND_LEN = 2;
  /** serializeSlot019Ind */
  protected void serializeSlot019Ind(short slot019Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot019Ind, SLOT_019_IND_LEN), beginSlot019Ind, SLOT_019_IND_LEN);
    localSlot019IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot019IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot019Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot019Ind() {
    return (getShort(beginSlot019Ind));
  }

  int localSlot020Counter = -1;

  public boolean isSlot020Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot020Counter != sharedCounter;
    localSlot020Counter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_020_LEN = 6;
  /** serialize this Slot020 */
  protected void serializeSlot020(char[] slot020) {
    shareString.getSerializedField().incrementCounter();
    arraycopy(slot020, 0, getStringValue(), beginSlot020, SLOT_020_LEN);
    localSlot020Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected char[] checkSlot020Constraints(char[] value) {
    return super.checkConstraints(value, 6, false, false);
  }
  /**
   * refreshSlot020 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public char[] refreshSlot020() {
    return (substring(getStringValue(), beginSlot020, beginSlot020 + SLOT_020_LEN));
  }

  int localSlot020IndCounter = -1;

  public boolean isSlot020IndModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localSlot020IndCounter != sharedCounter;
    localSlot020IndCounter = sharedCounter;
    return hasModified;
  }

  protected static final int SLOT_020_IND_LEN = 2;
  /** serializeSlot020Ind */
  protected void serializeSlot020Ind(short slot020Ind) {
    replaceValue( //  save the value as string
        getBinaryString(slot020Ind, SLOT_020_IND_LEN), beginSlot020Ind, SLOT_020_IND_LEN);
    localSlot020IndCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkSlot020IndMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshSlot020Ind is used to refresh the latest value of a variable from the Serialized String
   * the most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshSlot020Ind() {
    return (getShort(beginSlot020Ind));
  }
}
