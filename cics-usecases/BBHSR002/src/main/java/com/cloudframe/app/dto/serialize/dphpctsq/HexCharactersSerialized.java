package com.cloudframe.app.dto.serialize.dphpctsq;

/**
 * The class HexCharactersSerialized is used to define offsets in order to serialize in a fixed
 * String
 *
 * @author CloudFrame Inc. created on 2025-03-25 at 08:30. using version 5.0.0.231
 */
import com.cloudframe.app.data.Field;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class HexCharactersSerialized extends Field {

  protected Logger logger = LoggerFactory.getLogger(HexCharactersSerialized.class);
  /*  Length of the field, if serialized as a String */
  protected static final int HEX_CHARACTERS_LENGTH = 512;
  /*  offset of each of Child Fields when serialized as a String */
  protected int beginHex00;
  protected int beginHex01;
  protected int beginHex02;
  protected int beginHex03;
  protected int beginHex04;
  protected int beginHex05;
  protected int beginHex06;
  protected int beginHex07;
  protected int beginHex08;
  protected int beginHex09;
  protected int beginHex0a;
  protected int beginHex0b;
  protected int beginHex0c;
  protected int beginHex0d;
  protected int beginHex0e;
  protected int beginHex0f;
  protected int beginHex10;
  protected int beginHex11;
  protected int beginHex12;
  protected int beginHex13;
  protected int beginHex14;
  protected int beginHex15;
  protected int beginHex16;
  protected int beginHex17;
  protected int beginHex18;
  protected int beginHex19;
  protected int beginHex1a;
  protected int beginHex1b;
  protected int beginHex1c;
  protected int beginHex1d;
  protected int beginHex1e;
  protected int beginHex1f;
  protected int beginHex20;
  protected int beginHex21;
  protected int beginHex22;
  protected int beginHex23;
  protected int beginHex24;
  protected int beginHex25;
  protected int beginHex26;
  protected int beginHex27;
  protected int beginHex28;
  protected int beginHex29;
  protected int beginHex2a;
  protected int beginHex2b;
  protected int beginHex2c;
  protected int beginHex2d;
  protected int beginHex2e;
  protected int beginHex2f;
  protected int beginHex30;
  protected int beginHex31;
  protected int beginHex32;
  protected int beginHex33;
  protected int beginHex34;
  protected int beginHex35;
  protected int beginHex36;
  protected int beginHex37;
  protected int beginHex38;
  protected int beginHex39;
  protected int beginHex3a;
  protected int beginHex3b;
  protected int beginHex3c;
  protected int beginHex3d;
  protected int beginHex3e;
  protected int beginHex3f;
  protected int beginHex40;
  protected int beginHex41;
  protected int beginHex42;
  protected int beginHex43;
  protected int beginHex44;
  protected int beginHex45;
  protected int beginHex46;
  protected int beginHex47;
  protected int beginHex48;
  protected int beginHex49;
  protected int beginHex4a;
  protected int beginHex4b;
  protected int beginHex4c;
  protected int beginHex4d;
  protected int beginHex4e;
  protected int beginHex4f;
  protected int beginHex50;
  protected int beginHex51;
  protected int beginHex52;
  protected int beginHex53;
  protected int beginHex54;
  protected int beginHex55;
  protected int beginHex56;
  protected int beginHex57;
  protected int beginHex58;
  protected int beginHex59;
  protected int beginHex5a;
  protected int beginHex5b;
  protected int beginHex5c;
  protected int beginHex5d;
  protected int beginHex5e;
  protected int beginHex5f;
  protected int beginHex60;
  protected int beginHex61;
  protected int beginHex62;
  protected int beginHex63;
  protected int beginHex64;
  protected int beginHex65;
  protected int beginHex66;
  protected int beginHex67;
  protected int beginHex68;
  protected int beginHex69;
  protected int beginHex6a;
  protected int beginHex6b;
  protected int beginHex6c;
  protected int beginHex6d;
  protected int beginHex6e;
  protected int beginHex6f;
  protected int beginHex70;
  protected int beginHex71;
  protected int beginHex72;
  protected int beginHex73;
  protected int beginHex74;
  protected int beginHex75;
  protected int beginHex76;
  protected int beginHex77;
  protected int beginHex78;
  protected int beginHex79;
  protected int beginHex7a;
  protected int beginHex7b;
  protected int beginHex7c;
  protected int beginHex7d;
  protected int beginHex7e;
  protected int beginHex7f;
  protected int beginHex80;
  protected int beginHex81;
  protected int beginHex82;
  protected int beginHex83;
  protected int beginHex84;
  protected int beginHex85;
  protected int beginHex86;
  protected int beginHex87;
  protected int beginHex88;
  protected int beginHex89;
  protected int beginHex8a;
  protected int beginHex8b;
  protected int beginHex8c;
  protected int beginHex8d;
  protected int beginHex8e;
  protected int beginHex8f;
  protected int beginHex90;
  protected int beginHex91;
  protected int beginHex92;
  protected int beginHex93;
  protected int beginHex94;
  protected int beginHex95;
  protected int beginHex96;
  protected int beginHex97;
  protected int beginHex98;
  protected int beginHex99;
  protected int beginHex9a;
  protected int beginHex9b;
  protected int beginHex9c;
  protected int beginHex9d;
  protected int beginHex9e;
  protected int beginHex9f;
  protected int beginHexA0;
  protected int beginHexA1;
  protected int beginHexA2;
  protected int beginHexA3;
  protected int beginHexA4;
  protected int beginHexA5;
  protected int beginHexA6;
  protected int beginHexA7;
  protected int beginHexA8;
  protected int beginHexA9;
  protected int beginHexAa;
  protected int beginHexAb;
  protected int beginHexAc;
  protected int beginHexAd;
  protected int beginHexAe;
  protected int beginHexAf;
  protected int beginHexB0;
  protected int beginHexB1;
  protected int beginHexB2;
  protected int beginHexB3;
  protected int beginHexB4;
  protected int beginHexB5;
  protected int beginHexB6;
  protected int beginHexB7;
  protected int beginHexB8;
  protected int beginHexB9;
  protected int beginHexBa;
  protected int beginHexBb;
  protected int beginHexBc;
  protected int beginHexBd;
  protected int beginHexBe;
  protected int beginHexBf;
  protected int beginHexC0;
  protected int beginHexC1;
  protected int beginHexC2;
  protected int beginHexC3;
  protected int beginHexC4;
  protected int beginHexC5;
  protected int beginHexC6;
  protected int beginHexC7;
  protected int beginHexC8;
  protected int beginHexC9;
  protected int beginHexCa;
  protected int beginHexCb;
  protected int beginHexCc;
  protected int beginHexCd;
  protected int beginHexCe;
  protected int beginHexCf;
  protected int beginHexD0;
  protected int beginHexD1;
  protected int beginHexD2;
  protected int beginHexD3;
  protected int beginHexD4;
  protected int beginHexD5;
  protected int beginHexD6;
  protected int beginHexD7;
  protected int beginHexD8;
  protected int beginHexD9;
  protected int beginHexDa;
  protected int beginHexDb;
  protected int beginHexDc;
  protected int beginHexDd;
  protected int beginHexDe;
  protected int beginHexDf;
  protected int beginHexE0;
  protected int beginHexE1;
  protected int beginHexE2;
  protected int beginHexE3;
  protected int beginHexE4;
  protected int beginHexE5;
  protected int beginHexE6;
  protected int beginHexE7;
  protected int beginHexE8;
  protected int beginHexE9;
  protected int beginHexEa;
  protected int beginHexEb;
  protected int beginHexEc;
  protected int beginHexEd;
  protected int beginHexEe;
  protected int beginHexEf;
  protected int beginHexF0;
  protected int beginHexF1;
  protected int beginHexF2;
  protected int beginHexF3;
  protected int beginHexF4;
  protected int beginHexF5;
  protected int beginHexF6;
  protected int beginHexF7;
  protected int beginHexF8;
  protected int beginHexF9;
  protected int beginHexFa;
  protected int beginHexFb;
  protected int beginHexFc;
  protected int beginHexFd;
  protected int beginHexFe;
  protected int beginHexFf;

  /** Constructor for HexCharactersSerialized */
  public HexCharactersSerialized() {
    // TO-DO auto generated code
  }

  /**
   * Constructor for HexCharactersSerialized. sets the parent value to the parent
   *
   * @param parent
   * @param begin
   */
  public HexCharactersSerialized(Field parent, int begin) {
    setParent(parent, begin);
  }

  /**
   * sets parent for this HexCharactersSerialized to the parent
   *
   * @param parent
   */
  @Override
  public void setParent(Field parent) {
    setParent(parent, 0); // serialize this field at offset 0 by default
  }

  /**
   * sets parent for this HexCharactersSerialized to the parent and set the serialize offset to
   * parameter begin
   *
   * @param parent
   * @param begin - offset used when serializing this object to a String
   */
  public void setParent(Field parent, int begin) {
    super.setParent(parent);
    init(begin); // serialize this field at offset 0 by default
  }
  /** initializes the field in HexCharactersSerialized */
  @Override
  protected void init(int begin) {
    setStartOffset(begin);
    setLength(HEX_CHARACTERS_LENGTH);
    /*  set the offset/position of each field when this object is serialized as String */
    beginHex00 = getStartOffset() + 0; // set offset for serialization

    beginHex01 = getStartOffset() + 2; // set offset for serialization

    beginHex02 = getStartOffset() + 4; // set offset for serialization

    beginHex03 = getStartOffset() + 6; // set offset for serialization

    beginHex04 = getStartOffset() + 8; // set offset for serialization

    beginHex05 = getStartOffset() + 10; // set offset for serialization

    beginHex06 = getStartOffset() + 12; // set offset for serialization

    beginHex07 = getStartOffset() + 14; // set offset for serialization

    beginHex08 = getStartOffset() + 16; // set offset for serialization

    beginHex09 = getStartOffset() + 18; // set offset for serialization

    beginHex0a = getStartOffset() + 20; // set offset for serialization

    beginHex0b = getStartOffset() + 22; // set offset for serialization

    beginHex0c = getStartOffset() + 24; // set offset for serialization

    beginHex0d = getStartOffset() + 26; // set offset for serialization

    beginHex0e = getStartOffset() + 28; // set offset for serialization

    beginHex0f = getStartOffset() + 30; // set offset for serialization

    beginHex10 = getStartOffset() + 32; // set offset for serialization

    beginHex11 = getStartOffset() + 34; // set offset for serialization

    beginHex12 = getStartOffset() + 36; // set offset for serialization

    beginHex13 = getStartOffset() + 38; // set offset for serialization

    beginHex14 = getStartOffset() + 40; // set offset for serialization

    beginHex15 = getStartOffset() + 42; // set offset for serialization

    beginHex16 = getStartOffset() + 44; // set offset for serialization

    beginHex17 = getStartOffset() + 46; // set offset for serialization

    beginHex18 = getStartOffset() + 48; // set offset for serialization

    beginHex19 = getStartOffset() + 50; // set offset for serialization

    beginHex1a = getStartOffset() + 52; // set offset for serialization

    beginHex1b = getStartOffset() + 54; // set offset for serialization

    beginHex1c = getStartOffset() + 56; // set offset for serialization

    beginHex1d = getStartOffset() + 58; // set offset for serialization

    beginHex1e = getStartOffset() + 60; // set offset for serialization

    beginHex1f = getStartOffset() + 62; // set offset for serialization

    beginHex20 = getStartOffset() + 64; // set offset for serialization

    beginHex21 = getStartOffset() + 66; // set offset for serialization

    beginHex22 = getStartOffset() + 68; // set offset for serialization

    beginHex23 = getStartOffset() + 70; // set offset for serialization

    beginHex24 = getStartOffset() + 72; // set offset for serialization

    beginHex25 = getStartOffset() + 74; // set offset for serialization

    beginHex26 = getStartOffset() + 76; // set offset for serialization

    beginHex27 = getStartOffset() + 78; // set offset for serialization

    beginHex28 = getStartOffset() + 80; // set offset for serialization

    beginHex29 = getStartOffset() + 82; // set offset for serialization

    beginHex2a = getStartOffset() + 84; // set offset for serialization

    beginHex2b = getStartOffset() + 86; // set offset for serialization

    beginHex2c = getStartOffset() + 88; // set offset for serialization

    beginHex2d = getStartOffset() + 90; // set offset for serialization

    beginHex2e = getStartOffset() + 92; // set offset for serialization

    beginHex2f = getStartOffset() + 94; // set offset for serialization

    beginHex30 = getStartOffset() + 96; // set offset for serialization

    beginHex31 = getStartOffset() + 98; // set offset for serialization

    beginHex32 = getStartOffset() + 100; // set offset for serialization

    beginHex33 = getStartOffset() + 102; // set offset for serialization

    beginHex34 = getStartOffset() + 104; // set offset for serialization

    beginHex35 = getStartOffset() + 106; // set offset for serialization

    beginHex36 = getStartOffset() + 108; // set offset for serialization

    beginHex37 = getStartOffset() + 110; // set offset for serialization

    beginHex38 = getStartOffset() + 112; // set offset for serialization

    beginHex39 = getStartOffset() + 114; // set offset for serialization

    beginHex3a = getStartOffset() + 116; // set offset for serialization

    beginHex3b = getStartOffset() + 118; // set offset for serialization

    beginHex3c = getStartOffset() + 120; // set offset for serialization

    beginHex3d = getStartOffset() + 122; // set offset for serialization

    beginHex3e = getStartOffset() + 124; // set offset for serialization

    beginHex3f = getStartOffset() + 126; // set offset for serialization

    beginHex40 = getStartOffset() + 128; // set offset for serialization

    beginHex41 = getStartOffset() + 130; // set offset for serialization

    beginHex42 = getStartOffset() + 132; // set offset for serialization

    beginHex43 = getStartOffset() + 134; // set offset for serialization

    beginHex44 = getStartOffset() + 136; // set offset for serialization

    beginHex45 = getStartOffset() + 138; // set offset for serialization

    beginHex46 = getStartOffset() + 140; // set offset for serialization

    beginHex47 = getStartOffset() + 142; // set offset for serialization

    beginHex48 = getStartOffset() + 144; // set offset for serialization

    beginHex49 = getStartOffset() + 146; // set offset for serialization

    beginHex4a = getStartOffset() + 148; // set offset for serialization

    beginHex4b = getStartOffset() + 150; // set offset for serialization

    beginHex4c = getStartOffset() + 152; // set offset for serialization

    beginHex4d = getStartOffset() + 154; // set offset for serialization

    beginHex4e = getStartOffset() + 156; // set offset for serialization

    beginHex4f = getStartOffset() + 158; // set offset for serialization

    beginHex50 = getStartOffset() + 160; // set offset for serialization

    beginHex51 = getStartOffset() + 162; // set offset for serialization

    beginHex52 = getStartOffset() + 164; // set offset for serialization

    beginHex53 = getStartOffset() + 166; // set offset for serialization

    beginHex54 = getStartOffset() + 168; // set offset for serialization

    beginHex55 = getStartOffset() + 170; // set offset for serialization

    beginHex56 = getStartOffset() + 172; // set offset for serialization

    beginHex57 = getStartOffset() + 174; // set offset for serialization

    beginHex58 = getStartOffset() + 176; // set offset for serialization

    beginHex59 = getStartOffset() + 178; // set offset for serialization

    beginHex5a = getStartOffset() + 180; // set offset for serialization

    beginHex5b = getStartOffset() + 182; // set offset for serialization

    beginHex5c = getStartOffset() + 184; // set offset for serialization

    beginHex5d = getStartOffset() + 186; // set offset for serialization

    beginHex5e = getStartOffset() + 188; // set offset for serialization

    beginHex5f = getStartOffset() + 190; // set offset for serialization

    beginHex60 = getStartOffset() + 192; // set offset for serialization

    beginHex61 = getStartOffset() + 194; // set offset for serialization

    beginHex62 = getStartOffset() + 196; // set offset for serialization

    beginHex63 = getStartOffset() + 198; // set offset for serialization

    beginHex64 = getStartOffset() + 200; // set offset for serialization

    beginHex65 = getStartOffset() + 202; // set offset for serialization

    beginHex66 = getStartOffset() + 204; // set offset for serialization

    beginHex67 = getStartOffset() + 206; // set offset for serialization

    beginHex68 = getStartOffset() + 208; // set offset for serialization

    beginHex69 = getStartOffset() + 210; // set offset for serialization

    beginHex6a = getStartOffset() + 212; // set offset for serialization

    beginHex6b = getStartOffset() + 214; // set offset for serialization

    beginHex6c = getStartOffset() + 216; // set offset for serialization

    beginHex6d = getStartOffset() + 218; // set offset for serialization

    beginHex6e = getStartOffset() + 220; // set offset for serialization

    beginHex6f = getStartOffset() + 222; // set offset for serialization

    beginHex70 = getStartOffset() + 224; // set offset for serialization

    beginHex71 = getStartOffset() + 226; // set offset for serialization

    beginHex72 = getStartOffset() + 228; // set offset for serialization

    beginHex73 = getStartOffset() + 230; // set offset for serialization

    beginHex74 = getStartOffset() + 232; // set offset for serialization

    beginHex75 = getStartOffset() + 234; // set offset for serialization

    beginHex76 = getStartOffset() + 236; // set offset for serialization

    beginHex77 = getStartOffset() + 238; // set offset for serialization

    beginHex78 = getStartOffset() + 240; // set offset for serialization

    beginHex79 = getStartOffset() + 242; // set offset for serialization

    beginHex7a = getStartOffset() + 244; // set offset for serialization

    beginHex7b = getStartOffset() + 246; // set offset for serialization

    beginHex7c = getStartOffset() + 248; // set offset for serialization

    beginHex7d = getStartOffset() + 250; // set offset for serialization

    beginHex7e = getStartOffset() + 252; // set offset for serialization

    beginHex7f = getStartOffset() + 254; // set offset for serialization

    beginHex80 = getStartOffset() + 256; // set offset for serialization

    beginHex81 = getStartOffset() + 258; // set offset for serialization

    beginHex82 = getStartOffset() + 260; // set offset for serialization

    beginHex83 = getStartOffset() + 262; // set offset for serialization

    beginHex84 = getStartOffset() + 264; // set offset for serialization

    beginHex85 = getStartOffset() + 266; // set offset for serialization

    beginHex86 = getStartOffset() + 268; // set offset for serialization

    beginHex87 = getStartOffset() + 270; // set offset for serialization

    beginHex88 = getStartOffset() + 272; // set offset for serialization

    beginHex89 = getStartOffset() + 274; // set offset for serialization

    beginHex8a = getStartOffset() + 276; // set offset for serialization

    beginHex8b = getStartOffset() + 278; // set offset for serialization

    beginHex8c = getStartOffset() + 280; // set offset for serialization

    beginHex8d = getStartOffset() + 282; // set offset for serialization

    beginHex8e = getStartOffset() + 284; // set offset for serialization

    beginHex8f = getStartOffset() + 286; // set offset for serialization

    beginHex90 = getStartOffset() + 288; // set offset for serialization

    beginHex91 = getStartOffset() + 290; // set offset for serialization

    beginHex92 = getStartOffset() + 292; // set offset for serialization

    beginHex93 = getStartOffset() + 294; // set offset for serialization

    beginHex94 = getStartOffset() + 296; // set offset for serialization

    beginHex95 = getStartOffset() + 298; // set offset for serialization

    beginHex96 = getStartOffset() + 300; // set offset for serialization

    beginHex97 = getStartOffset() + 302; // set offset for serialization

    beginHex98 = getStartOffset() + 304; // set offset for serialization

    beginHex99 = getStartOffset() + 306; // set offset for serialization

    beginHex9a = getStartOffset() + 308; // set offset for serialization

    beginHex9b = getStartOffset() + 310; // set offset for serialization

    beginHex9c = getStartOffset() + 312; // set offset for serialization

    beginHex9d = getStartOffset() + 314; // set offset for serialization

    beginHex9e = getStartOffset() + 316; // set offset for serialization

    beginHex9f = getStartOffset() + 318; // set offset for serialization

    beginHexA0 = getStartOffset() + 320; // set offset for serialization

    beginHexA1 = getStartOffset() + 322; // set offset for serialization

    beginHexA2 = getStartOffset() + 324; // set offset for serialization

    beginHexA3 = getStartOffset() + 326; // set offset for serialization

    beginHexA4 = getStartOffset() + 328; // set offset for serialization

    beginHexA5 = getStartOffset() + 330; // set offset for serialization

    beginHexA6 = getStartOffset() + 332; // set offset for serialization

    beginHexA7 = getStartOffset() + 334; // set offset for serialization

    beginHexA8 = getStartOffset() + 336; // set offset for serialization

    beginHexA9 = getStartOffset() + 338; // set offset for serialization

    beginHexAa = getStartOffset() + 340; // set offset for serialization

    beginHexAb = getStartOffset() + 342; // set offset for serialization

    beginHexAc = getStartOffset() + 344; // set offset for serialization

    beginHexAd = getStartOffset() + 346; // set offset for serialization

    beginHexAe = getStartOffset() + 348; // set offset for serialization

    beginHexAf = getStartOffset() + 350; // set offset for serialization

    beginHexB0 = getStartOffset() + 352; // set offset for serialization

    beginHexB1 = getStartOffset() + 354; // set offset for serialization

    beginHexB2 = getStartOffset() + 356; // set offset for serialization

    beginHexB3 = getStartOffset() + 358; // set offset for serialization

    beginHexB4 = getStartOffset() + 360; // set offset for serialization

    beginHexB5 = getStartOffset() + 362; // set offset for serialization

    beginHexB6 = getStartOffset() + 364; // set offset for serialization

    beginHexB7 = getStartOffset() + 366; // set offset for serialization

    beginHexB8 = getStartOffset() + 368; // set offset for serialization

    beginHexB9 = getStartOffset() + 370; // set offset for serialization

    beginHexBa = getStartOffset() + 372; // set offset for serialization

    beginHexBb = getStartOffset() + 374; // set offset for serialization

    beginHexBc = getStartOffset() + 376; // set offset for serialization

    beginHexBd = getStartOffset() + 378; // set offset for serialization

    beginHexBe = getStartOffset() + 380; // set offset for serialization

    beginHexBf = getStartOffset() + 382; // set offset for serialization

    beginHexC0 = getStartOffset() + 384; // set offset for serialization

    beginHexC1 = getStartOffset() + 386; // set offset for serialization

    beginHexC2 = getStartOffset() + 388; // set offset for serialization

    beginHexC3 = getStartOffset() + 390; // set offset for serialization

    beginHexC4 = getStartOffset() + 392; // set offset for serialization

    beginHexC5 = getStartOffset() + 394; // set offset for serialization

    beginHexC6 = getStartOffset() + 396; // set offset for serialization

    beginHexC7 = getStartOffset() + 398; // set offset for serialization

    beginHexC8 = getStartOffset() + 400; // set offset for serialization

    beginHexC9 = getStartOffset() + 402; // set offset for serialization

    beginHexCa = getStartOffset() + 404; // set offset for serialization

    beginHexCb = getStartOffset() + 406; // set offset for serialization

    beginHexCc = getStartOffset() + 408; // set offset for serialization

    beginHexCd = getStartOffset() + 410; // set offset for serialization

    beginHexCe = getStartOffset() + 412; // set offset for serialization

    beginHexCf = getStartOffset() + 414; // set offset for serialization

    beginHexD0 = getStartOffset() + 416; // set offset for serialization

    beginHexD1 = getStartOffset() + 418; // set offset for serialization

    beginHexD2 = getStartOffset() + 420; // set offset for serialization

    beginHexD3 = getStartOffset() + 422; // set offset for serialization

    beginHexD4 = getStartOffset() + 424; // set offset for serialization

    beginHexD5 = getStartOffset() + 426; // set offset for serialization

    beginHexD6 = getStartOffset() + 428; // set offset for serialization

    beginHexD7 = getStartOffset() + 430; // set offset for serialization

    beginHexD8 = getStartOffset() + 432; // set offset for serialization

    beginHexD9 = getStartOffset() + 434; // set offset for serialization

    beginHexDa = getStartOffset() + 436; // set offset for serialization

    beginHexDb = getStartOffset() + 438; // set offset for serialization

    beginHexDc = getStartOffset() + 440; // set offset for serialization

    beginHexDd = getStartOffset() + 442; // set offset for serialization

    beginHexDe = getStartOffset() + 444; // set offset for serialization

    beginHexDf = getStartOffset() + 446; // set offset for serialization

    beginHexE0 = getStartOffset() + 448; // set offset for serialization

    beginHexE1 = getStartOffset() + 450; // set offset for serialization

    beginHexE2 = getStartOffset() + 452; // set offset for serialization

    beginHexE3 = getStartOffset() + 454; // set offset for serialization

    beginHexE4 = getStartOffset() + 456; // set offset for serialization

    beginHexE5 = getStartOffset() + 458; // set offset for serialization

    beginHexE6 = getStartOffset() + 460; // set offset for serialization

    beginHexE7 = getStartOffset() + 462; // set offset for serialization

    beginHexE8 = getStartOffset() + 464; // set offset for serialization

    beginHexE9 = getStartOffset() + 466; // set offset for serialization

    beginHexEa = getStartOffset() + 468; // set offset for serialization

    beginHexEb = getStartOffset() + 470; // set offset for serialization

    beginHexEc = getStartOffset() + 472; // set offset for serialization

    beginHexEd = getStartOffset() + 474; // set offset for serialization

    beginHexEe = getStartOffset() + 476; // set offset for serialization

    beginHexEf = getStartOffset() + 478; // set offset for serialization

    beginHexF0 = getStartOffset() + 480; // set offset for serialization

    beginHexF1 = getStartOffset() + 482; // set offset for serialization

    beginHexF2 = getStartOffset() + 484; // set offset for serialization

    beginHexF3 = getStartOffset() + 486; // set offset for serialization

    beginHexF4 = getStartOffset() + 488; // set offset for serialization

    beginHexF5 = getStartOffset() + 490; // set offset for serialization

    beginHexF6 = getStartOffset() + 492; // set offset for serialization

    beginHexF7 = getStartOffset() + 494; // set offset for serialization

    beginHexF8 = getStartOffset() + 496; // set offset for serialization

    beginHexF9 = getStartOffset() + 498; // set offset for serialization

    beginHexFa = getStartOffset() + 500; // set offset for serialization

    beginHexFb = getStartOffset() + 502; // set offset for serialization

    beginHexFc = getStartOffset() + 504; // set offset for serialization

    beginHexFd = getStartOffset() + 506; // set offset for serialization

    beginHexFe = getStartOffset() + 508; // set offset for serialization

    beginHexFf = getStartOffset() + 510; // set offset for serialization

    /*  end of offset */
  }

  int localHex00Counter = -1;

  public boolean isHex00Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex00Counter != sharedCounter;
    localHex00Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_00_LEN = 2;
  /** serializeHex00 */
  protected void serializeHex00(short hex00) {
    replaceValue( //  save the value as string
        getBinaryString(hex00, HEX_00_LEN), beginHex00, HEX_00_LEN);
    localHex00Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex00MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex00 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex00() {
    return (getShort(beginHex00));
  }

  int localHex01Counter = -1;

  public boolean isHex01Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex01Counter != sharedCounter;
    localHex01Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_01_LEN = 2;
  /** serializeHex01 */
  protected void serializeHex01(short hex01) {
    replaceValue( //  save the value as string
        getBinaryString(hex01, HEX_01_LEN), beginHex01, HEX_01_LEN);
    localHex01Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex01MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex01 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex01() {
    return (getShort(beginHex01));
  }

  int localHex02Counter = -1;

  public boolean isHex02Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex02Counter != sharedCounter;
    localHex02Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_02_LEN = 2;
  /** serializeHex02 */
  protected void serializeHex02(short hex02) {
    replaceValue( //  save the value as string
        getBinaryString(hex02, HEX_02_LEN), beginHex02, HEX_02_LEN);
    localHex02Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex02MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex02 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex02() {
    return (getShort(beginHex02));
  }

  int localHex03Counter = -1;

  public boolean isHex03Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex03Counter != sharedCounter;
    localHex03Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_03_LEN = 2;
  /** serializeHex03 */
  protected void serializeHex03(short hex03) {
    replaceValue( //  save the value as string
        getBinaryString(hex03, HEX_03_LEN), beginHex03, HEX_03_LEN);
    localHex03Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex03MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex03 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex03() {
    return (getShort(beginHex03));
  }

  int localHex04Counter = -1;

  public boolean isHex04Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex04Counter != sharedCounter;
    localHex04Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_04_LEN = 2;
  /** serializeHex04 */
  protected void serializeHex04(short hex04) {
    replaceValue( //  save the value as string
        getBinaryString(hex04, HEX_04_LEN), beginHex04, HEX_04_LEN);
    localHex04Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex04MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex04 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex04() {
    return (getShort(beginHex04));
  }

  int localHex05Counter = -1;

  public boolean isHex05Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex05Counter != sharedCounter;
    localHex05Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_05_LEN = 2;
  /** serializeHex05 */
  protected void serializeHex05(short hex05) {
    replaceValue( //  save the value as string
        getBinaryString(hex05, HEX_05_LEN), beginHex05, HEX_05_LEN);
    localHex05Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex05MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex05 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex05() {
    return (getShort(beginHex05));
  }

  int localHex06Counter = -1;

  public boolean isHex06Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex06Counter != sharedCounter;
    localHex06Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_06_LEN = 2;
  /** serializeHex06 */
  protected void serializeHex06(short hex06) {
    replaceValue( //  save the value as string
        getBinaryString(hex06, HEX_06_LEN), beginHex06, HEX_06_LEN);
    localHex06Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex06MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex06 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex06() {
    return (getShort(beginHex06));
  }

  int localHex07Counter = -1;

  public boolean isHex07Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex07Counter != sharedCounter;
    localHex07Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_07_LEN = 2;
  /** serializeHex07 */
  protected void serializeHex07(short hex07) {
    replaceValue( //  save the value as string
        getBinaryString(hex07, HEX_07_LEN), beginHex07, HEX_07_LEN);
    localHex07Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex07MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex07 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex07() {
    return (getShort(beginHex07));
  }

  int localHex08Counter = -1;

  public boolean isHex08Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex08Counter != sharedCounter;
    localHex08Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_08_LEN = 2;
  /** serializeHex08 */
  protected void serializeHex08(short hex08) {
    replaceValue( //  save the value as string
        getBinaryString(hex08, HEX_08_LEN), beginHex08, HEX_08_LEN);
    localHex08Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex08MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex08 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex08() {
    return (getShort(beginHex08));
  }

  int localHex09Counter = -1;

  public boolean isHex09Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex09Counter != sharedCounter;
    localHex09Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_09_LEN = 2;
  /** serializeHex09 */
  protected void serializeHex09(short hex09) {
    replaceValue( //  save the value as string
        getBinaryString(hex09, HEX_09_LEN), beginHex09, HEX_09_LEN);
    localHex09Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex09MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex09 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex09() {
    return (getShort(beginHex09));
  }

  int localHex0aCounter = -1;

  public boolean isHex0aModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex0aCounter != sharedCounter;
    localHex0aCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_0A_LEN = 2;
  /** serializeHex0a */
  protected void serializeHex0a(short hex0a) {
    replaceValue( //  save the value as string
        getBinaryString(hex0a, HEX_0A_LEN), beginHex0a, HEX_0A_LEN);
    localHex0aCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex0aMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex0a is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex0a() {
    return (getShort(beginHex0a));
  }

  int localHex0bCounter = -1;

  public boolean isHex0bModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex0bCounter != sharedCounter;
    localHex0bCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_0B_LEN = 2;
  /** serializeHex0b */
  protected void serializeHex0b(short hex0b) {
    replaceValue( //  save the value as string
        getBinaryString(hex0b, HEX_0B_LEN), beginHex0b, HEX_0B_LEN);
    localHex0bCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex0bMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex0b is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex0b() {
    return (getShort(beginHex0b));
  }

  int localHex0cCounter = -1;

  public boolean isHex0cModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex0cCounter != sharedCounter;
    localHex0cCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_0C_LEN = 2;
  /** serializeHex0c */
  protected void serializeHex0c(short hex0c) {
    replaceValue( //  save the value as string
        getBinaryString(hex0c, HEX_0C_LEN), beginHex0c, HEX_0C_LEN);
    localHex0cCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex0cMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex0c is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex0c() {
    return (getShort(beginHex0c));
  }

  int localHex0dCounter = -1;

  public boolean isHex0dModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex0dCounter != sharedCounter;
    localHex0dCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_0D_LEN = 2;
  /** serializeHex0d */
  protected void serializeHex0d(short hex0d) {
    replaceValue( //  save the value as string
        getBinaryString(hex0d, HEX_0D_LEN), beginHex0d, HEX_0D_LEN);
    localHex0dCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex0dMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex0d is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex0d() {
    return (getShort(beginHex0d));
  }

  int localHex0eCounter = -1;

  public boolean isHex0eModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex0eCounter != sharedCounter;
    localHex0eCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_0E_LEN = 2;
  /** serializeHex0e */
  protected void serializeHex0e(short hex0e) {
    replaceValue( //  save the value as string
        getBinaryString(hex0e, HEX_0E_LEN), beginHex0e, HEX_0E_LEN);
    localHex0eCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex0eMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex0e is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex0e() {
    return (getShort(beginHex0e));
  }

  int localHex0fCounter = -1;

  public boolean isHex0fModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex0fCounter != sharedCounter;
    localHex0fCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_0F_LEN = 2;
  /** serializeHex0f */
  protected void serializeHex0f(short hex0f) {
    replaceValue( //  save the value as string
        getBinaryString(hex0f, HEX_0F_LEN), beginHex0f, HEX_0F_LEN);
    localHex0fCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex0fMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex0f is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex0f() {
    return (getShort(beginHex0f));
  }

  int localHex10Counter = -1;

  public boolean isHex10Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex10Counter != sharedCounter;
    localHex10Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_10_LEN = 2;
  /** serializeHex10 */
  protected void serializeHex10(short hex10) {
    replaceValue( //  save the value as string
        getBinaryString(hex10, HEX_10_LEN), beginHex10, HEX_10_LEN);
    localHex10Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex10MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex10 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex10() {
    return (getShort(beginHex10));
  }

  int localHex11Counter = -1;

  public boolean isHex11Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex11Counter != sharedCounter;
    localHex11Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_11_LEN = 2;
  /** serializeHex11 */
  protected void serializeHex11(short hex11) {
    replaceValue( //  save the value as string
        getBinaryString(hex11, HEX_11_LEN), beginHex11, HEX_11_LEN);
    localHex11Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex11MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex11 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex11() {
    return (getShort(beginHex11));
  }

  int localHex12Counter = -1;

  public boolean isHex12Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex12Counter != sharedCounter;
    localHex12Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_12_LEN = 2;
  /** serializeHex12 */
  protected void serializeHex12(short hex12) {
    replaceValue( //  save the value as string
        getBinaryString(hex12, HEX_12_LEN), beginHex12, HEX_12_LEN);
    localHex12Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex12MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex12 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex12() {
    return (getShort(beginHex12));
  }

  int localHex13Counter = -1;

  public boolean isHex13Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex13Counter != sharedCounter;
    localHex13Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_13_LEN = 2;
  /** serializeHex13 */
  protected void serializeHex13(short hex13) {
    replaceValue( //  save the value as string
        getBinaryString(hex13, HEX_13_LEN), beginHex13, HEX_13_LEN);
    localHex13Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex13MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex13 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex13() {
    return (getShort(beginHex13));
  }

  int localHex14Counter = -1;

  public boolean isHex14Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex14Counter != sharedCounter;
    localHex14Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_14_LEN = 2;
  /** serializeHex14 */
  protected void serializeHex14(short hex14) {
    replaceValue( //  save the value as string
        getBinaryString(hex14, HEX_14_LEN), beginHex14, HEX_14_LEN);
    localHex14Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex14MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex14 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex14() {
    return (getShort(beginHex14));
  }

  int localHex15Counter = -1;

  public boolean isHex15Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex15Counter != sharedCounter;
    localHex15Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_15_LEN = 2;
  /** serializeHex15 */
  protected void serializeHex15(short hex15) {
    replaceValue( //  save the value as string
        getBinaryString(hex15, HEX_15_LEN), beginHex15, HEX_15_LEN);
    localHex15Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex15MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex15 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex15() {
    return (getShort(beginHex15));
  }

  int localHex16Counter = -1;

  public boolean isHex16Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex16Counter != sharedCounter;
    localHex16Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_16_LEN = 2;
  /** serializeHex16 */
  protected void serializeHex16(short hex16) {
    replaceValue( //  save the value as string
        getBinaryString(hex16, HEX_16_LEN), beginHex16, HEX_16_LEN);
    localHex16Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex16MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex16 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex16() {
    return (getShort(beginHex16));
  }

  int localHex17Counter = -1;

  public boolean isHex17Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex17Counter != sharedCounter;
    localHex17Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_17_LEN = 2;
  /** serializeHex17 */
  protected void serializeHex17(short hex17) {
    replaceValue( //  save the value as string
        getBinaryString(hex17, HEX_17_LEN), beginHex17, HEX_17_LEN);
    localHex17Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex17MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex17 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex17() {
    return (getShort(beginHex17));
  }

  int localHex18Counter = -1;

  public boolean isHex18Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex18Counter != sharedCounter;
    localHex18Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_18_LEN = 2;
  /** serializeHex18 */
  protected void serializeHex18(short hex18) {
    replaceValue( //  save the value as string
        getBinaryString(hex18, HEX_18_LEN), beginHex18, HEX_18_LEN);
    localHex18Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex18MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex18 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex18() {
    return (getShort(beginHex18));
  }

  int localHex19Counter = -1;

  public boolean isHex19Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex19Counter != sharedCounter;
    localHex19Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_19_LEN = 2;
  /** serializeHex19 */
  protected void serializeHex19(short hex19) {
    replaceValue( //  save the value as string
        getBinaryString(hex19, HEX_19_LEN), beginHex19, HEX_19_LEN);
    localHex19Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex19MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex19 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex19() {
    return (getShort(beginHex19));
  }

  int localHex1aCounter = -1;

  public boolean isHex1aModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex1aCounter != sharedCounter;
    localHex1aCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_1A_LEN = 2;
  /** serializeHex1a */
  protected void serializeHex1a(short hex1a) {
    replaceValue( //  save the value as string
        getBinaryString(hex1a, HEX_1A_LEN), beginHex1a, HEX_1A_LEN);
    localHex1aCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex1aMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex1a is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex1a() {
    return (getShort(beginHex1a));
  }

  int localHex1bCounter = -1;

  public boolean isHex1bModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex1bCounter != sharedCounter;
    localHex1bCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_1B_LEN = 2;
  /** serializeHex1b */
  protected void serializeHex1b(short hex1b) {
    replaceValue( //  save the value as string
        getBinaryString(hex1b, HEX_1B_LEN), beginHex1b, HEX_1B_LEN);
    localHex1bCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex1bMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex1b is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex1b() {
    return (getShort(beginHex1b));
  }

  int localHex1cCounter = -1;

  public boolean isHex1cModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex1cCounter != sharedCounter;
    localHex1cCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_1C_LEN = 2;
  /** serializeHex1c */
  protected void serializeHex1c(short hex1c) {
    replaceValue( //  save the value as string
        getBinaryString(hex1c, HEX_1C_LEN), beginHex1c, HEX_1C_LEN);
    localHex1cCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex1cMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex1c is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex1c() {
    return (getShort(beginHex1c));
  }

  int localHex1dCounter = -1;

  public boolean isHex1dModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex1dCounter != sharedCounter;
    localHex1dCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_1D_LEN = 2;
  /** serializeHex1d */
  protected void serializeHex1d(short hex1d) {
    replaceValue( //  save the value as string
        getBinaryString(hex1d, HEX_1D_LEN), beginHex1d, HEX_1D_LEN);
    localHex1dCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex1dMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex1d is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex1d() {
    return (getShort(beginHex1d));
  }

  int localHex1eCounter = -1;

  public boolean isHex1eModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex1eCounter != sharedCounter;
    localHex1eCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_1E_LEN = 2;
  /** serializeHex1e */
  protected void serializeHex1e(short hex1e) {
    replaceValue( //  save the value as string
        getBinaryString(hex1e, HEX_1E_LEN), beginHex1e, HEX_1E_LEN);
    localHex1eCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex1eMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex1e is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex1e() {
    return (getShort(beginHex1e));
  }

  int localHex1fCounter = -1;

  public boolean isHex1fModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex1fCounter != sharedCounter;
    localHex1fCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_1F_LEN = 2;
  /** serializeHex1f */
  protected void serializeHex1f(short hex1f) {
    replaceValue( //  save the value as string
        getBinaryString(hex1f, HEX_1F_LEN), beginHex1f, HEX_1F_LEN);
    localHex1fCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex1fMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex1f is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex1f() {
    return (getShort(beginHex1f));
  }

  int localHex20Counter = -1;

  public boolean isHex20Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex20Counter != sharedCounter;
    localHex20Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_20_LEN = 2;
  /** serializeHex20 */
  protected void serializeHex20(short hex20) {
    replaceValue( //  save the value as string
        getBinaryString(hex20, HEX_20_LEN), beginHex20, HEX_20_LEN);
    localHex20Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex20MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex20 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex20() {
    return (getShort(beginHex20));
  }

  int localHex21Counter = -1;

  public boolean isHex21Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex21Counter != sharedCounter;
    localHex21Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_21_LEN = 2;
  /** serializeHex21 */
  protected void serializeHex21(short hex21) {
    replaceValue( //  save the value as string
        getBinaryString(hex21, HEX_21_LEN), beginHex21, HEX_21_LEN);
    localHex21Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex21MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex21 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex21() {
    return (getShort(beginHex21));
  }

  int localHex22Counter = -1;

  public boolean isHex22Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex22Counter != sharedCounter;
    localHex22Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_22_LEN = 2;
  /** serializeHex22 */
  protected void serializeHex22(short hex22) {
    replaceValue( //  save the value as string
        getBinaryString(hex22, HEX_22_LEN), beginHex22, HEX_22_LEN);
    localHex22Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex22MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex22 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex22() {
    return (getShort(beginHex22));
  }

  int localHex23Counter = -1;

  public boolean isHex23Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex23Counter != sharedCounter;
    localHex23Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_23_LEN = 2;
  /** serializeHex23 */
  protected void serializeHex23(short hex23) {
    replaceValue( //  save the value as string
        getBinaryString(hex23, HEX_23_LEN), beginHex23, HEX_23_LEN);
    localHex23Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex23MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex23 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex23() {
    return (getShort(beginHex23));
  }

  int localHex24Counter = -1;

  public boolean isHex24Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex24Counter != sharedCounter;
    localHex24Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_24_LEN = 2;
  /** serializeHex24 */
  protected void serializeHex24(short hex24) {
    replaceValue( //  save the value as string
        getBinaryString(hex24, HEX_24_LEN), beginHex24, HEX_24_LEN);
    localHex24Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex24MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex24 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex24() {
    return (getShort(beginHex24));
  }

  int localHex25Counter = -1;

  public boolean isHex25Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex25Counter != sharedCounter;
    localHex25Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_25_LEN = 2;
  /** serializeHex25 */
  protected void serializeHex25(short hex25) {
    replaceValue( //  save the value as string
        getBinaryString(hex25, HEX_25_LEN), beginHex25, HEX_25_LEN);
    localHex25Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex25MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex25 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex25() {
    return (getShort(beginHex25));
  }

  int localHex26Counter = -1;

  public boolean isHex26Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex26Counter != sharedCounter;
    localHex26Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_26_LEN = 2;
  /** serializeHex26 */
  protected void serializeHex26(short hex26) {
    replaceValue( //  save the value as string
        getBinaryString(hex26, HEX_26_LEN), beginHex26, HEX_26_LEN);
    localHex26Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex26MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex26 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex26() {
    return (getShort(beginHex26));
  }

  int localHex27Counter = -1;

  public boolean isHex27Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex27Counter != sharedCounter;
    localHex27Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_27_LEN = 2;
  /** serializeHex27 */
  protected void serializeHex27(short hex27) {
    replaceValue( //  save the value as string
        getBinaryString(hex27, HEX_27_LEN), beginHex27, HEX_27_LEN);
    localHex27Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex27MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex27 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex27() {
    return (getShort(beginHex27));
  }

  int localHex28Counter = -1;

  public boolean isHex28Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex28Counter != sharedCounter;
    localHex28Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_28_LEN = 2;
  /** serializeHex28 */
  protected void serializeHex28(short hex28) {
    replaceValue( //  save the value as string
        getBinaryString(hex28, HEX_28_LEN), beginHex28, HEX_28_LEN);
    localHex28Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex28MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex28 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex28() {
    return (getShort(beginHex28));
  }

  int localHex29Counter = -1;

  public boolean isHex29Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex29Counter != sharedCounter;
    localHex29Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_29_LEN = 2;
  /** serializeHex29 */
  protected void serializeHex29(short hex29) {
    replaceValue( //  save the value as string
        getBinaryString(hex29, HEX_29_LEN), beginHex29, HEX_29_LEN);
    localHex29Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex29MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex29 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex29() {
    return (getShort(beginHex29));
  }

  int localHex2aCounter = -1;

  public boolean isHex2aModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex2aCounter != sharedCounter;
    localHex2aCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_2A_LEN = 2;
  /** serializeHex2a */
  protected void serializeHex2a(short hex2a) {
    replaceValue( //  save the value as string
        getBinaryString(hex2a, HEX_2A_LEN), beginHex2a, HEX_2A_LEN);
    localHex2aCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex2aMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex2a is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex2a() {
    return (getShort(beginHex2a));
  }

  int localHex2bCounter = -1;

  public boolean isHex2bModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex2bCounter != sharedCounter;
    localHex2bCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_2B_LEN = 2;
  /** serializeHex2b */
  protected void serializeHex2b(short hex2b) {
    replaceValue( //  save the value as string
        getBinaryString(hex2b, HEX_2B_LEN), beginHex2b, HEX_2B_LEN);
    localHex2bCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex2bMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex2b is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex2b() {
    return (getShort(beginHex2b));
  }

  int localHex2cCounter = -1;

  public boolean isHex2cModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex2cCounter != sharedCounter;
    localHex2cCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_2C_LEN = 2;
  /** serializeHex2c */
  protected void serializeHex2c(short hex2c) {
    replaceValue( //  save the value as string
        getBinaryString(hex2c, HEX_2C_LEN), beginHex2c, HEX_2C_LEN);
    localHex2cCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex2cMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex2c is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex2c() {
    return (getShort(beginHex2c));
  }

  int localHex2dCounter = -1;

  public boolean isHex2dModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex2dCounter != sharedCounter;
    localHex2dCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_2D_LEN = 2;
  /** serializeHex2d */
  protected void serializeHex2d(short hex2d) {
    replaceValue( //  save the value as string
        getBinaryString(hex2d, HEX_2D_LEN), beginHex2d, HEX_2D_LEN);
    localHex2dCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex2dMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex2d is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex2d() {
    return (getShort(beginHex2d));
  }

  int localHex2eCounter = -1;

  public boolean isHex2eModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex2eCounter != sharedCounter;
    localHex2eCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_2E_LEN = 2;
  /** serializeHex2e */
  protected void serializeHex2e(short hex2e) {
    replaceValue( //  save the value as string
        getBinaryString(hex2e, HEX_2E_LEN), beginHex2e, HEX_2E_LEN);
    localHex2eCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex2eMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex2e is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex2e() {
    return (getShort(beginHex2e));
  }

  int localHex2fCounter = -1;

  public boolean isHex2fModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex2fCounter != sharedCounter;
    localHex2fCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_2F_LEN = 2;
  /** serializeHex2f */
  protected void serializeHex2f(short hex2f) {
    replaceValue( //  save the value as string
        getBinaryString(hex2f, HEX_2F_LEN), beginHex2f, HEX_2F_LEN);
    localHex2fCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex2fMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex2f is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex2f() {
    return (getShort(beginHex2f));
  }

  int localHex30Counter = -1;

  public boolean isHex30Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex30Counter != sharedCounter;
    localHex30Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_30_LEN = 2;
  /** serializeHex30 */
  protected void serializeHex30(short hex30) {
    replaceValue( //  save the value as string
        getBinaryString(hex30, HEX_30_LEN), beginHex30, HEX_30_LEN);
    localHex30Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex30MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex30 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex30() {
    return (getShort(beginHex30));
  }

  int localHex31Counter = -1;

  public boolean isHex31Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex31Counter != sharedCounter;
    localHex31Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_31_LEN = 2;
  /** serializeHex31 */
  protected void serializeHex31(short hex31) {
    replaceValue( //  save the value as string
        getBinaryString(hex31, HEX_31_LEN), beginHex31, HEX_31_LEN);
    localHex31Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex31MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex31 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex31() {
    return (getShort(beginHex31));
  }

  int localHex32Counter = -1;

  public boolean isHex32Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex32Counter != sharedCounter;
    localHex32Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_32_LEN = 2;
  /** serializeHex32 */
  protected void serializeHex32(short hex32) {
    replaceValue( //  save the value as string
        getBinaryString(hex32, HEX_32_LEN), beginHex32, HEX_32_LEN);
    localHex32Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex32MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex32 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex32() {
    return (getShort(beginHex32));
  }

  int localHex33Counter = -1;

  public boolean isHex33Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex33Counter != sharedCounter;
    localHex33Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_33_LEN = 2;
  /** serializeHex33 */
  protected void serializeHex33(short hex33) {
    replaceValue( //  save the value as string
        getBinaryString(hex33, HEX_33_LEN), beginHex33, HEX_33_LEN);
    localHex33Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex33MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex33 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex33() {
    return (getShort(beginHex33));
  }

  int localHex34Counter = -1;

  public boolean isHex34Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex34Counter != sharedCounter;
    localHex34Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_34_LEN = 2;
  /** serializeHex34 */
  protected void serializeHex34(short hex34) {
    replaceValue( //  save the value as string
        getBinaryString(hex34, HEX_34_LEN), beginHex34, HEX_34_LEN);
    localHex34Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex34MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex34 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex34() {
    return (getShort(beginHex34));
  }

  int localHex35Counter = -1;

  public boolean isHex35Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex35Counter != sharedCounter;
    localHex35Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_35_LEN = 2;
  /** serializeHex35 */
  protected void serializeHex35(short hex35) {
    replaceValue( //  save the value as string
        getBinaryString(hex35, HEX_35_LEN), beginHex35, HEX_35_LEN);
    localHex35Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex35MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex35 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex35() {
    return (getShort(beginHex35));
  }

  int localHex36Counter = -1;

  public boolean isHex36Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex36Counter != sharedCounter;
    localHex36Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_36_LEN = 2;
  /** serializeHex36 */
  protected void serializeHex36(short hex36) {
    replaceValue( //  save the value as string
        getBinaryString(hex36, HEX_36_LEN), beginHex36, HEX_36_LEN);
    localHex36Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex36MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex36 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex36() {
    return (getShort(beginHex36));
  }

  int localHex37Counter = -1;

  public boolean isHex37Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex37Counter != sharedCounter;
    localHex37Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_37_LEN = 2;
  /** serializeHex37 */
  protected void serializeHex37(short hex37) {
    replaceValue( //  save the value as string
        getBinaryString(hex37, HEX_37_LEN), beginHex37, HEX_37_LEN);
    localHex37Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex37MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex37 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex37() {
    return (getShort(beginHex37));
  }

  int localHex38Counter = -1;

  public boolean isHex38Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex38Counter != sharedCounter;
    localHex38Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_38_LEN = 2;
  /** serializeHex38 */
  protected void serializeHex38(short hex38) {
    replaceValue( //  save the value as string
        getBinaryString(hex38, HEX_38_LEN), beginHex38, HEX_38_LEN);
    localHex38Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex38MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex38 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex38() {
    return (getShort(beginHex38));
  }

  int localHex39Counter = -1;

  public boolean isHex39Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex39Counter != sharedCounter;
    localHex39Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_39_LEN = 2;
  /** serializeHex39 */
  protected void serializeHex39(short hex39) {
    replaceValue( //  save the value as string
        getBinaryString(hex39, HEX_39_LEN), beginHex39, HEX_39_LEN);
    localHex39Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex39MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex39 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex39() {
    return (getShort(beginHex39));
  }

  int localHex3aCounter = -1;

  public boolean isHex3aModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex3aCounter != sharedCounter;
    localHex3aCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_3A_LEN = 2;
  /** serializeHex3a */
  protected void serializeHex3a(short hex3a) {
    replaceValue( //  save the value as string
        getBinaryString(hex3a, HEX_3A_LEN), beginHex3a, HEX_3A_LEN);
    localHex3aCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex3aMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex3a is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex3a() {
    return (getShort(beginHex3a));
  }

  int localHex3bCounter = -1;

  public boolean isHex3bModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex3bCounter != sharedCounter;
    localHex3bCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_3B_LEN = 2;
  /** serializeHex3b */
  protected void serializeHex3b(short hex3b) {
    replaceValue( //  save the value as string
        getBinaryString(hex3b, HEX_3B_LEN), beginHex3b, HEX_3B_LEN);
    localHex3bCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex3bMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex3b is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex3b() {
    return (getShort(beginHex3b));
  }

  int localHex3cCounter = -1;

  public boolean isHex3cModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex3cCounter != sharedCounter;
    localHex3cCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_3C_LEN = 2;
  /** serializeHex3c */
  protected void serializeHex3c(short hex3c) {
    replaceValue( //  save the value as string
        getBinaryString(hex3c, HEX_3C_LEN), beginHex3c, HEX_3C_LEN);
    localHex3cCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex3cMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex3c is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex3c() {
    return (getShort(beginHex3c));
  }

  int localHex3dCounter = -1;

  public boolean isHex3dModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex3dCounter != sharedCounter;
    localHex3dCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_3D_LEN = 2;
  /** serializeHex3d */
  protected void serializeHex3d(short hex3d) {
    replaceValue( //  save the value as string
        getBinaryString(hex3d, HEX_3D_LEN), beginHex3d, HEX_3D_LEN);
    localHex3dCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex3dMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex3d is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex3d() {
    return (getShort(beginHex3d));
  }

  int localHex3eCounter = -1;

  public boolean isHex3eModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex3eCounter != sharedCounter;
    localHex3eCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_3E_LEN = 2;
  /** serializeHex3e */
  protected void serializeHex3e(short hex3e) {
    replaceValue( //  save the value as string
        getBinaryString(hex3e, HEX_3E_LEN), beginHex3e, HEX_3E_LEN);
    localHex3eCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex3eMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex3e is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex3e() {
    return (getShort(beginHex3e));
  }

  int localHex3fCounter = -1;

  public boolean isHex3fModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex3fCounter != sharedCounter;
    localHex3fCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_3F_LEN = 2;
  /** serializeHex3f */
  protected void serializeHex3f(short hex3f) {
    replaceValue( //  save the value as string
        getBinaryString(hex3f, HEX_3F_LEN), beginHex3f, HEX_3F_LEN);
    localHex3fCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex3fMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex3f is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex3f() {
    return (getShort(beginHex3f));
  }

  int localHex40Counter = -1;

  public boolean isHex40Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex40Counter != sharedCounter;
    localHex40Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_40_LEN = 2;
  /** serializeHex40 */
  protected void serializeHex40(short hex40) {
    replaceValue( //  save the value as string
        getBinaryString(hex40, HEX_40_LEN), beginHex40, HEX_40_LEN);
    localHex40Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex40MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex40 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex40() {
    return (getShort(beginHex40));
  }

  int localHex41Counter = -1;

  public boolean isHex41Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex41Counter != sharedCounter;
    localHex41Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_41_LEN = 2;
  /** serializeHex41 */
  protected void serializeHex41(short hex41) {
    replaceValue( //  save the value as string
        getBinaryString(hex41, HEX_41_LEN), beginHex41, HEX_41_LEN);
    localHex41Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex41MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex41 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex41() {
    return (getShort(beginHex41));
  }

  int localHex42Counter = -1;

  public boolean isHex42Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex42Counter != sharedCounter;
    localHex42Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_42_LEN = 2;
  /** serializeHex42 */
  protected void serializeHex42(short hex42) {
    replaceValue( //  save the value as string
        getBinaryString(hex42, HEX_42_LEN), beginHex42, HEX_42_LEN);
    localHex42Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex42MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex42 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex42() {
    return (getShort(beginHex42));
  }

  int localHex43Counter = -1;

  public boolean isHex43Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex43Counter != sharedCounter;
    localHex43Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_43_LEN = 2;
  /** serializeHex43 */
  protected void serializeHex43(short hex43) {
    replaceValue( //  save the value as string
        getBinaryString(hex43, HEX_43_LEN), beginHex43, HEX_43_LEN);
    localHex43Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex43MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex43 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex43() {
    return (getShort(beginHex43));
  }

  int localHex44Counter = -1;

  public boolean isHex44Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex44Counter != sharedCounter;
    localHex44Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_44_LEN = 2;
  /** serializeHex44 */
  protected void serializeHex44(short hex44) {
    replaceValue( //  save the value as string
        getBinaryString(hex44, HEX_44_LEN), beginHex44, HEX_44_LEN);
    localHex44Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex44MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex44 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex44() {
    return (getShort(beginHex44));
  }

  int localHex45Counter = -1;

  public boolean isHex45Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex45Counter != sharedCounter;
    localHex45Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_45_LEN = 2;
  /** serializeHex45 */
  protected void serializeHex45(short hex45) {
    replaceValue( //  save the value as string
        getBinaryString(hex45, HEX_45_LEN), beginHex45, HEX_45_LEN);
    localHex45Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex45MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex45 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex45() {
    return (getShort(beginHex45));
  }

  int localHex46Counter = -1;

  public boolean isHex46Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex46Counter != sharedCounter;
    localHex46Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_46_LEN = 2;
  /** serializeHex46 */
  protected void serializeHex46(short hex46) {
    replaceValue( //  save the value as string
        getBinaryString(hex46, HEX_46_LEN), beginHex46, HEX_46_LEN);
    localHex46Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex46MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex46 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex46() {
    return (getShort(beginHex46));
  }

  int localHex47Counter = -1;

  public boolean isHex47Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex47Counter != sharedCounter;
    localHex47Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_47_LEN = 2;
  /** serializeHex47 */
  protected void serializeHex47(short hex47) {
    replaceValue( //  save the value as string
        getBinaryString(hex47, HEX_47_LEN), beginHex47, HEX_47_LEN);
    localHex47Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex47MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex47 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex47() {
    return (getShort(beginHex47));
  }

  int localHex48Counter = -1;

  public boolean isHex48Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex48Counter != sharedCounter;
    localHex48Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_48_LEN = 2;
  /** serializeHex48 */
  protected void serializeHex48(short hex48) {
    replaceValue( //  save the value as string
        getBinaryString(hex48, HEX_48_LEN), beginHex48, HEX_48_LEN);
    localHex48Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex48MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex48 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex48() {
    return (getShort(beginHex48));
  }

  int localHex49Counter = -1;

  public boolean isHex49Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex49Counter != sharedCounter;
    localHex49Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_49_LEN = 2;
  /** serializeHex49 */
  protected void serializeHex49(short hex49) {
    replaceValue( //  save the value as string
        getBinaryString(hex49, HEX_49_LEN), beginHex49, HEX_49_LEN);
    localHex49Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex49MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex49 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex49() {
    return (getShort(beginHex49));
  }

  int localHex4aCounter = -1;

  public boolean isHex4aModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex4aCounter != sharedCounter;
    localHex4aCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_4A_LEN = 2;
  /** serializeHex4a */
  protected void serializeHex4a(short hex4a) {
    replaceValue( //  save the value as string
        getBinaryString(hex4a, HEX_4A_LEN), beginHex4a, HEX_4A_LEN);
    localHex4aCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex4aMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex4a is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex4a() {
    return (getShort(beginHex4a));
  }

  int localHex4bCounter = -1;

  public boolean isHex4bModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex4bCounter != sharedCounter;
    localHex4bCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_4B_LEN = 2;
  /** serializeHex4b */
  protected void serializeHex4b(short hex4b) {
    replaceValue( //  save the value as string
        getBinaryString(hex4b, HEX_4B_LEN), beginHex4b, HEX_4B_LEN);
    localHex4bCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex4bMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex4b is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex4b() {
    return (getShort(beginHex4b));
  }

  int localHex4cCounter = -1;

  public boolean isHex4cModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex4cCounter != sharedCounter;
    localHex4cCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_4C_LEN = 2;
  /** serializeHex4c */
  protected void serializeHex4c(short hex4c) {
    replaceValue( //  save the value as string
        getBinaryString(hex4c, HEX_4C_LEN), beginHex4c, HEX_4C_LEN);
    localHex4cCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex4cMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex4c is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex4c() {
    return (getShort(beginHex4c));
  }

  int localHex4dCounter = -1;

  public boolean isHex4dModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex4dCounter != sharedCounter;
    localHex4dCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_4D_LEN = 2;
  /** serializeHex4d */
  protected void serializeHex4d(short hex4d) {
    replaceValue( //  save the value as string
        getBinaryString(hex4d, HEX_4D_LEN), beginHex4d, HEX_4D_LEN);
    localHex4dCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex4dMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex4d is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex4d() {
    return (getShort(beginHex4d));
  }

  int localHex4eCounter = -1;

  public boolean isHex4eModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex4eCounter != sharedCounter;
    localHex4eCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_4E_LEN = 2;
  /** serializeHex4e */
  protected void serializeHex4e(short hex4e) {
    replaceValue( //  save the value as string
        getBinaryString(hex4e, HEX_4E_LEN), beginHex4e, HEX_4E_LEN);
    localHex4eCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex4eMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex4e is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex4e() {
    return (getShort(beginHex4e));
  }

  int localHex4fCounter = -1;

  public boolean isHex4fModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex4fCounter != sharedCounter;
    localHex4fCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_4F_LEN = 2;
  /** serializeHex4f */
  protected void serializeHex4f(short hex4f) {
    replaceValue( //  save the value as string
        getBinaryString(hex4f, HEX_4F_LEN), beginHex4f, HEX_4F_LEN);
    localHex4fCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex4fMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex4f is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex4f() {
    return (getShort(beginHex4f));
  }

  int localHex50Counter = -1;

  public boolean isHex50Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex50Counter != sharedCounter;
    localHex50Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_50_LEN = 2;
  /** serializeHex50 */
  protected void serializeHex50(short hex50) {
    replaceValue( //  save the value as string
        getBinaryString(hex50, HEX_50_LEN), beginHex50, HEX_50_LEN);
    localHex50Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex50MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex50 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex50() {
    return (getShort(beginHex50));
  }

  int localHex51Counter = -1;

  public boolean isHex51Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex51Counter != sharedCounter;
    localHex51Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_51_LEN = 2;
  /** serializeHex51 */
  protected void serializeHex51(short hex51) {
    replaceValue( //  save the value as string
        getBinaryString(hex51, HEX_51_LEN), beginHex51, HEX_51_LEN);
    localHex51Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex51MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex51 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex51() {
    return (getShort(beginHex51));
  }

  int localHex52Counter = -1;

  public boolean isHex52Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex52Counter != sharedCounter;
    localHex52Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_52_LEN = 2;
  /** serializeHex52 */
  protected void serializeHex52(short hex52) {
    replaceValue( //  save the value as string
        getBinaryString(hex52, HEX_52_LEN), beginHex52, HEX_52_LEN);
    localHex52Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex52MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex52 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex52() {
    return (getShort(beginHex52));
  }

  int localHex53Counter = -1;

  public boolean isHex53Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex53Counter != sharedCounter;
    localHex53Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_53_LEN = 2;
  /** serializeHex53 */
  protected void serializeHex53(short hex53) {
    replaceValue( //  save the value as string
        getBinaryString(hex53, HEX_53_LEN), beginHex53, HEX_53_LEN);
    localHex53Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex53MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex53 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex53() {
    return (getShort(beginHex53));
  }

  int localHex54Counter = -1;

  public boolean isHex54Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex54Counter != sharedCounter;
    localHex54Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_54_LEN = 2;
  /** serializeHex54 */
  protected void serializeHex54(short hex54) {
    replaceValue( //  save the value as string
        getBinaryString(hex54, HEX_54_LEN), beginHex54, HEX_54_LEN);
    localHex54Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex54MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex54 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex54() {
    return (getShort(beginHex54));
  }

  int localHex55Counter = -1;

  public boolean isHex55Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex55Counter != sharedCounter;
    localHex55Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_55_LEN = 2;
  /** serializeHex55 */
  protected void serializeHex55(short hex55) {
    replaceValue( //  save the value as string
        getBinaryString(hex55, HEX_55_LEN), beginHex55, HEX_55_LEN);
    localHex55Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex55MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex55 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex55() {
    return (getShort(beginHex55));
  }

  int localHex56Counter = -1;

  public boolean isHex56Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex56Counter != sharedCounter;
    localHex56Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_56_LEN = 2;
  /** serializeHex56 */
  protected void serializeHex56(short hex56) {
    replaceValue( //  save the value as string
        getBinaryString(hex56, HEX_56_LEN), beginHex56, HEX_56_LEN);
    localHex56Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex56MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex56 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex56() {
    return (getShort(beginHex56));
  }

  int localHex57Counter = -1;

  public boolean isHex57Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex57Counter != sharedCounter;
    localHex57Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_57_LEN = 2;
  /** serializeHex57 */
  protected void serializeHex57(short hex57) {
    replaceValue( //  save the value as string
        getBinaryString(hex57, HEX_57_LEN), beginHex57, HEX_57_LEN);
    localHex57Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex57MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex57 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex57() {
    return (getShort(beginHex57));
  }

  int localHex58Counter = -1;

  public boolean isHex58Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex58Counter != sharedCounter;
    localHex58Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_58_LEN = 2;
  /** serializeHex58 */
  protected void serializeHex58(short hex58) {
    replaceValue( //  save the value as string
        getBinaryString(hex58, HEX_58_LEN), beginHex58, HEX_58_LEN);
    localHex58Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex58MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex58 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex58() {
    return (getShort(beginHex58));
  }

  int localHex59Counter = -1;

  public boolean isHex59Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex59Counter != sharedCounter;
    localHex59Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_59_LEN = 2;
  /** serializeHex59 */
  protected void serializeHex59(short hex59) {
    replaceValue( //  save the value as string
        getBinaryString(hex59, HEX_59_LEN), beginHex59, HEX_59_LEN);
    localHex59Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex59MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex59 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex59() {
    return (getShort(beginHex59));
  }

  int localHex5aCounter = -1;

  public boolean isHex5aModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex5aCounter != sharedCounter;
    localHex5aCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_5A_LEN = 2;
  /** serializeHex5a */
  protected void serializeHex5a(short hex5a) {
    replaceValue( //  save the value as string
        getBinaryString(hex5a, HEX_5A_LEN), beginHex5a, HEX_5A_LEN);
    localHex5aCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex5aMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex5a is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex5a() {
    return (getShort(beginHex5a));
  }

  int localHex5bCounter = -1;

  public boolean isHex5bModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex5bCounter != sharedCounter;
    localHex5bCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_5B_LEN = 2;
  /** serializeHex5b */
  protected void serializeHex5b(short hex5b) {
    replaceValue( //  save the value as string
        getBinaryString(hex5b, HEX_5B_LEN), beginHex5b, HEX_5B_LEN);
    localHex5bCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex5bMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex5b is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex5b() {
    return (getShort(beginHex5b));
  }

  int localHex5cCounter = -1;

  public boolean isHex5cModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex5cCounter != sharedCounter;
    localHex5cCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_5C_LEN = 2;
  /** serializeHex5c */
  protected void serializeHex5c(short hex5c) {
    replaceValue( //  save the value as string
        getBinaryString(hex5c, HEX_5C_LEN), beginHex5c, HEX_5C_LEN);
    localHex5cCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex5cMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex5c is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex5c() {
    return (getShort(beginHex5c));
  }

  int localHex5dCounter = -1;

  public boolean isHex5dModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex5dCounter != sharedCounter;
    localHex5dCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_5D_LEN = 2;
  /** serializeHex5d */
  protected void serializeHex5d(short hex5d) {
    replaceValue( //  save the value as string
        getBinaryString(hex5d, HEX_5D_LEN), beginHex5d, HEX_5D_LEN);
    localHex5dCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex5dMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex5d is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex5d() {
    return (getShort(beginHex5d));
  }

  int localHex5eCounter = -1;

  public boolean isHex5eModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex5eCounter != sharedCounter;
    localHex5eCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_5E_LEN = 2;
  /** serializeHex5e */
  protected void serializeHex5e(short hex5e) {
    replaceValue( //  save the value as string
        getBinaryString(hex5e, HEX_5E_LEN), beginHex5e, HEX_5E_LEN);
    localHex5eCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex5eMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex5e is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex5e() {
    return (getShort(beginHex5e));
  }

  int localHex5fCounter = -1;

  public boolean isHex5fModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex5fCounter != sharedCounter;
    localHex5fCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_5F_LEN = 2;
  /** serializeHex5f */
  protected void serializeHex5f(short hex5f) {
    replaceValue( //  save the value as string
        getBinaryString(hex5f, HEX_5F_LEN), beginHex5f, HEX_5F_LEN);
    localHex5fCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex5fMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex5f is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex5f() {
    return (getShort(beginHex5f));
  }

  int localHex60Counter = -1;

  public boolean isHex60Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex60Counter != sharedCounter;
    localHex60Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_60_LEN = 2;
  /** serializeHex60 */
  protected void serializeHex60(short hex60) {
    replaceValue( //  save the value as string
        getBinaryString(hex60, HEX_60_LEN), beginHex60, HEX_60_LEN);
    localHex60Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex60MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex60 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex60() {
    return (getShort(beginHex60));
  }

  int localHex61Counter = -1;

  public boolean isHex61Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex61Counter != sharedCounter;
    localHex61Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_61_LEN = 2;
  /** serializeHex61 */
  protected void serializeHex61(short hex61) {
    replaceValue( //  save the value as string
        getBinaryString(hex61, HEX_61_LEN), beginHex61, HEX_61_LEN);
    localHex61Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex61MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex61 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex61() {
    return (getShort(beginHex61));
  }

  int localHex62Counter = -1;

  public boolean isHex62Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex62Counter != sharedCounter;
    localHex62Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_62_LEN = 2;
  /** serializeHex62 */
  protected void serializeHex62(short hex62) {
    replaceValue( //  save the value as string
        getBinaryString(hex62, HEX_62_LEN), beginHex62, HEX_62_LEN);
    localHex62Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex62MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex62 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex62() {
    return (getShort(beginHex62));
  }

  int localHex63Counter = -1;

  public boolean isHex63Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex63Counter != sharedCounter;
    localHex63Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_63_LEN = 2;
  /** serializeHex63 */
  protected void serializeHex63(short hex63) {
    replaceValue( //  save the value as string
        getBinaryString(hex63, HEX_63_LEN), beginHex63, HEX_63_LEN);
    localHex63Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex63MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex63 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex63() {
    return (getShort(beginHex63));
  }

  int localHex64Counter = -1;

  public boolean isHex64Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex64Counter != sharedCounter;
    localHex64Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_64_LEN = 2;
  /** serializeHex64 */
  protected void serializeHex64(short hex64) {
    replaceValue( //  save the value as string
        getBinaryString(hex64, HEX_64_LEN), beginHex64, HEX_64_LEN);
    localHex64Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex64MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex64 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex64() {
    return (getShort(beginHex64));
  }

  int localHex65Counter = -1;

  public boolean isHex65Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex65Counter != sharedCounter;
    localHex65Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_65_LEN = 2;
  /** serializeHex65 */
  protected void serializeHex65(short hex65) {
    replaceValue( //  save the value as string
        getBinaryString(hex65, HEX_65_LEN), beginHex65, HEX_65_LEN);
    localHex65Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex65MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex65 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex65() {
    return (getShort(beginHex65));
  }

  int localHex66Counter = -1;

  public boolean isHex66Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex66Counter != sharedCounter;
    localHex66Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_66_LEN = 2;
  /** serializeHex66 */
  protected void serializeHex66(short hex66) {
    replaceValue( //  save the value as string
        getBinaryString(hex66, HEX_66_LEN), beginHex66, HEX_66_LEN);
    localHex66Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex66MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex66 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex66() {
    return (getShort(beginHex66));
  }

  int localHex67Counter = -1;

  public boolean isHex67Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex67Counter != sharedCounter;
    localHex67Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_67_LEN = 2;
  /** serializeHex67 */
  protected void serializeHex67(short hex67) {
    replaceValue( //  save the value as string
        getBinaryString(hex67, HEX_67_LEN), beginHex67, HEX_67_LEN);
    localHex67Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex67MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex67 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex67() {
    return (getShort(beginHex67));
  }

  int localHex68Counter = -1;

  public boolean isHex68Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex68Counter != sharedCounter;
    localHex68Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_68_LEN = 2;
  /** serializeHex68 */
  protected void serializeHex68(short hex68) {
    replaceValue( //  save the value as string
        getBinaryString(hex68, HEX_68_LEN), beginHex68, HEX_68_LEN);
    localHex68Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex68MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex68 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex68() {
    return (getShort(beginHex68));
  }

  int localHex69Counter = -1;

  public boolean isHex69Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex69Counter != sharedCounter;
    localHex69Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_69_LEN = 2;
  /** serializeHex69 */
  protected void serializeHex69(short hex69) {
    replaceValue( //  save the value as string
        getBinaryString(hex69, HEX_69_LEN), beginHex69, HEX_69_LEN);
    localHex69Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex69MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex69 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex69() {
    return (getShort(beginHex69));
  }

  int localHex6aCounter = -1;

  public boolean isHex6aModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex6aCounter != sharedCounter;
    localHex6aCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_6A_LEN = 2;
  /** serializeHex6a */
  protected void serializeHex6a(short hex6a) {
    replaceValue( //  save the value as string
        getBinaryString(hex6a, HEX_6A_LEN), beginHex6a, HEX_6A_LEN);
    localHex6aCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex6aMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex6a is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex6a() {
    return (getShort(beginHex6a));
  }

  int localHex6bCounter = -1;

  public boolean isHex6bModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex6bCounter != sharedCounter;
    localHex6bCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_6B_LEN = 2;
  /** serializeHex6b */
  protected void serializeHex6b(short hex6b) {
    replaceValue( //  save the value as string
        getBinaryString(hex6b, HEX_6B_LEN), beginHex6b, HEX_6B_LEN);
    localHex6bCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex6bMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex6b is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex6b() {
    return (getShort(beginHex6b));
  }

  int localHex6cCounter = -1;

  public boolean isHex6cModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex6cCounter != sharedCounter;
    localHex6cCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_6C_LEN = 2;
  /** serializeHex6c */
  protected void serializeHex6c(short hex6c) {
    replaceValue( //  save the value as string
        getBinaryString(hex6c, HEX_6C_LEN), beginHex6c, HEX_6C_LEN);
    localHex6cCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex6cMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex6c is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex6c() {
    return (getShort(beginHex6c));
  }

  int localHex6dCounter = -1;

  public boolean isHex6dModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex6dCounter != sharedCounter;
    localHex6dCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_6D_LEN = 2;
  /** serializeHex6d */
  protected void serializeHex6d(short hex6d) {
    replaceValue( //  save the value as string
        getBinaryString(hex6d, HEX_6D_LEN), beginHex6d, HEX_6D_LEN);
    localHex6dCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex6dMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex6d is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex6d() {
    return (getShort(beginHex6d));
  }

  int localHex6eCounter = -1;

  public boolean isHex6eModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex6eCounter != sharedCounter;
    localHex6eCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_6E_LEN = 2;
  /** serializeHex6e */
  protected void serializeHex6e(short hex6e) {
    replaceValue( //  save the value as string
        getBinaryString(hex6e, HEX_6E_LEN), beginHex6e, HEX_6E_LEN);
    localHex6eCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex6eMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex6e is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex6e() {
    return (getShort(beginHex6e));
  }

  int localHex6fCounter = -1;

  public boolean isHex6fModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex6fCounter != sharedCounter;
    localHex6fCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_6F_LEN = 2;
  /** serializeHex6f */
  protected void serializeHex6f(short hex6f) {
    replaceValue( //  save the value as string
        getBinaryString(hex6f, HEX_6F_LEN), beginHex6f, HEX_6F_LEN);
    localHex6fCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex6fMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex6f is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex6f() {
    return (getShort(beginHex6f));
  }

  int localHex70Counter = -1;

  public boolean isHex70Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex70Counter != sharedCounter;
    localHex70Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_70_LEN = 2;
  /** serializeHex70 */
  protected void serializeHex70(short hex70) {
    replaceValue( //  save the value as string
        getBinaryString(hex70, HEX_70_LEN), beginHex70, HEX_70_LEN);
    localHex70Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex70MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex70 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex70() {
    return (getShort(beginHex70));
  }

  int localHex71Counter = -1;

  public boolean isHex71Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex71Counter != sharedCounter;
    localHex71Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_71_LEN = 2;
  /** serializeHex71 */
  protected void serializeHex71(short hex71) {
    replaceValue( //  save the value as string
        getBinaryString(hex71, HEX_71_LEN), beginHex71, HEX_71_LEN);
    localHex71Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex71MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex71 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex71() {
    return (getShort(beginHex71));
  }

  int localHex72Counter = -1;

  public boolean isHex72Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex72Counter != sharedCounter;
    localHex72Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_72_LEN = 2;
  /** serializeHex72 */
  protected void serializeHex72(short hex72) {
    replaceValue( //  save the value as string
        getBinaryString(hex72, HEX_72_LEN), beginHex72, HEX_72_LEN);
    localHex72Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex72MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex72 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex72() {
    return (getShort(beginHex72));
  }

  int localHex73Counter = -1;

  public boolean isHex73Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex73Counter != sharedCounter;
    localHex73Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_73_LEN = 2;
  /** serializeHex73 */
  protected void serializeHex73(short hex73) {
    replaceValue( //  save the value as string
        getBinaryString(hex73, HEX_73_LEN), beginHex73, HEX_73_LEN);
    localHex73Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex73MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex73 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex73() {
    return (getShort(beginHex73));
  }

  int localHex74Counter = -1;

  public boolean isHex74Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex74Counter != sharedCounter;
    localHex74Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_74_LEN = 2;
  /** serializeHex74 */
  protected void serializeHex74(short hex74) {
    replaceValue( //  save the value as string
        getBinaryString(hex74, HEX_74_LEN), beginHex74, HEX_74_LEN);
    localHex74Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex74MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex74 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex74() {
    return (getShort(beginHex74));
  }

  int localHex75Counter = -1;

  public boolean isHex75Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex75Counter != sharedCounter;
    localHex75Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_75_LEN = 2;
  /** serializeHex75 */
  protected void serializeHex75(short hex75) {
    replaceValue( //  save the value as string
        getBinaryString(hex75, HEX_75_LEN), beginHex75, HEX_75_LEN);
    localHex75Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex75MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex75 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex75() {
    return (getShort(beginHex75));
  }

  int localHex76Counter = -1;

  public boolean isHex76Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex76Counter != sharedCounter;
    localHex76Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_76_LEN = 2;
  /** serializeHex76 */
  protected void serializeHex76(short hex76) {
    replaceValue( //  save the value as string
        getBinaryString(hex76, HEX_76_LEN), beginHex76, HEX_76_LEN);
    localHex76Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex76MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex76 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex76() {
    return (getShort(beginHex76));
  }

  int localHex77Counter = -1;

  public boolean isHex77Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex77Counter != sharedCounter;
    localHex77Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_77_LEN = 2;
  /** serializeHex77 */
  protected void serializeHex77(short hex77) {
    replaceValue( //  save the value as string
        getBinaryString(hex77, HEX_77_LEN), beginHex77, HEX_77_LEN);
    localHex77Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex77MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex77 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex77() {
    return (getShort(beginHex77));
  }

  int localHex78Counter = -1;

  public boolean isHex78Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex78Counter != sharedCounter;
    localHex78Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_78_LEN = 2;
  /** serializeHex78 */
  protected void serializeHex78(short hex78) {
    replaceValue( //  save the value as string
        getBinaryString(hex78, HEX_78_LEN), beginHex78, HEX_78_LEN);
    localHex78Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex78MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex78 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex78() {
    return (getShort(beginHex78));
  }

  int localHex79Counter = -1;

  public boolean isHex79Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex79Counter != sharedCounter;
    localHex79Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_79_LEN = 2;
  /** serializeHex79 */
  protected void serializeHex79(short hex79) {
    replaceValue( //  save the value as string
        getBinaryString(hex79, HEX_79_LEN), beginHex79, HEX_79_LEN);
    localHex79Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex79MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex79 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex79() {
    return (getShort(beginHex79));
  }

  int localHex7aCounter = -1;

  public boolean isHex7aModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex7aCounter != sharedCounter;
    localHex7aCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_7A_LEN = 2;
  /** serializeHex7a */
  protected void serializeHex7a(short hex7a) {
    replaceValue( //  save the value as string
        getBinaryString(hex7a, HEX_7A_LEN), beginHex7a, HEX_7A_LEN);
    localHex7aCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex7aMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex7a is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex7a() {
    return (getShort(beginHex7a));
  }

  int localHex7bCounter = -1;

  public boolean isHex7bModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex7bCounter != sharedCounter;
    localHex7bCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_7B_LEN = 2;
  /** serializeHex7b */
  protected void serializeHex7b(short hex7b) {
    replaceValue( //  save the value as string
        getBinaryString(hex7b, HEX_7B_LEN), beginHex7b, HEX_7B_LEN);
    localHex7bCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex7bMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex7b is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex7b() {
    return (getShort(beginHex7b));
  }

  int localHex7cCounter = -1;

  public boolean isHex7cModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex7cCounter != sharedCounter;
    localHex7cCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_7C_LEN = 2;
  /** serializeHex7c */
  protected void serializeHex7c(short hex7c) {
    replaceValue( //  save the value as string
        getBinaryString(hex7c, HEX_7C_LEN), beginHex7c, HEX_7C_LEN);
    localHex7cCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex7cMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex7c is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex7c() {
    return (getShort(beginHex7c));
  }

  int localHex7dCounter = -1;

  public boolean isHex7dModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex7dCounter != sharedCounter;
    localHex7dCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_7D_LEN = 2;
  /** serializeHex7d */
  protected void serializeHex7d(short hex7d) {
    replaceValue( //  save the value as string
        getBinaryString(hex7d, HEX_7D_LEN), beginHex7d, HEX_7D_LEN);
    localHex7dCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex7dMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex7d is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex7d() {
    return (getShort(beginHex7d));
  }

  int localHex7eCounter = -1;

  public boolean isHex7eModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex7eCounter != sharedCounter;
    localHex7eCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_7E_LEN = 2;
  /** serializeHex7e */
  protected void serializeHex7e(short hex7e) {
    replaceValue( //  save the value as string
        getBinaryString(hex7e, HEX_7E_LEN), beginHex7e, HEX_7E_LEN);
    localHex7eCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex7eMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex7e is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex7e() {
    return (getShort(beginHex7e));
  }

  int localHex7fCounter = -1;

  public boolean isHex7fModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex7fCounter != sharedCounter;
    localHex7fCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_7F_LEN = 2;
  /** serializeHex7f */
  protected void serializeHex7f(short hex7f) {
    replaceValue( //  save the value as string
        getBinaryString(hex7f, HEX_7F_LEN), beginHex7f, HEX_7F_LEN);
    localHex7fCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex7fMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex7f is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex7f() {
    return (getShort(beginHex7f));
  }

  int localHex80Counter = -1;

  public boolean isHex80Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex80Counter != sharedCounter;
    localHex80Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_80_LEN = 2;
  /** serializeHex80 */
  protected void serializeHex80(short hex80) {
    replaceValue( //  save the value as string
        getBinaryString(hex80, HEX_80_LEN), beginHex80, HEX_80_LEN);
    localHex80Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex80MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex80 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex80() {
    return (getShort(beginHex80));
  }

  int localHex81Counter = -1;

  public boolean isHex81Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex81Counter != sharedCounter;
    localHex81Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_81_LEN = 2;
  /** serializeHex81 */
  protected void serializeHex81(short hex81) {
    replaceValue( //  save the value as string
        getBinaryString(hex81, HEX_81_LEN), beginHex81, HEX_81_LEN);
    localHex81Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex81MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex81 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex81() {
    return (getShort(beginHex81));
  }

  int localHex82Counter = -1;

  public boolean isHex82Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex82Counter != sharedCounter;
    localHex82Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_82_LEN = 2;
  /** serializeHex82 */
  protected void serializeHex82(short hex82) {
    replaceValue( //  save the value as string
        getBinaryString(hex82, HEX_82_LEN), beginHex82, HEX_82_LEN);
    localHex82Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex82MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex82 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex82() {
    return (getShort(beginHex82));
  }

  int localHex83Counter = -1;

  public boolean isHex83Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex83Counter != sharedCounter;
    localHex83Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_83_LEN = 2;
  /** serializeHex83 */
  protected void serializeHex83(short hex83) {
    replaceValue( //  save the value as string
        getBinaryString(hex83, HEX_83_LEN), beginHex83, HEX_83_LEN);
    localHex83Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex83MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex83 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex83() {
    return (getShort(beginHex83));
  }

  int localHex84Counter = -1;

  public boolean isHex84Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex84Counter != sharedCounter;
    localHex84Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_84_LEN = 2;
  /** serializeHex84 */
  protected void serializeHex84(short hex84) {
    replaceValue( //  save the value as string
        getBinaryString(hex84, HEX_84_LEN), beginHex84, HEX_84_LEN);
    localHex84Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex84MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex84 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex84() {
    return (getShort(beginHex84));
  }

  int localHex85Counter = -1;

  public boolean isHex85Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex85Counter != sharedCounter;
    localHex85Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_85_LEN = 2;
  /** serializeHex85 */
  protected void serializeHex85(short hex85) {
    replaceValue( //  save the value as string
        getBinaryString(hex85, HEX_85_LEN), beginHex85, HEX_85_LEN);
    localHex85Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex85MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex85 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex85() {
    return (getShort(beginHex85));
  }

  int localHex86Counter = -1;

  public boolean isHex86Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex86Counter != sharedCounter;
    localHex86Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_86_LEN = 2;
  /** serializeHex86 */
  protected void serializeHex86(short hex86) {
    replaceValue( //  save the value as string
        getBinaryString(hex86, HEX_86_LEN), beginHex86, HEX_86_LEN);
    localHex86Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex86MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex86 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex86() {
    return (getShort(beginHex86));
  }

  int localHex87Counter = -1;

  public boolean isHex87Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex87Counter != sharedCounter;
    localHex87Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_87_LEN = 2;
  /** serializeHex87 */
  protected void serializeHex87(short hex87) {
    replaceValue( //  save the value as string
        getBinaryString(hex87, HEX_87_LEN), beginHex87, HEX_87_LEN);
    localHex87Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex87MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex87 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex87() {
    return (getShort(beginHex87));
  }

  int localHex88Counter = -1;

  public boolean isHex88Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex88Counter != sharedCounter;
    localHex88Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_88_LEN = 2;
  /** serializeHex88 */
  protected void serializeHex88(short hex88) {
    replaceValue( //  save the value as string
        getBinaryString(hex88, HEX_88_LEN), beginHex88, HEX_88_LEN);
    localHex88Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex88MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex88 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex88() {
    return (getShort(beginHex88));
  }

  int localHex89Counter = -1;

  public boolean isHex89Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex89Counter != sharedCounter;
    localHex89Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_89_LEN = 2;
  /** serializeHex89 */
  protected void serializeHex89(short hex89) {
    replaceValue( //  save the value as string
        getBinaryString(hex89, HEX_89_LEN), beginHex89, HEX_89_LEN);
    localHex89Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex89MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex89 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex89() {
    return (getShort(beginHex89));
  }

  int localHex8aCounter = -1;

  public boolean isHex8aModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex8aCounter != sharedCounter;
    localHex8aCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_8A_LEN = 2;
  /** serializeHex8a */
  protected void serializeHex8a(short hex8a) {
    replaceValue( //  save the value as string
        getBinaryString(hex8a, HEX_8A_LEN), beginHex8a, HEX_8A_LEN);
    localHex8aCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex8aMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex8a is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex8a() {
    return (getShort(beginHex8a));
  }

  int localHex8bCounter = -1;

  public boolean isHex8bModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex8bCounter != sharedCounter;
    localHex8bCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_8B_LEN = 2;
  /** serializeHex8b */
  protected void serializeHex8b(short hex8b) {
    replaceValue( //  save the value as string
        getBinaryString(hex8b, HEX_8B_LEN), beginHex8b, HEX_8B_LEN);
    localHex8bCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex8bMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex8b is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex8b() {
    return (getShort(beginHex8b));
  }

  int localHex8cCounter = -1;

  public boolean isHex8cModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex8cCounter != sharedCounter;
    localHex8cCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_8C_LEN = 2;
  /** serializeHex8c */
  protected void serializeHex8c(short hex8c) {
    replaceValue( //  save the value as string
        getBinaryString(hex8c, HEX_8C_LEN), beginHex8c, HEX_8C_LEN);
    localHex8cCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex8cMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex8c is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex8c() {
    return (getShort(beginHex8c));
  }

  int localHex8dCounter = -1;

  public boolean isHex8dModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex8dCounter != sharedCounter;
    localHex8dCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_8D_LEN = 2;
  /** serializeHex8d */
  protected void serializeHex8d(short hex8d) {
    replaceValue( //  save the value as string
        getBinaryString(hex8d, HEX_8D_LEN), beginHex8d, HEX_8D_LEN);
    localHex8dCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex8dMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex8d is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex8d() {
    return (getShort(beginHex8d));
  }

  int localHex8eCounter = -1;

  public boolean isHex8eModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex8eCounter != sharedCounter;
    localHex8eCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_8E_LEN = 2;
  /** serializeHex8e */
  protected void serializeHex8e(short hex8e) {
    replaceValue( //  save the value as string
        getBinaryString(hex8e, HEX_8E_LEN), beginHex8e, HEX_8E_LEN);
    localHex8eCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex8eMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex8e is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex8e() {
    return (getShort(beginHex8e));
  }

  int localHex8fCounter = -1;

  public boolean isHex8fModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex8fCounter != sharedCounter;
    localHex8fCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_8F_LEN = 2;
  /** serializeHex8f */
  protected void serializeHex8f(short hex8f) {
    replaceValue( //  save the value as string
        getBinaryString(hex8f, HEX_8F_LEN), beginHex8f, HEX_8F_LEN);
    localHex8fCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex8fMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex8f is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex8f() {
    return (getShort(beginHex8f));
  }

  int localHex90Counter = -1;

  public boolean isHex90Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex90Counter != sharedCounter;
    localHex90Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_90_LEN = 2;
  /** serializeHex90 */
  protected void serializeHex90(short hex90) {
    replaceValue( //  save the value as string
        getBinaryString(hex90, HEX_90_LEN), beginHex90, HEX_90_LEN);
    localHex90Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex90MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex90 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex90() {
    return (getShort(beginHex90));
  }

  int localHex91Counter = -1;

  public boolean isHex91Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex91Counter != sharedCounter;
    localHex91Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_91_LEN = 2;
  /** serializeHex91 */
  protected void serializeHex91(short hex91) {
    replaceValue( //  save the value as string
        getBinaryString(hex91, HEX_91_LEN), beginHex91, HEX_91_LEN);
    localHex91Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex91MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex91 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex91() {
    return (getShort(beginHex91));
  }

  int localHex92Counter = -1;

  public boolean isHex92Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex92Counter != sharedCounter;
    localHex92Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_92_LEN = 2;
  /** serializeHex92 */
  protected void serializeHex92(short hex92) {
    replaceValue( //  save the value as string
        getBinaryString(hex92, HEX_92_LEN), beginHex92, HEX_92_LEN);
    localHex92Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex92MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex92 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex92() {
    return (getShort(beginHex92));
  }

  int localHex93Counter = -1;

  public boolean isHex93Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex93Counter != sharedCounter;
    localHex93Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_93_LEN = 2;
  /** serializeHex93 */
  protected void serializeHex93(short hex93) {
    replaceValue( //  save the value as string
        getBinaryString(hex93, HEX_93_LEN), beginHex93, HEX_93_LEN);
    localHex93Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex93MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex93 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex93() {
    return (getShort(beginHex93));
  }

  int localHex94Counter = -1;

  public boolean isHex94Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex94Counter != sharedCounter;
    localHex94Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_94_LEN = 2;
  /** serializeHex94 */
  protected void serializeHex94(short hex94) {
    replaceValue( //  save the value as string
        getBinaryString(hex94, HEX_94_LEN), beginHex94, HEX_94_LEN);
    localHex94Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex94MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex94 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex94() {
    return (getShort(beginHex94));
  }

  int localHex95Counter = -1;

  public boolean isHex95Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex95Counter != sharedCounter;
    localHex95Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_95_LEN = 2;
  /** serializeHex95 */
  protected void serializeHex95(short hex95) {
    replaceValue( //  save the value as string
        getBinaryString(hex95, HEX_95_LEN), beginHex95, HEX_95_LEN);
    localHex95Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex95MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex95 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex95() {
    return (getShort(beginHex95));
  }

  int localHex96Counter = -1;

  public boolean isHex96Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex96Counter != sharedCounter;
    localHex96Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_96_LEN = 2;
  /** serializeHex96 */
  protected void serializeHex96(short hex96) {
    replaceValue( //  save the value as string
        getBinaryString(hex96, HEX_96_LEN), beginHex96, HEX_96_LEN);
    localHex96Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex96MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex96 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex96() {
    return (getShort(beginHex96));
  }

  int localHex97Counter = -1;

  public boolean isHex97Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex97Counter != sharedCounter;
    localHex97Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_97_LEN = 2;
  /** serializeHex97 */
  protected void serializeHex97(short hex97) {
    replaceValue( //  save the value as string
        getBinaryString(hex97, HEX_97_LEN), beginHex97, HEX_97_LEN);
    localHex97Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex97MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex97 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex97() {
    return (getShort(beginHex97));
  }

  int localHex98Counter = -1;

  public boolean isHex98Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex98Counter != sharedCounter;
    localHex98Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_98_LEN = 2;
  /** serializeHex98 */
  protected void serializeHex98(short hex98) {
    replaceValue( //  save the value as string
        getBinaryString(hex98, HEX_98_LEN), beginHex98, HEX_98_LEN);
    localHex98Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex98MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex98 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex98() {
    return (getShort(beginHex98));
  }

  int localHex99Counter = -1;

  public boolean isHex99Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex99Counter != sharedCounter;
    localHex99Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_99_LEN = 2;
  /** serializeHex99 */
  protected void serializeHex99(short hex99) {
    replaceValue( //  save the value as string
        getBinaryString(hex99, HEX_99_LEN), beginHex99, HEX_99_LEN);
    localHex99Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex99MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex99 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex99() {
    return (getShort(beginHex99));
  }

  int localHex9aCounter = -1;

  public boolean isHex9aModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex9aCounter != sharedCounter;
    localHex9aCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_9A_LEN = 2;
  /** serializeHex9a */
  protected void serializeHex9a(short hex9a) {
    replaceValue( //  save the value as string
        getBinaryString(hex9a, HEX_9A_LEN), beginHex9a, HEX_9A_LEN);
    localHex9aCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex9aMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex9a is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex9a() {
    return (getShort(beginHex9a));
  }

  int localHex9bCounter = -1;

  public boolean isHex9bModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex9bCounter != sharedCounter;
    localHex9bCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_9B_LEN = 2;
  /** serializeHex9b */
  protected void serializeHex9b(short hex9b) {
    replaceValue( //  save the value as string
        getBinaryString(hex9b, HEX_9B_LEN), beginHex9b, HEX_9B_LEN);
    localHex9bCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex9bMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex9b is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex9b() {
    return (getShort(beginHex9b));
  }

  int localHex9cCounter = -1;

  public boolean isHex9cModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex9cCounter != sharedCounter;
    localHex9cCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_9C_LEN = 2;
  /** serializeHex9c */
  protected void serializeHex9c(short hex9c) {
    replaceValue( //  save the value as string
        getBinaryString(hex9c, HEX_9C_LEN), beginHex9c, HEX_9C_LEN);
    localHex9cCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex9cMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex9c is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex9c() {
    return (getShort(beginHex9c));
  }

  int localHex9dCounter = -1;

  public boolean isHex9dModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex9dCounter != sharedCounter;
    localHex9dCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_9D_LEN = 2;
  /** serializeHex9d */
  protected void serializeHex9d(short hex9d) {
    replaceValue( //  save the value as string
        getBinaryString(hex9d, HEX_9D_LEN), beginHex9d, HEX_9D_LEN);
    localHex9dCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex9dMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex9d is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex9d() {
    return (getShort(beginHex9d));
  }

  int localHex9eCounter = -1;

  public boolean isHex9eModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex9eCounter != sharedCounter;
    localHex9eCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_9E_LEN = 2;
  /** serializeHex9e */
  protected void serializeHex9e(short hex9e) {
    replaceValue( //  save the value as string
        getBinaryString(hex9e, HEX_9E_LEN), beginHex9e, HEX_9E_LEN);
    localHex9eCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex9eMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex9e is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex9e() {
    return (getShort(beginHex9e));
  }

  int localHex9fCounter = -1;

  public boolean isHex9fModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHex9fCounter != sharedCounter;
    localHex9fCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_9F_LEN = 2;
  /** serializeHex9f */
  protected void serializeHex9f(short hex9f) {
    replaceValue( //  save the value as string
        getBinaryString(hex9f, HEX_9F_LEN), beginHex9f, HEX_9F_LEN);
    localHex9fCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHex9fMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHex9f is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHex9f() {
    return (getShort(beginHex9f));
  }

  int localHexA0Counter = -1;

  public boolean isHexA0Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexA0Counter != sharedCounter;
    localHexA0Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_A_0_LEN = 2;
  /** serializeHexA0 */
  protected void serializeHexA0(short hexA0) {
    replaceValue( //  save the value as string
        getBinaryString(hexA0, HEX_A_0_LEN), beginHexA0, HEX_A_0_LEN);
    localHexA0Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexA0MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexA0 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexA0() {
    return (getShort(beginHexA0));
  }

  int localHexA1Counter = -1;

  public boolean isHexA1Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexA1Counter != sharedCounter;
    localHexA1Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_A_1_LEN = 2;
  /** serializeHexA1 */
  protected void serializeHexA1(short hexA1) {
    replaceValue( //  save the value as string
        getBinaryString(hexA1, HEX_A_1_LEN), beginHexA1, HEX_A_1_LEN);
    localHexA1Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexA1MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexA1 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexA1() {
    return (getShort(beginHexA1));
  }

  int localHexA2Counter = -1;

  public boolean isHexA2Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexA2Counter != sharedCounter;
    localHexA2Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_A_2_LEN = 2;
  /** serializeHexA2 */
  protected void serializeHexA2(short hexA2) {
    replaceValue( //  save the value as string
        getBinaryString(hexA2, HEX_A_2_LEN), beginHexA2, HEX_A_2_LEN);
    localHexA2Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexA2MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexA2 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexA2() {
    return (getShort(beginHexA2));
  }

  int localHexA3Counter = -1;

  public boolean isHexA3Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexA3Counter != sharedCounter;
    localHexA3Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_A_3_LEN = 2;
  /** serializeHexA3 */
  protected void serializeHexA3(short hexA3) {
    replaceValue( //  save the value as string
        getBinaryString(hexA3, HEX_A_3_LEN), beginHexA3, HEX_A_3_LEN);
    localHexA3Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexA3MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexA3 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexA3() {
    return (getShort(beginHexA3));
  }

  int localHexA4Counter = -1;

  public boolean isHexA4Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexA4Counter != sharedCounter;
    localHexA4Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_A_4_LEN = 2;
  /** serializeHexA4 */
  protected void serializeHexA4(short hexA4) {
    replaceValue( //  save the value as string
        getBinaryString(hexA4, HEX_A_4_LEN), beginHexA4, HEX_A_4_LEN);
    localHexA4Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexA4MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexA4 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexA4() {
    return (getShort(beginHexA4));
  }

  int localHexA5Counter = -1;

  public boolean isHexA5Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexA5Counter != sharedCounter;
    localHexA5Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_A_5_LEN = 2;
  /** serializeHexA5 */
  protected void serializeHexA5(short hexA5) {
    replaceValue( //  save the value as string
        getBinaryString(hexA5, HEX_A_5_LEN), beginHexA5, HEX_A_5_LEN);
    localHexA5Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexA5MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexA5 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexA5() {
    return (getShort(beginHexA5));
  }

  int localHexA6Counter = -1;

  public boolean isHexA6Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexA6Counter != sharedCounter;
    localHexA6Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_A_6_LEN = 2;
  /** serializeHexA6 */
  protected void serializeHexA6(short hexA6) {
    replaceValue( //  save the value as string
        getBinaryString(hexA6, HEX_A_6_LEN), beginHexA6, HEX_A_6_LEN);
    localHexA6Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexA6MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexA6 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexA6() {
    return (getShort(beginHexA6));
  }

  int localHexA7Counter = -1;

  public boolean isHexA7Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexA7Counter != sharedCounter;
    localHexA7Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_A_7_LEN = 2;
  /** serializeHexA7 */
  protected void serializeHexA7(short hexA7) {
    replaceValue( //  save the value as string
        getBinaryString(hexA7, HEX_A_7_LEN), beginHexA7, HEX_A_7_LEN);
    localHexA7Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexA7MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexA7 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexA7() {
    return (getShort(beginHexA7));
  }

  int localHexA8Counter = -1;

  public boolean isHexA8Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexA8Counter != sharedCounter;
    localHexA8Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_A_8_LEN = 2;
  /** serializeHexA8 */
  protected void serializeHexA8(short hexA8) {
    replaceValue( //  save the value as string
        getBinaryString(hexA8, HEX_A_8_LEN), beginHexA8, HEX_A_8_LEN);
    localHexA8Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexA8MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexA8 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexA8() {
    return (getShort(beginHexA8));
  }

  int localHexA9Counter = -1;

  public boolean isHexA9Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexA9Counter != sharedCounter;
    localHexA9Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_A_9_LEN = 2;
  /** serializeHexA9 */
  protected void serializeHexA9(short hexA9) {
    replaceValue( //  save the value as string
        getBinaryString(hexA9, HEX_A_9_LEN), beginHexA9, HEX_A_9_LEN);
    localHexA9Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexA9MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexA9 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexA9() {
    return (getShort(beginHexA9));
  }

  int localHexAaCounter = -1;

  public boolean isHexAaModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexAaCounter != sharedCounter;
    localHexAaCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_AA_LEN = 2;
  /** serializeHexAa */
  protected void serializeHexAa(short hexAa) {
    replaceValue( //  save the value as string
        getBinaryString(hexAa, HEX_AA_LEN), beginHexAa, HEX_AA_LEN);
    localHexAaCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexAaMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexAa is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexAa() {
    return (getShort(beginHexAa));
  }

  int localHexAbCounter = -1;

  public boolean isHexAbModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexAbCounter != sharedCounter;
    localHexAbCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_AB_LEN = 2;
  /** serializeHexAb */
  protected void serializeHexAb(short hexAb) {
    replaceValue( //  save the value as string
        getBinaryString(hexAb, HEX_AB_LEN), beginHexAb, HEX_AB_LEN);
    localHexAbCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexAbMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexAb is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexAb() {
    return (getShort(beginHexAb));
  }

  int localHexAcCounter = -1;

  public boolean isHexAcModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexAcCounter != sharedCounter;
    localHexAcCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_AC_LEN = 2;
  /** serializeHexAc */
  protected void serializeHexAc(short hexAc) {
    replaceValue( //  save the value as string
        getBinaryString(hexAc, HEX_AC_LEN), beginHexAc, HEX_AC_LEN);
    localHexAcCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexAcMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexAc is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexAc() {
    return (getShort(beginHexAc));
  }

  int localHexAdCounter = -1;

  public boolean isHexAdModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexAdCounter != sharedCounter;
    localHexAdCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_AD_LEN = 2;
  /** serializeHexAd */
  protected void serializeHexAd(short hexAd) {
    replaceValue( //  save the value as string
        getBinaryString(hexAd, HEX_AD_LEN), beginHexAd, HEX_AD_LEN);
    localHexAdCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexAdMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexAd is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexAd() {
    return (getShort(beginHexAd));
  }

  int localHexAeCounter = -1;

  public boolean isHexAeModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexAeCounter != sharedCounter;
    localHexAeCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_AE_LEN = 2;
  /** serializeHexAe */
  protected void serializeHexAe(short hexAe) {
    replaceValue( //  save the value as string
        getBinaryString(hexAe, HEX_AE_LEN), beginHexAe, HEX_AE_LEN);
    localHexAeCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexAeMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexAe is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexAe() {
    return (getShort(beginHexAe));
  }

  int localHexAfCounter = -1;

  public boolean isHexAfModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexAfCounter != sharedCounter;
    localHexAfCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_AF_LEN = 2;
  /** serializeHexAf */
  protected void serializeHexAf(short hexAf) {
    replaceValue( //  save the value as string
        getBinaryString(hexAf, HEX_AF_LEN), beginHexAf, HEX_AF_LEN);
    localHexAfCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexAfMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexAf is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexAf() {
    return (getShort(beginHexAf));
  }

  int localHexB0Counter = -1;

  public boolean isHexB0Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexB0Counter != sharedCounter;
    localHexB0Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_B_0_LEN = 2;
  /** serializeHexB0 */
  protected void serializeHexB0(short hexB0) {
    replaceValue( //  save the value as string
        getBinaryString(hexB0, HEX_B_0_LEN), beginHexB0, HEX_B_0_LEN);
    localHexB0Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexB0MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexB0 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexB0() {
    return (getShort(beginHexB0));
  }

  int localHexB1Counter = -1;

  public boolean isHexB1Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexB1Counter != sharedCounter;
    localHexB1Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_B_1_LEN = 2;
  /** serializeHexB1 */
  protected void serializeHexB1(short hexB1) {
    replaceValue( //  save the value as string
        getBinaryString(hexB1, HEX_B_1_LEN), beginHexB1, HEX_B_1_LEN);
    localHexB1Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexB1MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexB1 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexB1() {
    return (getShort(beginHexB1));
  }

  int localHexB2Counter = -1;

  public boolean isHexB2Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexB2Counter != sharedCounter;
    localHexB2Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_B_2_LEN = 2;
  /** serializeHexB2 */
  protected void serializeHexB2(short hexB2) {
    replaceValue( //  save the value as string
        getBinaryString(hexB2, HEX_B_2_LEN), beginHexB2, HEX_B_2_LEN);
    localHexB2Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexB2MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexB2 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexB2() {
    return (getShort(beginHexB2));
  }

  int localHexB3Counter = -1;

  public boolean isHexB3Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexB3Counter != sharedCounter;
    localHexB3Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_B_3_LEN = 2;
  /** serializeHexB3 */
  protected void serializeHexB3(short hexB3) {
    replaceValue( //  save the value as string
        getBinaryString(hexB3, HEX_B_3_LEN), beginHexB3, HEX_B_3_LEN);
    localHexB3Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexB3MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexB3 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexB3() {
    return (getShort(beginHexB3));
  }

  int localHexB4Counter = -1;

  public boolean isHexB4Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexB4Counter != sharedCounter;
    localHexB4Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_B_4_LEN = 2;
  /** serializeHexB4 */
  protected void serializeHexB4(short hexB4) {
    replaceValue( //  save the value as string
        getBinaryString(hexB4, HEX_B_4_LEN), beginHexB4, HEX_B_4_LEN);
    localHexB4Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexB4MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexB4 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexB4() {
    return (getShort(beginHexB4));
  }

  int localHexB5Counter = -1;

  public boolean isHexB5Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexB5Counter != sharedCounter;
    localHexB5Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_B_5_LEN = 2;
  /** serializeHexB5 */
  protected void serializeHexB5(short hexB5) {
    replaceValue( //  save the value as string
        getBinaryString(hexB5, HEX_B_5_LEN), beginHexB5, HEX_B_5_LEN);
    localHexB5Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexB5MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexB5 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexB5() {
    return (getShort(beginHexB5));
  }

  int localHexB6Counter = -1;

  public boolean isHexB6Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexB6Counter != sharedCounter;
    localHexB6Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_B_6_LEN = 2;
  /** serializeHexB6 */
  protected void serializeHexB6(short hexB6) {
    replaceValue( //  save the value as string
        getBinaryString(hexB6, HEX_B_6_LEN), beginHexB6, HEX_B_6_LEN);
    localHexB6Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexB6MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexB6 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexB6() {
    return (getShort(beginHexB6));
  }

  int localHexB7Counter = -1;

  public boolean isHexB7Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexB7Counter != sharedCounter;
    localHexB7Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_B_7_LEN = 2;
  /** serializeHexB7 */
  protected void serializeHexB7(short hexB7) {
    replaceValue( //  save the value as string
        getBinaryString(hexB7, HEX_B_7_LEN), beginHexB7, HEX_B_7_LEN);
    localHexB7Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexB7MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexB7 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexB7() {
    return (getShort(beginHexB7));
  }

  int localHexB8Counter = -1;

  public boolean isHexB8Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexB8Counter != sharedCounter;
    localHexB8Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_B_8_LEN = 2;
  /** serializeHexB8 */
  protected void serializeHexB8(short hexB8) {
    replaceValue( //  save the value as string
        getBinaryString(hexB8, HEX_B_8_LEN), beginHexB8, HEX_B_8_LEN);
    localHexB8Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexB8MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexB8 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexB8() {
    return (getShort(beginHexB8));
  }

  int localHexB9Counter = -1;

  public boolean isHexB9Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexB9Counter != sharedCounter;
    localHexB9Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_B_9_LEN = 2;
  /** serializeHexB9 */
  protected void serializeHexB9(short hexB9) {
    replaceValue( //  save the value as string
        getBinaryString(hexB9, HEX_B_9_LEN), beginHexB9, HEX_B_9_LEN);
    localHexB9Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexB9MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexB9 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexB9() {
    return (getShort(beginHexB9));
  }

  int localHexBaCounter = -1;

  public boolean isHexBaModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexBaCounter != sharedCounter;
    localHexBaCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_BA_LEN = 2;
  /** serializeHexBa */
  protected void serializeHexBa(short hexBa) {
    replaceValue( //  save the value as string
        getBinaryString(hexBa, HEX_BA_LEN), beginHexBa, HEX_BA_LEN);
    localHexBaCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexBaMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexBa is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexBa() {
    return (getShort(beginHexBa));
  }

  int localHexBbCounter = -1;

  public boolean isHexBbModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexBbCounter != sharedCounter;
    localHexBbCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_BB_LEN = 2;
  /** serializeHexBb */
  protected void serializeHexBb(short hexBb) {
    replaceValue( //  save the value as string
        getBinaryString(hexBb, HEX_BB_LEN), beginHexBb, HEX_BB_LEN);
    localHexBbCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexBbMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexBb is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexBb() {
    return (getShort(beginHexBb));
  }

  int localHexBcCounter = -1;

  public boolean isHexBcModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexBcCounter != sharedCounter;
    localHexBcCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_BC_LEN = 2;
  /** serializeHexBc */
  protected void serializeHexBc(short hexBc) {
    replaceValue( //  save the value as string
        getBinaryString(hexBc, HEX_BC_LEN), beginHexBc, HEX_BC_LEN);
    localHexBcCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexBcMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexBc is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexBc() {
    return (getShort(beginHexBc));
  }

  int localHexBdCounter = -1;

  public boolean isHexBdModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexBdCounter != sharedCounter;
    localHexBdCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_BD_LEN = 2;
  /** serializeHexBd */
  protected void serializeHexBd(short hexBd) {
    replaceValue( //  save the value as string
        getBinaryString(hexBd, HEX_BD_LEN), beginHexBd, HEX_BD_LEN);
    localHexBdCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexBdMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexBd is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexBd() {
    return (getShort(beginHexBd));
  }

  int localHexBeCounter = -1;

  public boolean isHexBeModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexBeCounter != sharedCounter;
    localHexBeCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_BE_LEN = 2;
  /** serializeHexBe */
  protected void serializeHexBe(short hexBe) {
    replaceValue( //  save the value as string
        getBinaryString(hexBe, HEX_BE_LEN), beginHexBe, HEX_BE_LEN);
    localHexBeCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexBeMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexBe is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexBe() {
    return (getShort(beginHexBe));
  }

  int localHexBfCounter = -1;

  public boolean isHexBfModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexBfCounter != sharedCounter;
    localHexBfCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_BF_LEN = 2;
  /** serializeHexBf */
  protected void serializeHexBf(short hexBf) {
    replaceValue( //  save the value as string
        getBinaryString(hexBf, HEX_BF_LEN), beginHexBf, HEX_BF_LEN);
    localHexBfCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexBfMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexBf is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexBf() {
    return (getShort(beginHexBf));
  }

  int localHexC0Counter = -1;

  public boolean isHexC0Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexC0Counter != sharedCounter;
    localHexC0Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_C_0_LEN = 2;
  /** serializeHexC0 */
  protected void serializeHexC0(short hexC0) {
    replaceValue( //  save the value as string
        getBinaryString(hexC0, HEX_C_0_LEN), beginHexC0, HEX_C_0_LEN);
    localHexC0Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexC0MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexC0 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexC0() {
    return (getShort(beginHexC0));
  }

  int localHexC1Counter = -1;

  public boolean isHexC1Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexC1Counter != sharedCounter;
    localHexC1Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_C_1_LEN = 2;
  /** serializeHexC1 */
  protected void serializeHexC1(short hexC1) {
    replaceValue( //  save the value as string
        getBinaryString(hexC1, HEX_C_1_LEN), beginHexC1, HEX_C_1_LEN);
    localHexC1Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexC1MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexC1 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexC1() {
    return (getShort(beginHexC1));
  }

  int localHexC2Counter = -1;

  public boolean isHexC2Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexC2Counter != sharedCounter;
    localHexC2Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_C_2_LEN = 2;
  /** serializeHexC2 */
  protected void serializeHexC2(short hexC2) {
    replaceValue( //  save the value as string
        getBinaryString(hexC2, HEX_C_2_LEN), beginHexC2, HEX_C_2_LEN);
    localHexC2Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexC2MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexC2 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexC2() {
    return (getShort(beginHexC2));
  }

  int localHexC3Counter = -1;

  public boolean isHexC3Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexC3Counter != sharedCounter;
    localHexC3Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_C_3_LEN = 2;
  /** serializeHexC3 */
  protected void serializeHexC3(short hexC3) {
    replaceValue( //  save the value as string
        getBinaryString(hexC3, HEX_C_3_LEN), beginHexC3, HEX_C_3_LEN);
    localHexC3Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexC3MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexC3 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexC3() {
    return (getShort(beginHexC3));
  }

  int localHexC4Counter = -1;

  public boolean isHexC4Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexC4Counter != sharedCounter;
    localHexC4Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_C_4_LEN = 2;
  /** serializeHexC4 */
  protected void serializeHexC4(short hexC4) {
    replaceValue( //  save the value as string
        getBinaryString(hexC4, HEX_C_4_LEN), beginHexC4, HEX_C_4_LEN);
    localHexC4Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexC4MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexC4 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexC4() {
    return (getShort(beginHexC4));
  }

  int localHexC5Counter = -1;

  public boolean isHexC5Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexC5Counter != sharedCounter;
    localHexC5Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_C_5_LEN = 2;
  /** serializeHexC5 */
  protected void serializeHexC5(short hexC5) {
    replaceValue( //  save the value as string
        getBinaryString(hexC5, HEX_C_5_LEN), beginHexC5, HEX_C_5_LEN);
    localHexC5Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexC5MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexC5 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexC5() {
    return (getShort(beginHexC5));
  }

  int localHexC6Counter = -1;

  public boolean isHexC6Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexC6Counter != sharedCounter;
    localHexC6Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_C_6_LEN = 2;
  /** serializeHexC6 */
  protected void serializeHexC6(short hexC6) {
    replaceValue( //  save the value as string
        getBinaryString(hexC6, HEX_C_6_LEN), beginHexC6, HEX_C_6_LEN);
    localHexC6Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexC6MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexC6 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexC6() {
    return (getShort(beginHexC6));
  }

  int localHexC7Counter = -1;

  public boolean isHexC7Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexC7Counter != sharedCounter;
    localHexC7Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_C_7_LEN = 2;
  /** serializeHexC7 */
  protected void serializeHexC7(short hexC7) {
    replaceValue( //  save the value as string
        getBinaryString(hexC7, HEX_C_7_LEN), beginHexC7, HEX_C_7_LEN);
    localHexC7Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexC7MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexC7 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexC7() {
    return (getShort(beginHexC7));
  }

  int localHexC8Counter = -1;

  public boolean isHexC8Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexC8Counter != sharedCounter;
    localHexC8Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_C_8_LEN = 2;
  /** serializeHexC8 */
  protected void serializeHexC8(short hexC8) {
    replaceValue( //  save the value as string
        getBinaryString(hexC8, HEX_C_8_LEN), beginHexC8, HEX_C_8_LEN);
    localHexC8Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexC8MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexC8 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexC8() {
    return (getShort(beginHexC8));
  }

  int localHexC9Counter = -1;

  public boolean isHexC9Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexC9Counter != sharedCounter;
    localHexC9Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_C_9_LEN = 2;
  /** serializeHexC9 */
  protected void serializeHexC9(short hexC9) {
    replaceValue( //  save the value as string
        getBinaryString(hexC9, HEX_C_9_LEN), beginHexC9, HEX_C_9_LEN);
    localHexC9Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexC9MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexC9 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexC9() {
    return (getShort(beginHexC9));
  }

  int localHexCaCounter = -1;

  public boolean isHexCaModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexCaCounter != sharedCounter;
    localHexCaCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_CA_LEN = 2;
  /** serializeHexCa */
  protected void serializeHexCa(short hexCa) {
    replaceValue( //  save the value as string
        getBinaryString(hexCa, HEX_CA_LEN), beginHexCa, HEX_CA_LEN);
    localHexCaCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexCaMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexCa is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexCa() {
    return (getShort(beginHexCa));
  }

  int localHexCbCounter = -1;

  public boolean isHexCbModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexCbCounter != sharedCounter;
    localHexCbCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_CB_LEN = 2;
  /** serializeHexCb */
  protected void serializeHexCb(short hexCb) {
    replaceValue( //  save the value as string
        getBinaryString(hexCb, HEX_CB_LEN), beginHexCb, HEX_CB_LEN);
    localHexCbCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexCbMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexCb is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexCb() {
    return (getShort(beginHexCb));
  }

  int localHexCcCounter = -1;

  public boolean isHexCcModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexCcCounter != sharedCounter;
    localHexCcCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_CC_LEN = 2;
  /** serializeHexCc */
  protected void serializeHexCc(short hexCc) {
    replaceValue( //  save the value as string
        getBinaryString(hexCc, HEX_CC_LEN), beginHexCc, HEX_CC_LEN);
    localHexCcCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexCcMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexCc is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexCc() {
    return (getShort(beginHexCc));
  }

  int localHexCdCounter = -1;

  public boolean isHexCdModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexCdCounter != sharedCounter;
    localHexCdCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_CD_LEN = 2;
  /** serializeHexCd */
  protected void serializeHexCd(short hexCd) {
    replaceValue( //  save the value as string
        getBinaryString(hexCd, HEX_CD_LEN), beginHexCd, HEX_CD_LEN);
    localHexCdCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexCdMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexCd is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexCd() {
    return (getShort(beginHexCd));
  }

  int localHexCeCounter = -1;

  public boolean isHexCeModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexCeCounter != sharedCounter;
    localHexCeCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_CE_LEN = 2;
  /** serializeHexCe */
  protected void serializeHexCe(short hexCe) {
    replaceValue( //  save the value as string
        getBinaryString(hexCe, HEX_CE_LEN), beginHexCe, HEX_CE_LEN);
    localHexCeCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexCeMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexCe is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexCe() {
    return (getShort(beginHexCe));
  }

  int localHexCfCounter = -1;

  public boolean isHexCfModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexCfCounter != sharedCounter;
    localHexCfCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_CF_LEN = 2;
  /** serializeHexCf */
  protected void serializeHexCf(short hexCf) {
    replaceValue( //  save the value as string
        getBinaryString(hexCf, HEX_CF_LEN), beginHexCf, HEX_CF_LEN);
    localHexCfCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexCfMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexCf is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexCf() {
    return (getShort(beginHexCf));
  }

  int localHexD0Counter = -1;

  public boolean isHexD0Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexD0Counter != sharedCounter;
    localHexD0Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_D_0_LEN = 2;
  /** serializeHexD0 */
  protected void serializeHexD0(short hexD0) {
    replaceValue( //  save the value as string
        getBinaryString(hexD0, HEX_D_0_LEN), beginHexD0, HEX_D_0_LEN);
    localHexD0Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexD0MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexD0 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexD0() {
    return (getShort(beginHexD0));
  }

  int localHexD1Counter = -1;

  public boolean isHexD1Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexD1Counter != sharedCounter;
    localHexD1Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_D_1_LEN = 2;
  /** serializeHexD1 */
  protected void serializeHexD1(short hexD1) {
    replaceValue( //  save the value as string
        getBinaryString(hexD1, HEX_D_1_LEN), beginHexD1, HEX_D_1_LEN);
    localHexD1Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexD1MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexD1 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexD1() {
    return (getShort(beginHexD1));
  }

  int localHexD2Counter = -1;

  public boolean isHexD2Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexD2Counter != sharedCounter;
    localHexD2Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_D_2_LEN = 2;
  /** serializeHexD2 */
  protected void serializeHexD2(short hexD2) {
    replaceValue( //  save the value as string
        getBinaryString(hexD2, HEX_D_2_LEN), beginHexD2, HEX_D_2_LEN);
    localHexD2Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexD2MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexD2 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexD2() {
    return (getShort(beginHexD2));
  }

  int localHexD3Counter = -1;

  public boolean isHexD3Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexD3Counter != sharedCounter;
    localHexD3Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_D_3_LEN = 2;
  /** serializeHexD3 */
  protected void serializeHexD3(short hexD3) {
    replaceValue( //  save the value as string
        getBinaryString(hexD3, HEX_D_3_LEN), beginHexD3, HEX_D_3_LEN);
    localHexD3Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexD3MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexD3 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexD3() {
    return (getShort(beginHexD3));
  }

  int localHexD4Counter = -1;

  public boolean isHexD4Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexD4Counter != sharedCounter;
    localHexD4Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_D_4_LEN = 2;
  /** serializeHexD4 */
  protected void serializeHexD4(short hexD4) {
    replaceValue( //  save the value as string
        getBinaryString(hexD4, HEX_D_4_LEN), beginHexD4, HEX_D_4_LEN);
    localHexD4Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexD4MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexD4 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexD4() {
    return (getShort(beginHexD4));
  }

  int localHexD5Counter = -1;

  public boolean isHexD5Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexD5Counter != sharedCounter;
    localHexD5Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_D_5_LEN = 2;
  /** serializeHexD5 */
  protected void serializeHexD5(short hexD5) {
    replaceValue( //  save the value as string
        getBinaryString(hexD5, HEX_D_5_LEN), beginHexD5, HEX_D_5_LEN);
    localHexD5Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexD5MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexD5 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexD5() {
    return (getShort(beginHexD5));
  }

  int localHexD6Counter = -1;

  public boolean isHexD6Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexD6Counter != sharedCounter;
    localHexD6Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_D_6_LEN = 2;
  /** serializeHexD6 */
  protected void serializeHexD6(short hexD6) {
    replaceValue( //  save the value as string
        getBinaryString(hexD6, HEX_D_6_LEN), beginHexD6, HEX_D_6_LEN);
    localHexD6Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexD6MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexD6 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexD6() {
    return (getShort(beginHexD6));
  }

  int localHexD7Counter = -1;

  public boolean isHexD7Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexD7Counter != sharedCounter;
    localHexD7Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_D_7_LEN = 2;
  /** serializeHexD7 */
  protected void serializeHexD7(short hexD7) {
    replaceValue( //  save the value as string
        getBinaryString(hexD7, HEX_D_7_LEN), beginHexD7, HEX_D_7_LEN);
    localHexD7Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexD7MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexD7 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexD7() {
    return (getShort(beginHexD7));
  }

  int localHexD8Counter = -1;

  public boolean isHexD8Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexD8Counter != sharedCounter;
    localHexD8Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_D_8_LEN = 2;
  /** serializeHexD8 */
  protected void serializeHexD8(short hexD8) {
    replaceValue( //  save the value as string
        getBinaryString(hexD8, HEX_D_8_LEN), beginHexD8, HEX_D_8_LEN);
    localHexD8Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexD8MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexD8 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexD8() {
    return (getShort(beginHexD8));
  }

  int localHexD9Counter = -1;

  public boolean isHexD9Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexD9Counter != sharedCounter;
    localHexD9Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_D_9_LEN = 2;
  /** serializeHexD9 */
  protected void serializeHexD9(short hexD9) {
    replaceValue( //  save the value as string
        getBinaryString(hexD9, HEX_D_9_LEN), beginHexD9, HEX_D_9_LEN);
    localHexD9Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexD9MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexD9 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexD9() {
    return (getShort(beginHexD9));
  }

  int localHexDaCounter = -1;

  public boolean isHexDaModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexDaCounter != sharedCounter;
    localHexDaCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_DA_LEN = 2;
  /** serializeHexDa */
  protected void serializeHexDa(short hexDa) {
    replaceValue( //  save the value as string
        getBinaryString(hexDa, HEX_DA_LEN), beginHexDa, HEX_DA_LEN);
    localHexDaCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexDaMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexDa is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexDa() {
    return (getShort(beginHexDa));
  }

  int localHexDbCounter = -1;

  public boolean isHexDbModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexDbCounter != sharedCounter;
    localHexDbCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_DB_LEN = 2;
  /** serializeHexDb */
  protected void serializeHexDb(short hexDb) {
    replaceValue( //  save the value as string
        getBinaryString(hexDb, HEX_DB_LEN), beginHexDb, HEX_DB_LEN);
    localHexDbCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexDbMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexDb is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexDb() {
    return (getShort(beginHexDb));
  }

  int localHexDcCounter = -1;

  public boolean isHexDcModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexDcCounter != sharedCounter;
    localHexDcCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_DC_LEN = 2;
  /** serializeHexDc */
  protected void serializeHexDc(short hexDc) {
    replaceValue( //  save the value as string
        getBinaryString(hexDc, HEX_DC_LEN), beginHexDc, HEX_DC_LEN);
    localHexDcCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexDcMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexDc is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexDc() {
    return (getShort(beginHexDc));
  }

  int localHexDdCounter = -1;

  public boolean isHexDdModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexDdCounter != sharedCounter;
    localHexDdCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_DD_LEN = 2;
  /** serializeHexDd */
  protected void serializeHexDd(short hexDd) {
    replaceValue( //  save the value as string
        getBinaryString(hexDd, HEX_DD_LEN), beginHexDd, HEX_DD_LEN);
    localHexDdCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexDdMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexDd is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexDd() {
    return (getShort(beginHexDd));
  }

  int localHexDeCounter = -1;

  public boolean isHexDeModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexDeCounter != sharedCounter;
    localHexDeCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_DE_LEN = 2;
  /** serializeHexDe */
  protected void serializeHexDe(short hexDe) {
    replaceValue( //  save the value as string
        getBinaryString(hexDe, HEX_DE_LEN), beginHexDe, HEX_DE_LEN);
    localHexDeCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexDeMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexDe is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexDe() {
    return (getShort(beginHexDe));
  }

  int localHexDfCounter = -1;

  public boolean isHexDfModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexDfCounter != sharedCounter;
    localHexDfCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_DF_LEN = 2;
  /** serializeHexDf */
  protected void serializeHexDf(short hexDf) {
    replaceValue( //  save the value as string
        getBinaryString(hexDf, HEX_DF_LEN), beginHexDf, HEX_DF_LEN);
    localHexDfCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexDfMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexDf is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexDf() {
    return (getShort(beginHexDf));
  }

  int localHexE0Counter = -1;

  public boolean isHexE0Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexE0Counter != sharedCounter;
    localHexE0Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_E_0_LEN = 2;
  /** serializeHexE0 */
  protected void serializeHexE0(short hexE0) {
    replaceValue( //  save the value as string
        getBinaryString(hexE0, HEX_E_0_LEN), beginHexE0, HEX_E_0_LEN);
    localHexE0Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexE0MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexE0 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexE0() {
    return (getShort(beginHexE0));
  }

  int localHexE1Counter = -1;

  public boolean isHexE1Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexE1Counter != sharedCounter;
    localHexE1Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_E_1_LEN = 2;
  /** serializeHexE1 */
  protected void serializeHexE1(short hexE1) {
    replaceValue( //  save the value as string
        getBinaryString(hexE1, HEX_E_1_LEN), beginHexE1, HEX_E_1_LEN);
    localHexE1Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexE1MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexE1 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexE1() {
    return (getShort(beginHexE1));
  }

  int localHexE2Counter = -1;

  public boolean isHexE2Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexE2Counter != sharedCounter;
    localHexE2Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_E_2_LEN = 2;
  /** serializeHexE2 */
  protected void serializeHexE2(short hexE2) {
    replaceValue( //  save the value as string
        getBinaryString(hexE2, HEX_E_2_LEN), beginHexE2, HEX_E_2_LEN);
    localHexE2Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexE2MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexE2 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexE2() {
    return (getShort(beginHexE2));
  }

  int localHexE3Counter = -1;

  public boolean isHexE3Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexE3Counter != sharedCounter;
    localHexE3Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_E_3_LEN = 2;
  /** serializeHexE3 */
  protected void serializeHexE3(short hexE3) {
    replaceValue( //  save the value as string
        getBinaryString(hexE3, HEX_E_3_LEN), beginHexE3, HEX_E_3_LEN);
    localHexE3Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexE3MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexE3 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexE3() {
    return (getShort(beginHexE3));
  }

  int localHexE4Counter = -1;

  public boolean isHexE4Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexE4Counter != sharedCounter;
    localHexE4Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_E_4_LEN = 2;
  /** serializeHexE4 */
  protected void serializeHexE4(short hexE4) {
    replaceValue( //  save the value as string
        getBinaryString(hexE4, HEX_E_4_LEN), beginHexE4, HEX_E_4_LEN);
    localHexE4Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexE4MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexE4 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexE4() {
    return (getShort(beginHexE4));
  }

  int localHexE5Counter = -1;

  public boolean isHexE5Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexE5Counter != sharedCounter;
    localHexE5Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_E_5_LEN = 2;
  /** serializeHexE5 */
  protected void serializeHexE5(short hexE5) {
    replaceValue( //  save the value as string
        getBinaryString(hexE5, HEX_E_5_LEN), beginHexE5, HEX_E_5_LEN);
    localHexE5Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexE5MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexE5 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexE5() {
    return (getShort(beginHexE5));
  }

  int localHexE6Counter = -1;

  public boolean isHexE6Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexE6Counter != sharedCounter;
    localHexE6Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_E_6_LEN = 2;
  /** serializeHexE6 */
  protected void serializeHexE6(short hexE6) {
    replaceValue( //  save the value as string
        getBinaryString(hexE6, HEX_E_6_LEN), beginHexE6, HEX_E_6_LEN);
    localHexE6Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexE6MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexE6 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexE6() {
    return (getShort(beginHexE6));
  }

  int localHexE7Counter = -1;

  public boolean isHexE7Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexE7Counter != sharedCounter;
    localHexE7Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_E_7_LEN = 2;
  /** serializeHexE7 */
  protected void serializeHexE7(short hexE7) {
    replaceValue( //  save the value as string
        getBinaryString(hexE7, HEX_E_7_LEN), beginHexE7, HEX_E_7_LEN);
    localHexE7Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexE7MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexE7 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexE7() {
    return (getShort(beginHexE7));
  }

  int localHexE8Counter = -1;

  public boolean isHexE8Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexE8Counter != sharedCounter;
    localHexE8Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_E_8_LEN = 2;
  /** serializeHexE8 */
  protected void serializeHexE8(short hexE8) {
    replaceValue( //  save the value as string
        getBinaryString(hexE8, HEX_E_8_LEN), beginHexE8, HEX_E_8_LEN);
    localHexE8Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexE8MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexE8 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexE8() {
    return (getShort(beginHexE8));
  }

  int localHexE9Counter = -1;

  public boolean isHexE9Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexE9Counter != sharedCounter;
    localHexE9Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_E_9_LEN = 2;
  /** serializeHexE9 */
  protected void serializeHexE9(short hexE9) {
    replaceValue( //  save the value as string
        getBinaryString(hexE9, HEX_E_9_LEN), beginHexE9, HEX_E_9_LEN);
    localHexE9Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexE9MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexE9 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexE9() {
    return (getShort(beginHexE9));
  }

  int localHexEaCounter = -1;

  public boolean isHexEaModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexEaCounter != sharedCounter;
    localHexEaCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_EA_LEN = 2;
  /** serializeHexEa */
  protected void serializeHexEa(short hexEa) {
    replaceValue( //  save the value as string
        getBinaryString(hexEa, HEX_EA_LEN), beginHexEa, HEX_EA_LEN);
    localHexEaCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexEaMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexEa is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexEa() {
    return (getShort(beginHexEa));
  }

  int localHexEbCounter = -1;

  public boolean isHexEbModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexEbCounter != sharedCounter;
    localHexEbCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_EB_LEN = 2;
  /** serializeHexEb */
  protected void serializeHexEb(short hexEb) {
    replaceValue( //  save the value as string
        getBinaryString(hexEb, HEX_EB_LEN), beginHexEb, HEX_EB_LEN);
    localHexEbCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexEbMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexEb is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexEb() {
    return (getShort(beginHexEb));
  }

  int localHexEcCounter = -1;

  public boolean isHexEcModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexEcCounter != sharedCounter;
    localHexEcCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_EC_LEN = 2;
  /** serializeHexEc */
  protected void serializeHexEc(short hexEc) {
    replaceValue( //  save the value as string
        getBinaryString(hexEc, HEX_EC_LEN), beginHexEc, HEX_EC_LEN);
    localHexEcCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexEcMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexEc is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexEc() {
    return (getShort(beginHexEc));
  }

  int localHexEdCounter = -1;

  public boolean isHexEdModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexEdCounter != sharedCounter;
    localHexEdCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_ED_LEN = 2;
  /** serializeHexEd */
  protected void serializeHexEd(short hexEd) {
    replaceValue( //  save the value as string
        getBinaryString(hexEd, HEX_ED_LEN), beginHexEd, HEX_ED_LEN);
    localHexEdCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexEdMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexEd is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexEd() {
    return (getShort(beginHexEd));
  }

  int localHexEeCounter = -1;

  public boolean isHexEeModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexEeCounter != sharedCounter;
    localHexEeCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_EE_LEN = 2;
  /** serializeHexEe */
  protected void serializeHexEe(short hexEe) {
    replaceValue( //  save the value as string
        getBinaryString(hexEe, HEX_EE_LEN), beginHexEe, HEX_EE_LEN);
    localHexEeCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexEeMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexEe is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexEe() {
    return (getShort(beginHexEe));
  }

  int localHexEfCounter = -1;

  public boolean isHexEfModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexEfCounter != sharedCounter;
    localHexEfCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_EF_LEN = 2;
  /** serializeHexEf */
  protected void serializeHexEf(short hexEf) {
    replaceValue( //  save the value as string
        getBinaryString(hexEf, HEX_EF_LEN), beginHexEf, HEX_EF_LEN);
    localHexEfCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexEfMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexEf is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexEf() {
    return (getShort(beginHexEf));
  }

  int localHexF0Counter = -1;

  public boolean isHexF0Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexF0Counter != sharedCounter;
    localHexF0Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_F_0_LEN = 2;
  /** serializeHexF0 */
  protected void serializeHexF0(short hexF0) {
    replaceValue( //  save the value as string
        getBinaryString(hexF0, HEX_F_0_LEN), beginHexF0, HEX_F_0_LEN);
    localHexF0Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexF0MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexF0 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexF0() {
    return (getShort(beginHexF0));
  }

  int localHexF1Counter = -1;

  public boolean isHexF1Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexF1Counter != sharedCounter;
    localHexF1Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_F_1_LEN = 2;
  /** serializeHexF1 */
  protected void serializeHexF1(short hexF1) {
    replaceValue( //  save the value as string
        getBinaryString(hexF1, HEX_F_1_LEN), beginHexF1, HEX_F_1_LEN);
    localHexF1Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexF1MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexF1 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexF1() {
    return (getShort(beginHexF1));
  }

  int localHexF2Counter = -1;

  public boolean isHexF2Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexF2Counter != sharedCounter;
    localHexF2Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_F_2_LEN = 2;
  /** serializeHexF2 */
  protected void serializeHexF2(short hexF2) {
    replaceValue( //  save the value as string
        getBinaryString(hexF2, HEX_F_2_LEN), beginHexF2, HEX_F_2_LEN);
    localHexF2Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexF2MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexF2 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexF2() {
    return (getShort(beginHexF2));
  }

  int localHexF3Counter = -1;

  public boolean isHexF3Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexF3Counter != sharedCounter;
    localHexF3Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_F_3_LEN = 2;
  /** serializeHexF3 */
  protected void serializeHexF3(short hexF3) {
    replaceValue( //  save the value as string
        getBinaryString(hexF3, HEX_F_3_LEN), beginHexF3, HEX_F_3_LEN);
    localHexF3Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexF3MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexF3 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexF3() {
    return (getShort(beginHexF3));
  }

  int localHexF4Counter = -1;

  public boolean isHexF4Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexF4Counter != sharedCounter;
    localHexF4Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_F_4_LEN = 2;
  /** serializeHexF4 */
  protected void serializeHexF4(short hexF4) {
    replaceValue( //  save the value as string
        getBinaryString(hexF4, HEX_F_4_LEN), beginHexF4, HEX_F_4_LEN);
    localHexF4Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexF4MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexF4 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexF4() {
    return (getShort(beginHexF4));
  }

  int localHexF5Counter = -1;

  public boolean isHexF5Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexF5Counter != sharedCounter;
    localHexF5Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_F_5_LEN = 2;
  /** serializeHexF5 */
  protected void serializeHexF5(short hexF5) {
    replaceValue( //  save the value as string
        getBinaryString(hexF5, HEX_F_5_LEN), beginHexF5, HEX_F_5_LEN);
    localHexF5Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexF5MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexF5 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexF5() {
    return (getShort(beginHexF5));
  }

  int localHexF6Counter = -1;

  public boolean isHexF6Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexF6Counter != sharedCounter;
    localHexF6Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_F_6_LEN = 2;
  /** serializeHexF6 */
  protected void serializeHexF6(short hexF6) {
    replaceValue( //  save the value as string
        getBinaryString(hexF6, HEX_F_6_LEN), beginHexF6, HEX_F_6_LEN);
    localHexF6Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexF6MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexF6 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexF6() {
    return (getShort(beginHexF6));
  }

  int localHexF7Counter = -1;

  public boolean isHexF7Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexF7Counter != sharedCounter;
    localHexF7Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_F_7_LEN = 2;
  /** serializeHexF7 */
  protected void serializeHexF7(short hexF7) {
    replaceValue( //  save the value as string
        getBinaryString(hexF7, HEX_F_7_LEN), beginHexF7, HEX_F_7_LEN);
    localHexF7Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexF7MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexF7 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexF7() {
    return (getShort(beginHexF7));
  }

  int localHexF8Counter = -1;

  public boolean isHexF8Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexF8Counter != sharedCounter;
    localHexF8Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_F_8_LEN = 2;
  /** serializeHexF8 */
  protected void serializeHexF8(short hexF8) {
    replaceValue( //  save the value as string
        getBinaryString(hexF8, HEX_F_8_LEN), beginHexF8, HEX_F_8_LEN);
    localHexF8Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexF8MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexF8 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexF8() {
    return (getShort(beginHexF8));
  }

  int localHexF9Counter = -1;

  public boolean isHexF9Modified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexF9Counter != sharedCounter;
    localHexF9Counter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_F_9_LEN = 2;
  /** serializeHexF9 */
  protected void serializeHexF9(short hexF9) {
    replaceValue( //  save the value as string
        getBinaryString(hexF9, HEX_F_9_LEN), beginHexF9, HEX_F_9_LEN);
    localHexF9Counter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexF9MaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexF9 is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexF9() {
    return (getShort(beginHexF9));
  }

  int localHexFaCounter = -1;

  public boolean isHexFaModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexFaCounter != sharedCounter;
    localHexFaCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_FA_LEN = 2;
  /** serializeHexFa */
  protected void serializeHexFa(short hexFa) {
    replaceValue( //  save the value as string
        getBinaryString(hexFa, HEX_FA_LEN), beginHexFa, HEX_FA_LEN);
    localHexFaCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexFaMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexFa is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexFa() {
    return (getShort(beginHexFa));
  }

  int localHexFbCounter = -1;

  public boolean isHexFbModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexFbCounter != sharedCounter;
    localHexFbCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_FB_LEN = 2;
  /** serializeHexFb */
  protected void serializeHexFb(short hexFb) {
    replaceValue( //  save the value as string
        getBinaryString(hexFb, HEX_FB_LEN), beginHexFb, HEX_FB_LEN);
    localHexFbCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexFbMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexFb is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexFb() {
    return (getShort(beginHexFb));
  }

  int localHexFcCounter = -1;

  public boolean isHexFcModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexFcCounter != sharedCounter;
    localHexFcCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_FC_LEN = 2;
  /** serializeHexFc */
  protected void serializeHexFc(short hexFc) {
    replaceValue( //  save the value as string
        getBinaryString(hexFc, HEX_FC_LEN), beginHexFc, HEX_FC_LEN);
    localHexFcCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexFcMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexFc is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexFc() {
    return (getShort(beginHexFc));
  }

  int localHexFdCounter = -1;

  public boolean isHexFdModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexFdCounter != sharedCounter;
    localHexFdCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_FD_LEN = 2;
  /** serializeHexFd */
  protected void serializeHexFd(short hexFd) {
    replaceValue( //  save the value as string
        getBinaryString(hexFd, HEX_FD_LEN), beginHexFd, HEX_FD_LEN);
    localHexFdCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexFdMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexFd is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexFd() {
    return (getShort(beginHexFd));
  }

  int localHexFeCounter = -1;

  public boolean isHexFeModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexFeCounter != sharedCounter;
    localHexFeCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_FE_LEN = 2;
  /** serializeHexFe */
  protected void serializeHexFe(short hexFe) {
    replaceValue( //  save the value as string
        getBinaryString(hexFe, HEX_FE_LEN), beginHexFe, HEX_FE_LEN);
    localHexFeCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexFeMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexFe is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexFe() {
    return (getShort(beginHexFe));
  }

  int localHexFfCounter = -1;

  public boolean isHexFfModified() {
    int sharedCounter = shareString.getSerializedField().getModifiedCounter();
    boolean hasModified = localHexFfCounter != sharedCounter;
    localHexFfCounter = sharedCounter;
    return hasModified;
  }

  protected static final int HEX_FF_LEN = 2;
  /** serializeHexFf */
  protected void serializeHexFf(short hexFf) {
    replaceValue( //  save the value as string
        getBinaryString(hexFf, HEX_FF_LEN), beginHexFf, HEX_FF_LEN);
    localHexFfCounter = shareString.getSerializedField().getModifiedCounter();
  }

  protected short checkHexFfMaxLimit(long number) {
    return (short) checkMaxLimit(number, true /*isSigned*/, 2 /*dataLen*/);
  }
  /**
   * refreshHexFf is used to refresh the latest value of a variable from the Serialized String the
   * most common reason to serialize an Object as a string in to write to a file. There can be
   * several other reasons for serialization as well
   */
  public short refreshHexFf() {
    return (getShort(beginHexFf));
  }
}
