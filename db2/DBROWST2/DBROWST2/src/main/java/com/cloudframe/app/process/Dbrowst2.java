  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-29 at 05:41. */
  
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.exception.Terminate;
  import org.springframework.beans.factory.annotation.Autowired;
  import org.springframework.beans.factory.annotation.Qualifier;
  import com.cloudframe.app.repository.Dbrowst2Repository;
  import com.cloudframe.app.utility.CFUtil;
  import com.cloudframe.app.dbrowst2.dto.*;
  import com.cloudframe.app.dbrowst2.dto.Sqlca;
  import com.cloudframe.app.dbrowst2.dto.SecTable;
  import com.cloudframe.app.dbrowst2.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.common.SQLS;
  import org.springframework.beans.factory.annotation.Value;
  import com.cloudframe.app.dao.Db2Base;
  import java.sql.SQLException;
  
  @Component("dbrowst2")
  
  public class Dbrowst2 extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Dbrowst2.class);
  
  private Sqlca sqlca = new Sqlca() ;
  private SecTable secTable = new SecTable() ;
  private Work work = new Work() ;
  
  @Value("${DBROWST2.dbQualifier:}")
  private String dbQualifier;
  
  
  @Autowired 
  @Qualifier("db2Base")
  Db2Base db2Base;
  @Autowired 
  @Qualifier("dbrowst2Repository")
  Dbrowst2Repository dbrowst2Repository;
  
  
  
  
  
  
      /**
      * process 
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");
          ;
//  PERFORM START-OF-PROGRAM
          startOfProgram();/*START-OF-PROGRAM*/
          if (this.isProgramEnded()) {
              return getRc();
          }
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
        finally {
		handleDbAtEnd(db2Base); 
		clearVars(); 
      

      }
      
       return getRc(); // Exit with return code
      // end of process method
      }
      /**
      * startOfProgram 
      *   This method is derived from 
  *   COBOL Paragraph - START-OF-PROGRAM COBOL Cyclomatic complexity - 2
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      private void startOfProgram() throws Exception {
//  PERFORM 0000-INITIALIZE THRU 0000-EXIT
          initialize();/*0000-INITIALIZE*/
          if (this.isProgramEnded()) {
              return ;
          }
//  PERFORM 1000-FETCH-ROWS THRU 1000-EXIT
          fetchRows();/*1000-FETCH-ROWS*/
          if (this.isProgramEnded()) {
              return ;
          }
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          this.setProgramEnded(true);
          return ;
      
      }
      /**
      * initialize 
      *   This method is derived from 
  *   COBOL Paragraph - 0000-INITIALIZE COBOL Cyclomatic complexity - 1
      * Input  : None 

      * Output :  

      * - maxRows                        COBOL Name: WS-MAX-ROWS
      *
      * @throws CFException
      */
      private void initialize() throws Exception {

// *

// *
//  MOVE 30 TO WS-MAX-ROWS
          work.setMaxRows(30);
          ;
      
      }
      /**
      * fetchRows 
      *   This method is derived from 
  *   COBOL Paragraph - 1000-FETCH-ROWS COBOL Cyclomatic complexity - 13
      * Input  :  

      * - sqlcode                        COBOL Name: SQLCODE
      * - ix                             COBOL Name: IX
      * - maxRows                        COBOL Name: WS-MAX-ROWS
      * - isin                           COBOL Name: WS-ISIN
      * - sedol                          COBOL Name: WS-SEDOL
      * - ric                            COBOL Name: WS-RIC
      *
      * Output :  

      * - sqlcode_Ws                     COBOL Name: WS-SQLCODE
      * - sqlcode                        COBOL Name: SQLCODE
      * - dsnnrows                       COBOL Name: DSNNROWS
      * - ix                             COBOL Name: IX
      *
      * @throws CFException
      */
      private void fetchRows() throws Exception {
			// Declare local variables used in the method
			int sqlcode = 0;
			// End of variable declaration

//  SELECT ISIN , SEDOL , RIC FROM TBDEMSEC ORDER BY ISIN ASC
          dbrowst2Repository.openSecCursorDbrowst2(sqlca);
//  IF SQLCODE = 0 THEN
          sqlcode = sqlca.getSqlcode();
//  ELSE
          if (	( sqlcode != 0 )) { 
              // MOVE SQLCODE TO WS-SQLCODE
              //  FORMAT1016704244 = "-999"
              work.setSqlcode_Ws(CFUtil.cobolNumberFormatter(CONSTANTS.FORMAT1016704244,String.valueOf(sqlca.getSqlcode()).toCharArray()));
//  DISPLAY 'OPEN SEC_CURSOR FAILED : SQLCODE = ' WS-SQLCODE
              logger.info("OPEN SEC_CURSOR FAILED : SQLCODE = {}", new String(work.getSqlcode_Ws())); 
//  GOBACK
              setNotLogged(false); // no need to log, it is a normal termination
              this.setProgramEnded(true);
              return ;
          }
  

// *
// FETCH NEXT ROWSET FROM SEC_CURSOR FOR 30 ROWS INTO  ?    ,   ?    ,   ? 
//  PERFORM 3 TIMES
          for (int index0 = 0; index0 < 3;index0++) {
//  MOVE 30 TO DSNNROWS
              work.setDsnnrows(30);
//  FETCH NEXT ROWSET FROM SEC_CURSOR FOR 30 ROWS INTO ? , ? , ?
              dbrowst2Repository.fetchSecCursorDbrowst2(sqlca,secTable);
              sqlcode = sqlca.getSqlcode();
//  IF SQLCODE = 0 THEN
              if (	( sqlcode == 0 )) { 
//  PERFORM VARYING IX FROM 1 BY 1 UNTIL IX > WS-MAX-ROWS
                  for (work.setIx(1); (	( work.getIx() <= work.getMaxRows() ) ) ; work.setIx(work.getIx() + 1) ) {
//  DISPLAY WS-ISIN (IX) ' ** ' WS-SEDOL (IX) ' ** ' WS-RIC (IX)
                      logger.info("{} ** {} ** {}", new String(secTable.getIsin(work.getIx() - 1)), new String(secTable.getSedol(work.getIx() - 1)), new String(secTable.getRic(work.getIx() - 1))); 
                  }
              }
  
//  ELSE
              else { 
                  // MOVE SQLCODE TO WS-SQLCODE
                  //  FORMAT1016704244 = "-999"
                  work.setSqlcode_Ws(CFUtil.cobolNumberFormatter(CONSTANTS.FORMAT1016704244,String.valueOf(sqlca.getSqlcode()).toCharArray()));
//  DISPLAY 'FETCH SEC_CURSOR FAILED : SQLCODE = ' WS-SQLCODE
                  logger.info("FETCH SEC_CURSOR FAILED : SQLCODE = {}", new String(work.getSqlcode_Ws())); 
//  GOBACK
                  setNotLogged(false); // no need to log, it is a normal termination
                  this.setProgramEnded(true);
                  return ;
              }
          }
//  CLOSE SEC_CURSOR
          dbrowst2Repository.closeSecCursorDbrowst2(sqlca);
//  IF SQLCODE = 0 THEN
          sqlcode = sqlca.getSqlcode();
//  ELSE
          if (	( sqlcode != 0 )) { 
              // MOVE SQLCODE TO WS-SQLCODE
              //  FORMAT1016704244 = "-999"
              work.setSqlcode_Ws(CFUtil.cobolNumberFormatter(CONSTANTS.FORMAT1016704244,String.valueOf(sqlca.getSqlcode()).toCharArray()));
//  DISPLAY 'OPEN SEC_CURSOR FAILED : SQLCODE = ' WS-SQLCODE
              logger.info("OPEN SEC_CURSOR FAILED : SQLCODE = {}", new String(work.getSqlcode_Ws())); 
//  GOBACK
              setNotLogged(false); // no need to log, it is a normal termination
              this.setProgramEnded(true);
              return ;
          }
  
          ;
      
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          db2Base.reset("DBROWST2" ,dbQualifier, true/*use Dynamic SQL*/);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
  
  
  
  
  
  }
