  package com.cloudframe.app.process.impl;
  /* Java code generated by CloudFrame on 2025-04-24 at 16:45. */
  
  import com.cloudframe.app.dlcrntof.DlcrntofCtx.*;
  import com.cloudframe.app.dlcrntof.DlcrntofCtx;
  import com.cloudframe.app.process.Dlcrntof;
  import com.cloudframe.app.process.BaseProcess;
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.dto.GlobalExecutorCtx;
  import org.springframework.beans.factory.annotation.Autowired;
  import org.springframework.beans.factory.annotation.Qualifier;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.repository.DlcrntofRepository;
  import com.cloudframe.app.utility.CFUtil;
  import com.cloudframe.app.dlcrntof.dto.*;
  import com.cloudframe.app.dlcrntof.dto.Sqlca;
  import com.cloudframe.app.dlcrntof.dto.Dcltbdelsec;
  import com.cloudframe.app.dlcrntof.dto.IsinTableData;
  import com.cloudframe.app.dlcrntof.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.common.SQLS;
  import org.springframework.beans.factory.annotation.Value;
  import com.cloudframe.app.dao.Db2Base;
  import java.sql.SQLException;
  
  @Component("dlcrntof")
  
  public class DlcrntofImpl extends CommonProcess implements Dlcrntof {
  
  Logger logger = LoggerFactory.getLogger(DlcrntofImpl.class);
  
  
  @Value("${DLCRNTOF.dbQualifier:}")
  private String dbQualifier;
  
  
  @Autowired 
  @Qualifier("db2Base")
  Db2Base db2Base;
  @Autowired 
  @Qualifier("dlcrntofRepository")
  DlcrntofRepository dlcrntofRepository;
  
  
  private static final int ISINDELETED_OFFSET = 0;
  private static final int ISINDELETED_LENGTH = 12;
  
  
  
  
      /**
      * process 
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      public int process(DlcrntofCtx programCtx) throws Exception {
       try {
       setCodePage("1047");
            // Reset program ended flag
           programCtx.setProgramEnded(false);
      	db2Base.reset("DLCRNTOF" ,dbQualifier, true/*use Dynamic SQL*/);
          ;
//  PERFORM 0000-MAINLINE
          mainline(programCtx);/*0000-MAINLINE*/
          if (programCtx.isProgramEnded()) {
              return programCtx.getRc();
          }
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
        finally {
		handleDbAtEnd(db2Base); 
		clearVars(); 
      

      }
      
       return programCtx.getRc(); // Exit with return code
      // end of process method
      }
      /**
      * mainline 
      *   This method is derived from 
  *   COBOL Paragraph - 0000-MAINLINE COBOL Cyclomatic complexity - 3
      * Input  : None 

      * Output :  

      * - deleteCnt                      COBOL Name: WS-DELETE-CNT
      *
      * @throws CFException
      */
      @Override
      public MainlineOutCtx mainline(DlcrntofCtx programCtx) throws Exception {
MainlineOutCtx methodOut = programCtx.getMainlineOutCtx();
//  PERFORM 1000-INSERT-INTO-TBDELSEC THRU 1000-EXIT
          insertIntoTbdelsec(programCtx.getInsertIntoTbdelsecInCtx());/*1000-INSERT-INTO-TBDELSEC*/
          if (programCtx.isProgramEnded()) {
              return methodOut;
          }
//  COMMIT
          try {
          	// COMMIT
          	// reset SQLCODE
          	methodOut.setSqlcode(0);
             // execute jdbc commit
             db2Base.commit();
          }
           catch (SQLException e) {
                     methodOut.setSqlcode(Db2Base.fillSQLCode(e.getMessage()));
                 }
           catch(Exception e) {
             handleErrorCode(e);
          }

// *
//  DISPLAY 'Count of Securities BEFORE Delete'
          logger.info("Count of Securities BEFORE Delete"); 
//  PERFORM 9000-PRINT-TBDELSEC-ROWCNT THRU 9000-EXIT
          printTbdelsecRowcnt(programCtx.getPrintTbdelsecRowcntInCtx());/*9000-PRINT-TBDELSEC-ROWCNT*/
          if (programCtx.isProgramEnded()) {
              return methodOut;
          }

// *

// *
          // MOVE 0 TO WS-DELETE-CNT
          methodOut.setDeleteCnt((short)0);
//  PERFORM 2000-DELETE-TBDELSEC-ROWS THRU 2000-EXIT
          deleteTbdelsecRows(programCtx.getDeleteTbdelsecRowsInCtx());/*2000-DELETE-TBDELSEC-ROWS*/
          if (programCtx.isProgramEnded()) {
              return methodOut;
          }

// *
//  PERFORM 3000-PRINT-DELETED-ISIN THRU 3000-EXIT
          printDeletedIsin(programCtx.getPrintDeletedIsinInCtx());/*3000-PRINT-DELETED-ISIN*/
          if (programCtx.isProgramEnded()) {
              return methodOut;
          }

// *
//  DISPLAY 'Count of Securities AFTER  Delete'
          logger.info("Count of Securities AFTER  Delete"); 
//  PERFORM 9000-PRINT-TBDELSEC-ROWCNT THRU 9000-EXIT
          printTbdelsecRowcnt(programCtx.getPrintTbdelsecRowcntInCtx());/*9000-PRINT-TBDELSEC-ROWCNT*/
          if (programCtx.isProgramEnded()) {
              return methodOut;
          }

// *
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          programCtx.setProgramEnded(true);
          return methodOut;
      
      }
      /**
      * insertIntoTbdelsec 
      *   This method is derived from 
  *   COBOL Paragraph - 1000-INSERT-INTO-TBDELSEC COBOL Cyclomatic complexity - 4
      * Input  :  

      * - sqlcode                        COBOL Name: SQLCODE
      *
      * Output :  

      * - sqlcode_Ws                     COBOL Name: WS-SQLCODE
      * - sqlcode                        COBOL Name: SQLCODE
      *
      * @throws CFException
      */
      @Override
      public InsertIntoTbdelsecOutCtx insertIntoTbdelsec(InsertIntoTbdelsecInCtx methodIn) throws Exception {
DlcrntofCtx programCtx = methodIn.getDlcrntofCtx();
InsertIntoTbdelsecOutCtx methodOut = methodIn.getInsertIntoTbdelsecOutCtx();
//  INSERT INTO TBDELSEC SELECT * FROM TBDEMSEC
          dlcrntofRepository.insertTbdemsec(programCtx.getSqlca());

// *
          // MOVE SQLCODE TO WS-SQLCODE
          //  FORMAT1016334848 = "----"
          methodOut.setSqlcode_Ws(CFUtil.cobolNumberFormatter(CONSTANTS.FORMAT1016334848,String.valueOf(methodOut.getSqlcode()).toCharArray()));
//  IF SQLCODE = 0 OR -803 THEN
//  ELSE
          if (	( methodOut.getSqlcode() != 0 ) && 	( methodOut.getSqlcode() != -803 )) { 
//  DISPLAY 'INSERT TO TBDELSEC FAILED : SQLCODE = ' WS-SQLCODE
              logger.info("INSERT TO TBDELSEC FAILED : SQLCODE = {}", new String(methodOut.getSqlcode_Ws())); 
//  PERFORM 9999-RAISE-ERROR
              raiseError(programCtx);/*9999-RAISE-ERROR*/
              if (programCtx.isProgramEnded()) {
                  return methodOut;
              }
          }
      
      return methodOut;
      }
      /**
      * deleteTbdelsecRows 
      *   This method is derived from 
  *   COBOL Paragraph - 2000-DELETE-TBDELSEC-ROWS COBOL Cyclomatic complexity - 15
      * Input  :  

      * - sqlcode                        COBOL Name: SQLCODE
      * - deleteCnt                      COBOL Name: WS-DELETE-CNT
      * - recordsLoaded400               COBOL Name: 400-RECORDS-LOADED
      * - chkpFreq                       COBOL Name: WS-CHKP-FREQ
      *
      * Output :  

      * - isin                           COBOL Name: ISIN
      * - sqlcode_Ws                     COBOL Name: WS-SQLCODE
      * - sqlcode                        COBOL Name: SQLCODE
      * - deleteCnt                      COBOL Name: WS-DELETE-CNT
      * - recordsLoaded400               COBOL Name: 400-RECORDS-LOADED
      * - isinDeleted                    COBOL Name: ISIN-DELETED
      *
      * @throws CFException
      */
      @Override
      public DeleteTbdelsecRowsOutCtx deleteTbdelsecRows(DeleteTbdelsecRowsInCtx methodIn) throws Exception {
DlcrntofCtx programCtx = methodIn.getDlcrntofCtx();
DeleteTbdelsecRowsOutCtx methodOut = methodIn.getDeleteTbdelsecRowsOutCtx();
//  MOVE 'IG0000000000' TO ISIN
//  LITERAL_IG0000000000 = 'IG0000000000'
          methodOut.setIsin(CONSTANTS.LITERAL_IG0000000000);
//  SELECT ISIN , SEDOL , RIC , INTLKEY , COMPANY , COUNTRY FROM TBDELSEC WHERE ISIN <> ?
          programCtx.setSecCursorResultSet(dlcrntofRepository.openSecCursorDlcrntof(methodOut.getDcltbdelsec(),programCtx.getSqlca()));

// *
          // MOVE SQLCODE TO WS-SQLCODE
          //  FORMAT1016334848 = "----"
          methodOut.setSqlcode_Ws(CFUtil.cobolNumberFormatter(CONSTANTS.FORMAT1016334848,String.valueOf(methodOut.getSqlcode()).toCharArray()));
//  IF SQLCODE = 0 THEN
//  ELSE
          if (	( methodOut.getSqlcode() != 0 )) { 
//  DISPLAY 'OPEN SEC_CURSOR  FAILED : SQLCODE = ' WS-SQLCODE
              logger.info("OPEN SEC_CURSOR  FAILED : SQLCODE = {}", new String(methodOut.getSqlcode_Ws())); 
//  PERFORM 9999-RAISE-ERROR
              raiseError(programCtx);/*9999-RAISE-ERROR*/
              if (programCtx.isProgramEnded()) {
                  return methodOut;
              }
          }

// *
//  PERFORM UNTIL SQLCODE NOT = 0
          while ((	( methodOut.getSqlcode() == 0 ))) {
//  FETCH SEC_CURSOR INTO ? , ? , ? , ? , ? , ?
              dlcrntofRepository.fetchSecCursorDlcrntof(programCtx.getSecCursorResultSet(),methodOut.getDcltbdelsec(),programCtx.getSqlca());
              // MOVE SQLCODE TO WS-SQLCODE
              //  FORMAT1016334848 = "----"
              methodOut.setSqlcode_Ws(CFUtil.cobolNumberFormatter(CONSTANTS.FORMAT1016334848,String.valueOf(methodOut.getSqlcode()).toCharArray()));

// *
//  EVALUATE SQLCODE
              switch(methodOut.getSqlcode()){
              	case 0:
//  DELETE FROM TBDELSEC WHERE CURRENT OF SEC_CURSOR
                  dlcrntofRepository.deleteTbdelsec1(programCtx.getSecCursorResultSet(),programCtx.getSqlca());
                  // MOVE SQLCODE TO WS-SQLCODE
                  //  FORMAT1016334848 = "----"
                  methodOut.setSqlcode_Ws(CFUtil.cobolNumberFormatter(CONSTANTS.FORMAT1016334848,String.valueOf(methodOut.getSqlcode()).toCharArray()));
//  IF SQLCODE = 0 THEN
                  if (	( methodOut.getSqlcode() == 0 )) { 
//  ADD 1 TO WS-DELETE-CNT
                      methodOut.setDeleteCnt( (short) (methodOut.getDeleteCnt()+(short)1));
//  ADD 1 TO 400-RECORDS-LOADED
                      methodOut.setRecordsLoaded400(methodOut.getRecordsLoaded400()+1);
//  MOVE ISIN TO ISIN-DELETED ( 400-RECORDS-LOADED )
                      methodOut.setIsinDeleted(methodOut.getRecordsLoaded400() - 1,methodOut.getIsin());
//  IF WS-DELETE-CNT = WS-CHKP-FREQ
                      if (	( methodOut.getDeleteCnt() == methodIn.getChkpFreq() )) { 
//  DISPLAY 'Intermediate COMMIT...'
                          logger.info("Intermediate COMMIT..."); 
//  COMMIT
                          try {
                          	// COMMIT
                          	// reset SQLCODE
                          	methodOut.setSqlcode(0);
                             // execute jdbc commit
                             db2Base.commit();
                          }
                           catch (SQLException e) {
                                     methodOut.setSqlcode(Db2Base.fillSQLCode(e.getMessage()));
                                 }
                           catch(Exception e) {
                             handleErrorCode(e);
                          }
                          // MOVE 0 TO WS-DELETE-CNT
                          methodOut.setDeleteCnt((short)0);
                      }
                  }
//  ELSE
                  else { 
//  DISPLAY 'DELETE CURRENT OF SEC_CURSOR' ' FAILED : SQLCODE = ' WS-SQLCODE
                      logger.info("DELETE CURRENT OF SEC_CURSOR FAILED : SQLCODE = {}", new String(methodOut.getSqlcode_Ws())); 
//  PERFORM 9999-RAISE-ERROR
                      raiseError(programCtx);/*9999-RAISE-ERROR*/
                      if (programCtx.isProgramEnded()) {
                          return methodOut;
                      }
                  }
              break;
              	case 100:
              break;
              default :
//  DISPLAY 'FETCH SEC_CURSOR FAILED : SQLCODE = ' WS-SQLCODE
                  logger.info("FETCH SEC_CURSOR FAILED : SQLCODE = {}", new String(methodOut.getSqlcode_Ws())); 
//  PERFORM 9999-RAISE-ERROR
                  raiseError(programCtx);/*9999-RAISE-ERROR*/
                  if (programCtx.isProgramEnded()) {
                      return methodOut;
                  }
              }
          }
//  CLOSE SEC_CURSOR
          dlcrntofRepository.closeSecCursorDlcrntof(programCtx.getSecCursorResultSet(),programCtx.getSqlca());
          // MOVE SQLCODE TO WS-SQLCODE
          //  FORMAT1016334848 = "----"
          methodOut.setSqlcode_Ws(CFUtil.cobolNumberFormatter(CONSTANTS.FORMAT1016334848,String.valueOf(methodOut.getSqlcode()).toCharArray()));

// *
//  IF SQLCODE = 0 THEN
//  ELSE
          if (	( methodOut.getSqlcode() != 0 )) { 
//  DISPLAY 'CLOSE SEC_CURSOR FAILED : SQLCODE = ' WS-SQLCODE
              logger.info("CLOSE SEC_CURSOR FAILED : SQLCODE = {}", new String(methodOut.getSqlcode_Ws())); 
//  PERFORM 9999-RAISE-ERROR
              raiseError(programCtx);/*9999-RAISE-ERROR*/
              if (programCtx.isProgramEnded()) {
                  return methodOut;
              }
          }

// *
//  DISPLAY 'Final COMMIT...'
          logger.info("Final COMMIT..."); 
//  COMMIT
          try {
          	// COMMIT
          	// reset SQLCODE
          	methodOut.setSqlcode(0);
             // execute jdbc commit
             db2Base.commit();
          }
           catch (SQLException e) {
                     methodOut.setSqlcode(Db2Base.fillSQLCode(e.getMessage()));
                 }
           catch(Exception e) {
             handleErrorCode(e);
          }
          ;
      
      return methodOut;
      }
      /**
      * printDeletedIsin 
      *   This method is derived from 
  *   COBOL Paragraph - 3000-PRINT-DELETED-ISIN COBOL Cyclomatic complexity - 3
      * Input  :  

      * - isinIdx                        COBOL Name: ISIN-IDX
      * - recordsLoaded400               COBOL Name: 400-RECORDS-LOADED
      * - isinEntry                      COBOL Name: ISIN-ENTRY
      *
      * Output :  

      * - isinIdx                        COBOL Name: ISIN-IDX
      *
      * @throws CFException
      */
      @Override
      public PrintDeletedIsinOutCtx printDeletedIsin(PrintDeletedIsinInCtx methodIn) throws Exception {
DlcrntofCtx programCtx = methodIn.getDlcrntofCtx();
PrintDeletedIsinOutCtx methodOut = methodIn.getPrintDeletedIsinOutCtx();
//  SORT ISIN-ENTRY ASCENDING ISIN-DELETED
          try {
              methodIn.getIsinEntry().sort(IsinEntry.getIsinDeletedComparator(true/*isAscending*/));
          } catch(Exception e) {
              logger.error("{}",e);
          }	
//  DISPLAY 'Following Securities Deleted.'
          logger.info("Following Securities Deleted."); 
//  DISPLAY '-----------------------------'
          logger.info("-----------------------------"); 
//  PERFORM VARYING ISIN-IDX FROM 1 BY 1 UNTIL ISIN-IDX > 400-RECORDS-LOADED
          for (methodOut.setIsinIdx(1); (	( methodOut.getIsinIdx() <= methodIn.getRecordsLoaded400() )) ; methodOut.setIsinIdx(methodOut.getIsinIdx() + 1) ) {
//  DISPLAY '   ' ISIN-ENTRY ( ISIN-IDX )
              logger.info("   {}", methodIn.getIsinEntry(methodOut.getIsinIdx() - 1).toString()); 
          }
//  DISPLAY ' '
          logger.info(" "); 
      
      return methodOut;
      }
      /**
      * printTbdelsecRowcnt 
      *   This method is derived from 
  *   COBOL Paragraph - 9000-PRINT-TBDELSEC-ROWCNT COBOL Cyclomatic complexity - 3
      * Input  :  

      * - sqlcode                        COBOL Name: SQLCODE
      * - secnt                          COBOL Name: WS-SECNT
      *
      * Output :  

      * - sqlcode_Ws                     COBOL Name: WS-SQLCODE
      * - sqlcode                        COBOL Name: SQLCODE
      * - dispSecnt                      COBOL Name: WS-DISP-SECNT
      * - secnt                          COBOL Name: WS-SECNT
      *
      * @throws CFException
      */
      @Override
      public PrintTbdelsecRowcntOutCtx printTbdelsecRowcnt(PrintTbdelsecRowcntInCtx methodIn) throws Exception {
DlcrntofCtx programCtx = methodIn.getDlcrntofCtx();
PrintTbdelsecRowcntOutCtx methodOut = methodIn.getPrintTbdelsecRowcntOutCtx();
//  SELECT COUNT ( * ) FROM TBDELSEC
          dlcrntofRepository.selectTbdelsec(programCtx.getSqlca(),methodOut.getWork());

// *
          // MOVE SQLCODE TO WS-SQLCODE
          //  FORMAT1016334848 = "----"
          methodOut.setSqlcode_Ws(CFUtil.cobolNumberFormatter(CONSTANTS.FORMAT1016334848,String.valueOf(methodOut.getSqlcode()).toCharArray()));
//  IF SQLCODE = 0 THEN
//  ELSE
          if (	( methodOut.getSqlcode() != 0 )) { 
//  DISPLAY 'SELECT COUNT(*)  FAILED : SQLCODE = ' WS-SQLCODE
              logger.info("SELECT COUNT(*)  FAILED : SQLCODE = {}", new String(methodOut.getSqlcode_Ws())); 
//  PERFORM 9999-RAISE-ERROR
              raiseError(programCtx);/*9999-RAISE-ERROR*/
              if (programCtx.isProgramEnded()) {
                  return methodOut;
              }
          }

// *
//  MOVE WS-SECNT TO WS-DISP-SECNT
//  FORMAT_1059277505 = "ZZZ9"
          methodOut.setDispSecnt(CFUtil.cobolNumberFormatter(CONSTANTS.FORMAT_1059277505,String.valueOf(methodOut.getSecnt()).toCharArray()));
//  DISPLAY '# OF SECURITIES = ' WS-DISP-SECNT
          logger.info("# OF SECURITIES = {}", new String(methodOut.getDispSecnt())); 
      
      return methodOut;
      }
      /**
      * raiseError 
      *   This method is derived from 
  *   COBOL Paragraph - 9999-RAISE-ERROR COBOL Cyclomatic complexity - 3
      * Input  : None 

      * Output :  

      * - rc                             COBOL Name: RETURN-CODE
      *
      * @throws CFException
      */
      @Override
      public RaiseErrorOutCtx raiseError(DlcrntofCtx programCtx) throws Exception {
RaiseErrorOutCtx methodOut = programCtx.getRaiseErrorOutCtx();
          // MOVE 16 TO RETURN-CODE
          programCtx.setRc( 16);
//  DELETE FROM TBDELSEC WHERE ISIN NOT LIKE 'IG%'
          dlcrntofRepository.deleteTbdelsec2(programCtx.getSqlca());
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          programCtx.setProgramEnded(true);
          return methodOut;
      
      }
  
  
  
  
  
  
  
  
  }
