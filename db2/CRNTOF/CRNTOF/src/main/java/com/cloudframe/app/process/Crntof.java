  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-28 at 07:03. */
  
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.exception.Terminate;
  import org.springframework.beans.factory.annotation.Autowired;
  import org.springframework.beans.factory.annotation.Qualifier;
  import com.cloudframe.app.repository.CrntofRepository;
  import com.cloudframe.app.crntof.dto.*;
  import com.cloudframe.app.crntof.dto.Sqlca;
  import com.cloudframe.app.crntof.dto.CrntofTable;
  import com.cloudframe.app.crntof.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.common.SQLS;
  import com.cloudframe.app.utility.CFUtil;
  import org.springframework.beans.factory.annotation.Value;
  import com.cloudframe.app.dao.Db2Base;
  import java.sql.SQLException;
  
  @Component("crntof")
  
  public class Crntof extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Crntof.class);
  
  private Sqlca sqlca = new Sqlca() ;
  private CrntofTable crntofTable = new CrntofTable() ;
  private Work work = new Work() ;
  
  @Value("${CRNTOF.dbQualifier:}")
  private String dbQualifier;
  
  
  @Autowired 
  @Qualifier("db2Base")
  Db2Base db2Base;
  @Autowired 
  @Qualifier("crntofRepository")
  CrntofRepository crntofRepository;
  
  
  
  
  
  
      /**
      * process 
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");
          ;
//  PERFORM 0000-MAINLINE
          mainline();/*0000-MAINLINE*/
          if (this.isProgramEnded()) {
              return getRc();
          }
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
        finally {
		handleDbAtEnd(db2Base); 
		clearVars(); 
      

      }
      
       return getRc(); // Exit with return code
      // end of process method
      }
      /**
      * mainline 
      *   This method is derived from 
  *   COBOL Paragraph - 0000-MAINLINE COBOL Cyclomatic complexity - 2
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      private void mainline() throws Exception {
//  DISPLAY "Executing CURRENT OF Case"
          logger.info("Executing CURRENT OF Case"); 
//  PERFORM 1000-EXECUTE-QUERY
          executeQuery();/*1000-EXECUTE-QUERY*/
          if (this.isProgramEnded()) {
              return ;
          }
//  PERFORM 2000-FETCH-CURSOR
          fetchCursor();/*2000-FETCH-CURSOR*/
          if (this.isProgramEnded()) {
              return ;
          }
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          this.setProgramEnded(true);
          return ;
      
      }
      /**
      * executeQuery 
      *   This method is derived from 
  *   COBOL Paragraph - 1000-EXECUTE-QUERY COBOL Cyclomatic complexity - 27
      * Input  :  

      * - sqlcode                        COBOL Name: SQLCODE
      *
      * Output :  

      * - crntofName01                   COBOL Name: CRNTOF-NAME
      * - crntofNameN                    COBOL Name: CRNTOF-NAME-N
      * - crntofName                     COBOL Name: WS-CRNTOF-NAME
      *
      * @throws CFException
      */
      private void executeQuery() throws Exception {
			// Declare local variables used in the method
			int sqlcode = 0;
			// End of variable declaration

//  DISPLAY "OPENING PMA Cursor"
          logger.info("OPENING PMA Cursor"); 
//  SELECT CRNTOFID , CRNTOFNAME FROM CRNTOF FOR UPDATE OF CRNTOFNAME
          crntofRepository.openPmaCursorCrntof(sqlca);
//  PERFORM UNTIL SQLCODE NOT EQUAL 0
          while ((	( sqlca.getSqlcode() == 0 ))) {
//  FETCH PMA_CURSOR INTO ? , ?
              crntofRepository.fetchPmaCursorCrntof(crntofTable,sqlca);
              sqlcode = sqlca.getSqlcode();
//  IF SQLCODE = 0 THEN
              if (	( sqlcode == 0 )) { 
//  EVALUATE CRNTOF-NAME
                  switch(new String(crntofTable.getCrntofName01())){
                  	case "cobol ":
//  MOVE 'java' TO CRNTOF-NAME
                      crntofTable.setCrntofName01(CONSTANTS.LITERAL_java_B2_);
//  MOVE 0 TO CRNTOF-NAME-N
                      crntofTable.setCrntofNameN((short)0);
//  UPDATE CRNTOF SET CRNTOFNAME = ? WHERE CURRENT OF PMA_CURSOR
                      crntofRepository.updateCrntof2(crntofTable,sqlca);
                      sqlcode = sqlca.getSqlcode();
//  IF SQLCODE = 0 THEN
                      if (	( sqlcode == 0 )) { 
//  DISPLAY 'SUCCESS'
                          logger.info("SUCCESS"); 
                      }
  
//  ELSE
                      else { 
//  DISPLAY 'FAIL : ' SQLCODE
                          logger.info("FAIL : {}", String.valueOf(sqlca.getSqlcode())); 
                      }
                  break;
                  	case "java  ":
//  MOVE 'cobol' TO CRNTOF-NAME
                      crntofTable.setCrntofName01(CONSTANTS.LITERAL_cobol_B_);
//  MOVE 0 TO CRNTOF-NAME-N
                      crntofTable.setCrntofNameN((short)0);
//  UPDATE CRNTOF SET CRNTOFNAME = ? WHERE CURRENT OF PMA_CURSOR
                      crntofRepository.updateCrntof3(crntofTable,sqlca);
                      sqlcode = sqlca.getSqlcode();
//  IF SQLCODE = 0 THEN
                      if (	( sqlcode == 0 )) { 
//  DISPLAY 'SUCCESS'
                          logger.info("SUCCESS"); 
                      }
  
//  ELSE
                      else { 
//  DISPLAY 'FAIL : ' SQLCODE
                          logger.info("FAIL : {}", String.valueOf(sqlca.getSqlcode())); 
                      }
                  break;
                  default :
                      // MOVE CRNTOF-NAME TO WS-CRNTOF-NAME
                      work.setCrntofName(crntofTable.getCrntofName01());
                  }
              }
  
//  ELSE
              else { 
//  DISPLAY "SQL CODE " SQLCODE
                  logger.info("SQL CODE {}", String.valueOf(sqlca.getSqlcode())); 
              }
          }
//  CLOSE PMA_CURSOR
          crntofRepository.closePmaCursorCrntof(sqlca);
//  COMMIT
          try {
          	// COMMIT
          	// reset SQLCODE
          	sqlca.setSqlcode(0);
             // execute jdbc commit
             db2Base.commit();
          }
           catch (SQLException e) {
                     sqlca.setSqlcode(Db2Base.fillSQLCode(e.getMessage()));
                 }
           catch(Exception e) {
             handleErrorCode(e);
          }
//  DISPLAY "OPENING PMA Cursor2"
          logger.info("OPENING PMA Cursor2"); 
//  SELECT CRNTOFID , CRNTOFNAME FROM CRNTOF WHERE CRNTOFNAME IN ( 'COBOL' , 'JAVA' ) FOR UPDATE OF CRNTOFNAME
          crntofRepository.openPmaCursor2Crntof(sqlca);
//  PERFORM UNTIL SQLCODE NOT EQUAL 0
          while ((	( sqlca.getSqlcode() == 0 ))) {
//  FETCH PMA_CURSOR2 INTO ?
              crntofRepository.fetchPmaCursor2Crntof(crntofTable,sqlca);
              sqlcode = sqlca.getSqlcode();
//  IF SQLCODE = 0 THEN
              if (	( sqlcode == 0 )) { 
//  EVALUATE WS-CRNTOF-NAME
                  switch(new String(work.getCrntofName())){
                  	case "COBOL ":
//  MOVE 'JAVA' TO CRNTOF-NAME
                      crntofTable.setCrntofName01(CONSTANTS.LITERAL_JAVA_B2_);
//  MOVE 0 TO CRNTOF-NAME-N
                      crntofTable.setCrntofNameN((short)0);
//  UPDATE CRNTOF SET CRNTOFNAME = ? WHERE CURRENT OF PMA_CURSOR2
                      crntofRepository.updateCrntof6(crntofTable,sqlca);
                      sqlcode = sqlca.getSqlcode();
//  IF SQLCODE = 0 THEN
                      if (	( sqlcode == 0 )) { 
//  DISPLAY 'SUCCESS'
                          logger.info("SUCCESS"); 
                      }
  
//  ELSE
                      else { 
//  DISPLAY 'FAIL : ' SQLCODE
                          logger.info("FAIL : {}", String.valueOf(sqlca.getSqlcode())); 
                      }
                  break;
                  	case "JAVA  ":
//  MOVE 'COBOL' TO CRNTOF-NAME
                      crntofTable.setCrntofName01(CONSTANTS.LITERAL_COBOL_B_);
//  MOVE 0 TO CRNTOF-NAME-N
                      crntofTable.setCrntofNameN((short)0);
//  UPDATE CRNTOF SET CRNTOFNAME = ? WHERE CURRENT OF PMA_CURSOR2
                      crntofRepository.updateCrntof7(crntofTable,sqlca);
                      sqlcode = sqlca.getSqlcode();
//  IF SQLCODE = 0 THEN
                      if (	( sqlcode == 0 )) { 
//  DISPLAY 'SUCCESS'
                          logger.info("SUCCESS"); 
                      }
  
//  ELSE
                      else { 
//  DISPLAY 'FAIL : ' SQLCODE
                          logger.info("FAIL : {}", String.valueOf(sqlca.getSqlcode())); 
                      }
                  break;
                  }
              }
  
//  ELSE
              else { 
//  DISPLAY "SQL CODE " SQLCODE
                  logger.info("SQL CODE {}", String.valueOf(sqlca.getSqlcode())); 
              }
          }
//  CLOSE PMA_CURSOR2
          crntofRepository.closePmaCursor2Crntof(sqlca);
//  COMMIT
          try {
          	// COMMIT
          	// reset SQLCODE
          	sqlca.setSqlcode(0);
             // execute jdbc commit
             db2Base.commit();
          }
           catch (SQLException e) {
                     sqlca.setSqlcode(Db2Base.fillSQLCode(e.getMessage()));
                 }
           catch(Exception e) {
             handleErrorCode(e);
          }
      
      }
      /**
      * fetchCursor 
      *   This method is derived from 
  *   COBOL Paragraph - 2000-FETCH-CURSOR COBOL Cyclomatic complexity - 10
      * Input  :  

      * - sqlcode                        COBOL Name: SQLCODE
      *
      * Output : None 

      * @throws CFException
      */
      private void fetchCursor() throws Exception {
      
// * Following para was added for cffix-210 insensitive scroll
//  DISPLAY "OPENING PMA Cursor 3"
          logger.info("OPENING PMA Cursor 3"); 
//  SELECT CRNTOFID FROM CRNTOF WHERE CRNTOFNAME IN ( 'COBOL' , 'JAVA' )
          crntofRepository.openPmaCursor3Crntof(sqlca);
//  EVALUATE SQLCODE
          switch(sqlca.getSqlcode()){
          	case 0:
//  DISPLAY "Successfully opened cursor"
              logger.info("Successfully opened cursor"); 
          break;
          default :
//  DISPLAY "Unable to open cursor 3 " SQLCODE
              logger.info("Unable to open cursor 3 {}", String.valueOf(sqlca.getSqlcode())); 
          }
//  FETCH PMA_CURSOR3 INTO ?
          crntofRepository.fetchPmaCursor3Crntof(crntofTable,sqlca);
//  EVALUATE SQLCODE
          switch(sqlca.getSqlcode()){
          	case 0:
          
          	case 100:
//  DISPLAY "Successfully executed fetch"
              logger.info("Successfully executed fetch"); 
          break;
          default :
//  DISPLAY "Unable to fetch from cursor 3 : " SQLCODE
              logger.info("Unable to fetch from cursor 3 : {}", String.valueOf(sqlca.getSqlcode())); 
          }
//  CLOSE PMA_CURSOR3
          crntofRepository.closePmaCursor3Crntof(sqlca);
      
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          db2Base.reset("CRNTOF" ,dbQualifier, true/*use Dynamic SQL*/);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
  
  
  
  
  
  }
