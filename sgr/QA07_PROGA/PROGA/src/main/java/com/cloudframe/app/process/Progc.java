  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-21 at 12:08. */
  
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.utility.CFUtil;
  import com.cloudframe.app.progc.dto.*;
  import com.cloudframe.app.progc.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  
  @Component("progc")
  
  public class Progc extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Progc.class);
  
  private Work work = new Work() ;
  
  
  
  
  
  
  
  
  
      /**
      * process 
      * Input  :  

      * - rc                             COBOL Name: RETURN-CODE
      * - lsReturnCode                   COBOL Name: LS-RETURN-CODE
      *
      * Output : None 

      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");
//  DISPLAY 'INSIDE PROGRAMC WITH RETURN CODE ' RETURN-CODE
          logger.info("INSIDE PROGRAMC WITH RETURN CODE {}", String.valueOf(getRc())); 

// *    Move 12 to ls-return-code
//  DISPLAY 'INSIDE PROGRAMC WITH LS-RETURN CODE ' LS-RETURN-CODE
          logger.info("INSIDE PROGRAMC WITH LS-RETURN CODE {}", String.valueOf(work.getLsReturnCode())); 
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          this.setProgramEnded(true);
          return getRc();
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
      
      // end of process method
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
      public int call(Object[] params) throws Exception {
      
      int len = params.length;
            work.setLsReturnCode(CFUtil.getInt((byte[]) params[0],0));
         // invoke the process and return rc
         return process();
         
      }
      
  
  
  
  
  
  }
