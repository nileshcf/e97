  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-29 at 07:30. */
  
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import org.springframework.web.bind.annotation.RequestParam;
  import com.cloudframe.app.callfd02.dto.InputKey;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.data.Field;
  import com.cloudframe.app.callfd02.dto.*;
  import com.cloudframe.app.callfd02.dto.ParmData;
  import com.cloudframe.app.callfd02.dto.InputRec;
  import com.cloudframe.app.callfd02.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("callfd02")
  
  public class Callfd02 extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Callfd02.class);
  
  private ParmData parmData = new ParmData() ;
  private InputRec inputRec = new InputRec() ;
  private Work work = new Work() ;
  
  
  
  
  
  
  
  
  
      public int setParameter(String parmData,String inputRec) throws Exception {
      		if(parmData != null)
      		    this.parmData.setString(com.cloudframe.app.data.Field.getParm(parmData),new String(CONSTANTS.EBCDIC_ENCODING));
      		if(inputRec != null)
      		    this.inputRec.setString(com.cloudframe.app.data.Field.getParm(inputRec),new String(CONSTANTS.EBCDIC_ENCODING));
      		setInitDone(false);
      		process();
      		return getRc();
      }
      /**
      * process 
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");
          ;
//  PERFORM 0000-MAINLINE
          mainline();/*0000-MAINLINE*/
          if (this.isProgramEnded()) {
              return getRc();
          }
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
      
       return getRc(); // Exit with return code
      // end of process method
      }
      /**
      * mainline 
      *   This method is derived from 
  *   COBOL Paragraph - 0000-MAINLINE COBOL Cyclomatic complexity - 2
      * Input  :  

      * - parmMarker                     COBOL Name: LK-PARM-MARKER
      *
      * Output :  

      * - ioType                         COBOL Name: LK-IO-TYPE
      * - inputMarker                    COBOL Name: LK-INPUT-MARKER
      * - parmMarker                     COBOL Name: LK-PARM-MARKER
      *
      * @throws CFException
      */
      private void mainline() throws Exception {
			// Declare local variables used in the method
			InputKey inputKey = inputRec.getInputKey();
			// End of variable declaration

      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
//  MOVE 'OT' TO LK-IO-TYPE
//  LITERAL_OT = 'OT'
          inputKey.setIoType(CONSTANTS.LITERAL_OT);
  
//  MOVE LK-PARM-MARKER TO LK-INPUT-MARKER
          inputRec.setInputMarker(parmData.getParmMarker());
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          this.setProgramEnded(true);
          return ;
      
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
      public int call(Object[] params) throws Exception {
      
      int len = params.length;
         if (len > 0 && params[0] != null )
            parmData.set((Field)params[0]);
         if (len > 1 && params[1] != null )
            inputRec.set((Field)params[1]);
         // invoke the process and return rc
         return process();
         
      }
      
      public int call(Field... parameters) throws Exception {
         for (int index = 0; index < parameters.length;index++) {
             switch(index) {
              case 0:
                      if(parameters[index] != null ) {
              		if (parameters[index] instanceof ParmData) {
                       	this.parmData = ((ParmData) parameters[index]);
                  	} else {
                       	this.parmData.set(parameters[index]);
                  	}
                  }
                
                  break;
              case 1:
                      if(parameters[index] != null ) {
              		if (parameters[index] instanceof InputRec) {
                       	this.inputRec = ((InputRec) parameters[index]);
                  	} else {
                       	this.inputRec.set(parameters[index]);
                  	}
                  }
                
                  break;
            }
         }
          return process();
      }
      
      
  
  
  
  
  
  }
