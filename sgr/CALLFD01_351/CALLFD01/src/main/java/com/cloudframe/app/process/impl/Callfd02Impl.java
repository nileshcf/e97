  package com.cloudframe.app.process.impl;
  /* Java code generated by CloudFrame on 2025-04-24 at 16:45. */
  
  import com.cloudframe.app.callfd02.Callfd02Ctx.*;
  import com.cloudframe.app.callfd02.Callfd02Ctx;
  import com.cloudframe.app.process.Callfd02;
  import com.cloudframe.app.process.BaseProcess;
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import org.springframework.web.bind.annotation.RequestParam;
  import com.cloudframe.app.dto.GlobalExecutorCtx;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.dto.ProgramContext;
  import com.cloudframe.app.data.Field;
  import com.cloudframe.app.callfd02.dto.*;
  import com.cloudframe.app.callfd02.dto.ParmData;
  import com.cloudframe.app.callfd02.dto.InputRec;
  import com.cloudframe.app.callfd02.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("callfd02")
  
  public class Callfd02Impl extends CommonProcess implements Callfd02 {
  
  Logger logger = LoggerFactory.getLogger(Callfd02Impl.class);
  
  
  
  
  
  
  
  
  
  
      @Override
      public int setParameter(Callfd02Ctx programCtx, String parmData,String inputRec) throws Exception {
      		if(parmData != null)
      		    programCtx.getParmData().setString(com.cloudframe.app.data.Field.getParm(parmData),new String(CONSTANTS.EBCDIC_ENCODING));
      		if(inputRec != null)
      		    programCtx.getInputRec().setString(com.cloudframe.app.data.Field.getParm(inputRec),new String(CONSTANTS.EBCDIC_ENCODING));
      		setInitDone(false);
      		process(programCtx);
      		return programCtx.getRc();
      }
      /**
      * process 
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      public int process(Callfd02Ctx programCtx) throws Exception {
       try {
       setCodePage("1047");
            // Reset program ended flag
           programCtx.setProgramEnded(false);
ProcessInCtx methodIn = programCtx.getProcessInCtx();
          ;
//  PERFORM 0000-MAINLINE
          mainline(programCtx.getMainlineInCtx());/*0000-MAINLINE*/
          if (programCtx.isProgramEnded()) {
              return programCtx.getRc();
          }
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
      
       return programCtx.getRc(); // Exit with return code
      // end of process method
      }
      /**
      * mainline 
      *   This method is derived from 
  *   COBOL Paragraph - 0000-MAINLINE COBOL Cyclomatic complexity - 2
      * Input  :  

      * - parmMarker                     COBOL Name: LK-PARM-MARKER
      *
      * Output :  

      * - ioType                         COBOL Name: LK-IO-TYPE
      * - inputMarker                    COBOL Name: LK-INPUT-MARKER
      * - parmMarker                     COBOL Name: LK-PARM-MARKER
      *
      * @throws CFException
      */
      @Override
      public MainlineOutCtx mainline(MainlineInCtx methodIn) throws Exception {
      
// *----------------------------------------------------------------*

// *----------------------------------------------------------------*
Callfd02Ctx programCtx = methodIn.getCallfd02Ctx();
MainlineOutCtx methodOut = methodIn.getMainlineOutCtx();
//  MOVE 'OT' TO LK-IO-TYPE
//  LITERAL_OT = 'OT'
          methodOut.setIoType(CONSTANTS.LITERAL_OT);
//  MOVE LK-PARM-MARKER TO LK-INPUT-MARKER
          methodOut.setInputMarker(methodOut.getParmMarker());
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          programCtx.setProgramEnded(true);
          return methodOut;
      
      }
  
  
  
      public int call(ProgramContext ctx, Object[] params) throws Exception {
      Callfd02Ctx programCtx = (Callfd02Ctx) ctx;
      
      int len = params.length;
         if (len > 0 && params[0] != null )
            programCtx.getParmData().set((Field)params[0]);
         if (len > 1 && params[1] != null )
            programCtx.getInputRec().set((Field)params[1]);
         // invoke the process and return rc
         return process(programCtx);
         
      }
      
      public int call(ProgramContext ctx, Field... parameters) throws Exception {
      Callfd02Ctx programCtx = (Callfd02Ctx) ctx;
         for (int index = 0; index < parameters.length;index++) {
             switch(index) {
              case 0:
                      if(parameters[index] != null ) {
              		if (parameters[index] instanceof ParmData) {
                       	programCtx.setParmData((ParmData) parameters[index]);
                  	} else {
                       	programCtx.getParmData().set(parameters[index]);
                  	}
                  }
                
                  break;
              case 1:
                      if(parameters[index] != null ) {
              		if (parameters[index] instanceof InputRec) {
                       	programCtx.setInputRec((InputRec) parameters[index]);
                  	} else {
                       	programCtx.getInputRec().set(parameters[index]);
                  	}
                  }
                
                  break;
            }
         }
      	return process(programCtx);
      }
      
      
  
  
  
  
  
  }
