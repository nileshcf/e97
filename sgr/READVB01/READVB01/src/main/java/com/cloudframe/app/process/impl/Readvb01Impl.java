  package com.cloudframe.app.process.impl;
  /* Java code generated by CloudFrame on 2025-04-28 at 08:05. */
  
  import org.springframework.beans.factory.annotation.Autowired;
  import org.springframework.beans.factory.annotation.Qualifier;
  import com.cloudframe.app.global.shared.file.*;
  import com.cloudframe.app.readvb01.Readvb01Ctx.*;
  import com.cloudframe.app.readvb01.Readvb01Ctx;
  import com.cloudframe.app.process.Readvb01;
  import com.cloudframe.app.process.BaseProcess;
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.dto.GlobalExecutorCtx;
  import com.cloudframe.app.global.shared.file.records.*;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.process.Readvb02;
  import com.cloudframe.app.process.*;
  import com.cloudframe.app.global.sharedvar.MiscData;
  import com.cloudframe.app.readvb01.dto.*;
  import com.cloudframe.app.readvb01.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("readvb01")
  
  public class Readvb01Impl extends CommonProcess implements Readvb01 {
  
  Logger logger = LoggerFactory.getLogger(Readvb01Impl.class);
  
  
  
  
  @Autowired 
  @Qualifier("readvb02")
  Readvb02 readvb02;
  
  
  
  
  
  
      /**
      * process 
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      public int process(Readvb01Ctx programCtx) throws Exception {
       try {
       setCodePage("1047");
            // Reset program ended flag
           programCtx.setProgramEnded(false);
          ;
//  PERFORM 0000-MAINLINE
          mainline(programCtx.getMainlineInCtx());/*0000-MAINLINE*/
          if (programCtx.isProgramEnded()) {
              return programCtx.getRc();
          }
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
        finally {
      		if(xmlIn.hasOpened() && !xmlIn.isReadOnly()) { 
      			xmlIn.flush(); 
      		}
      }
      
       return programCtx.getRc(); // Exit with return code
      // end of process method
      }
      /**
      * mainline 
      *   This method is derived from 
  *   COBOL Paragraph - 0000-MAINLINE COBOL Cyclomatic complexity - 6
      * Input  :  

      * - xmlData                        COBOL Name: WS-XML-DATA
      *
      * Output :  

      * - miscData                       COBOL Name: WS-MISC-DATA
      * - xmlStat                        COBOL Name: XML-STAT
      * - rc                             COBOL Name: RETURN-CODE
      * - eofFlag                        COBOL Name: WS-EOF-FLAG
      * - recLenIn                       COBOL Name: WS-REC-LEN-IN
      * - xmlRecordMax                   COBOL Name: XML-RECORD-MAX
      *
      * @throws CFException
      */
      @Override
      public MainlineOutCtx mainline(MainlineInCtx methodIn) throws Exception {
Readvb01Ctx programCtx = methodIn.getReadvb01Ctx();
MainlineOutCtx methodOut = methodIn.getMainlineOutCtx();
//  INITIALIZE WS-MISC-DATA
          methodOut.getMiscData().initialize();
//  OPEN INPUT XML-IN
          xmlIn.open(new String(CONSTANTS.MODE_READ_ONLY_36242),xmlIn.getFileName(),xmlIn.getXmlInCharSet(),xmlIn.getXmlInCrlfFlag());
          methodOut.setXmlStat(xmlIn.getStatus() );
//  IF XML-STAT NOT = 0
          if (	( methodOut.getXmlStat() != 0 )) { 
              // MOVE 16 TO RETURN-CODE
              programCtx.setRc( 16);
//  DISPLAY 'Error Opening XMLIN File: ' XML-STAT
              logger.info("Error Opening XMLIN File: {}", String.valueOf(methodOut.getXmlStat())); 
//  GOBACK
              setNotLogged(false); // no need to log, it is a normal termination
              programCtx.setProgramEnded(true);
              return methodOut;
          }
//  DISPLAY 'Folling are the rows read back from VB file:'
          logger.info("Folling are the rows read back from VB file:"); 
//  DISPLAY '--------------------------------------------'
          logger.info("--------------------------------------------"); 
//  MOVE +0 TO WS-EOF-FLAG
          methodOut.setEofFlag((short)0);
//  PERFORM UNTIL NO-MORE-RECORDS
          while ((!(methodOut.isNoMoreRecords()) )) {
//  READ XML-IN INTO WS-XML-DATA
              	xmlIn.read();
              methodOut.setRecLenIn(xmlIn.getRecLen());
              methodOut.setXmlStat(xmlIn.getStatus() );
              if (!xmlIn.hasEnded()) {
                 methodOut.getXmlRecordMax().setString(xmlIn.getRecord());
                 methodIn.setXmlData(replace(methodIn.getXmlData(),substring(methodOut.getXmlRecordMax().toCharArray(),0,xmlIn.getRecLen()),0,methodIn.getXmlData().length));
              }
//  AT END
              if (xmlIn.hasEnded()) {  
//  SET NO-MORE-RECORDS TO TRUE
                  methodOut.setNoMoreRecordsTrue(); 
                  
                }
//  NOT AT END
              else  {
//  DISPLAY 'RECORD LENGTH = ' WS-REC-LEN-IN
                  logger.info("RECORD LENGTH = {}", String.valueOf(methodOut.getRecLenIn())); 
//  DISPLAY WS-XML-DATA (1 : WS-REC-LEN-IN )
                  logger.info(new String(substring(methodIn.getXmlData(),0,(int) methodOut.getRecLenIn()))); 
                }
//  IF NO-MORE-RECORDS
//  ELSE
              if (!(methodOut.isNoMoreRecords()) ) { 
//  CALL WS-READVB02
                  // CALL WS-READVB02
                  	programCtx.setRc( readvb02.process(programCtx.getGlobalCtx().getContext("READVB02")));
              }
          }
//  CLOSE XML-IN
          xmlIn.close(); 
          methodOut.setXmlStat(xmlIn.getStatus() );
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          programCtx.setProgramEnded(true);
          return methodOut;
      
      }
  
  
  
  
  
  
  
  
  }
