  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-23 at 10:09. */
  
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.c88comm.dto.*;
  import com.cloudframe.app.c88comm.dto.Group;
  import com.cloudframe.app.c88comm.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("c88comm")
  
  public class C88comm extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(C88comm.class);
  
  private Group group = new Group() ;
  private Work work = new Work() ;
  
  
  
  
  
  
  
  
  
      /**
      * process 
      * Input  : None 

      * Output : None 

      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");
          ;
//  PERFORM MAIN-PARA
          mainPara();/*MAIN-PARA*/
          if (this.isProgramEnded()) {
              return getRc();
          }
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
      
       return getRc(); // Exit with return code
      // end of process method
      }
      /**
      * mainPara 
      *   This method is derived from 
  *   COBOL Paragraph - MAIN-PARA COBOL Cyclomatic complexity - 2
      * Input  : None 

      * Output :  

      * - group                          COBOL Name: WS-GROUP
      * - numbers                        COBOL Name: WS-NUMBERS
      *
      * @throws CFException
      */
      private void mainPara() throws Exception {
      
// *
          // MOVE '1' TO WS-GROUP
          //  LITERAL_1 = '1'
          group.setString(CONSTANTS.LITERAL_1);
//  SET WS-ALL TO TRUE
          group.setAllTrue(); 
          
//  DISPLAY '*3* (' WS-NUMBERS ')'
          logger.info("*3* ({})", String.valueOf(group.getNumbers())); 
//  GOBACK
          setNotLogged(false); // no need to log, it is a normal termination
          this.setProgramEnded(true);
          return ;
      
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
        CFUtil.setDecimalAsComma();
       }
  
  
  
  
  
  
  }
