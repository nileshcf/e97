  package com.cloudframe.app.process;
  /* Java code generated by CloudFrame on 2025-04-29 at 07:26. */
  
  import org.springframework.web.bind.annotation.GetMapping;
  import org.slf4j.Logger;
  import org.slf4j.LoggerFactory;
  import com.cloudframe.app.exception.CFException;
  import org.springframework.stereotype.Component;
  import org.springframework.web.bind.annotation.RestController;
  import com.cloudframe.app.process.Called3;
  import org.springframework.beans.factory.annotation.Autowired;
  import org.springframework.beans.factory.annotation.Qualifier;
  import com.cloudframe.app.utility.SpringContextHandler;
  import com.cloudframe.app.exception.Terminate;
  import com.cloudframe.app.data.Field;
  import com.cloudframe.app.caller2.dto.*;
  import com.cloudframe.app.caller2.dto.Parm;
  import com.cloudframe.app.global.sharedvar.ExExternal;
  import com.cloudframe.app.caller2.dto.Work;
  import com.cloudframe.app.common.CONSTANTS;
  import com.cloudframe.app.utility.CFUtil;
  
  @Component("caller2")
  
  public class Caller2 extends CommonProcess {
  
  Logger logger = LoggerFactory.getLogger(Caller2.class);
  
  private Parm parm = new Parm() ;
  private Work work = new Work() ;
  
  
  
  @Autowired 
  @Qualifier("called3")
  Called3 called3;
  @Autowired 
  @Qualifier("global_exExternal")
  ExExternal exExternal;
  
  
  
  
  
  
      /**
      * process 
      * Input  : None 

      * Output :  

      * - exProgramName                  COBOL Name: EX-PROGRAM-NAME
      * - prog                           COBOL Name: WS-PROG
      * - rc                             COBOL Name: RETURN-CODE
      *
      * @throws CFException
      */
      @Override
      public int process() throws Exception {
       initVars();
       try {
       setCodePage("1047");

// *    Comment for the hello world 2
//  MOVE 'CALLED1' TO EX-PROGRAM-NAME
          exExternal.setExProgramName(CONSTANTS.LITERAL_CALLED1_B_);
          // MOVE 'CALLED3' TO WS-PROG
          work.setProg(CONSTANTS.LITERAL_CALLED3_B_);
//  CALL WS-PROG USING WS-PARM
          // CALL WS-PROG USING WS-PARM
               this.setRc( called3.call(parm));
          called3 = SpringContextHandler.getFreshBean("called3",Called3.class);
          SpringContextHandler.getFreshBean("called2",BaseProcess.class);
          SpringContextHandler.getFreshBean(String.valueOf(work.getProg1()).toLowerCase(),BaseProcess.class);
//  STOP RUN
          throw Terminate.TERMINATE;
       } catch(Exception e) {
            handleErrorCode(e);
            throw e;
       }
      
      // end of process method
      }
  
  
      /**
* This method pre-initializes variables consistent with
* what a mainframe program would have done at the start of a program
*/
      @Override
      public void initVars() throws CFException {
      setProgramEnded(false);
          if(!isInitDone()) {
          	this.setRc(0);
          	setInitDone(true);
          }
        CFUtil.resetDecimalAsComma();
       }
  
      public void setFromCalled3(Object[] params) {
      int len = params.length;
         if (len > 0)
         if(params[0] instanceof Field) 
   parm.setString(((Field)params[0] ).toCharArray());
 else    parm.setString((char[])params[0] );
      }
  
  
  
  
  
  }
